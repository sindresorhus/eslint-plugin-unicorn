import {outdent} from 'outdent';
import {typescriptEslintParser} from '../scripts/parsers.js';
import {getTester} from './utils/test.js';

const {test} = getTester(import.meta);

const typeAware = code => ({
	code,
	filename: 'file.ts',
	languageOptions: {
		parser: typescriptEslintParser,
		parserOptions: {projectService: {allowDefaultProject: ['*.ts']}},
	},
});

const typeAnnotated = code => ({
	code,
	filename: 'file.ts',
	languageOptions: {
		parser: typescriptEslintParser,
	},
});

test.snapshot({
	valid: [
		'map.has(key)',
		'map.get(key)',
		'map.get(key) !== undefined',
		'if (map.get(key)) {}',
		'if (new URLSearchParams().get(key)) {}',
		'if (new Headers().get(key)) {}',
		'if (new FormData().get(key)) {}',
		'new URLSearchParams().get(key) !== undefined',
		'new Headers().get(key) !== undefined',
		'new FormData().get(key) !== undefined',
		'new URLSearchParams()?.get(key) !== null',
		'new URLSearchParams().get?.(key) !== null',
		'new URLSearchParams()[get](key) !== null',
		'new URLSearchParams().get(...key) !== null',
		'new URLSearchParams().get(key, value) !== null',
		'new URLSearchParams().get(/* comment */ key) !== null',
		'const URLSearchParams = class {get() {} has() {}}; new URLSearchParams().get(key) !== null',
		'const parameters = createParameters(); parameters.get(key) !== null',
		typeAware('declare const map: Map<string, boolean>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, false>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, number>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, 0>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, string>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, "">; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, bigint>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, 0n>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, object | undefined>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, object | null>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, unknown>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, any>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, {}>; if (map.get(key)) {}'),
		typeAware('export {}; class Empty {} declare const map: Map<string, Empty>; if (map.get(key)) {}'),
		typeAware('export {}; interface Empty {} declare const map: Map<string, Empty>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, {id: string}>; if (map.get(key)) {}'),
		typeAware('export {}; class Value {id = 1;} declare const map: Map<string, Value>; if (map.get(key)) {}'),
		typeAware('export {}; interface Value {id: string} declare const map: Map<string, Value>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, {toString(): string}>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, undefined>; map.get(key) !== undefined'),
		typeAware('declare const map: Map<string, object | undefined>; map.get(key) != null'),
		typeAware('declare const map: Map<string, object | null>; map.get(key) != null'),
		typeAware('declare const map: Map<string, object | null>; map.get(key) != undefined'),
		typeAware('declare const map: Map<string, object | null>; map.get(key) == undefined'),
		typeAware('declare const map: Map<string, object | null>; map.get(key) != void 0'),
		typeAware('declare const map: Map<string, object | null>; map.get(key) == void 0'),
		typeAware('declare const map: Map<string, object | undefined>; map.get(key) === undefined'),
		typeAware('declare const map: Map<string, object>; map.get(key) === null'),
		typeAware('declare const map: Map<string, object>; map.get(key) !== null'),
		typeAware('declare const map: Map<string, object | undefined>; map.get(key) !== void 0'),
		typeAware('declare const map: Map<string, object>; map?.get(key) !== undefined'),
		typeAware('declare const map: Map<string, object>; map.get?.(key) !== undefined'),
		typeAware('declare const map: Map<string, object>; map[get](key) !== undefined'),
		typeAware('declare const map: Map<string, object>; map.get(...key) !== undefined'),
		typeAware('declare const map: Map<string, object>; map.get(key, value) !== undefined'),
		typeAware('declare const map: Map<string, object>; map.get(/* comment */ key) !== undefined'),
		// Boolean context with a comment inside the call: the comment guard skips it (distinct from the comparison path above)
		typeAware('declare const map: Map<string, object>; if (map.get(/* comment */ key)) {}'),
		// A `satisfies` expression is not recognized as an `undefined` sentinel
		typeAware('declare const map: Map<string, object>; map.get(key) !== (undefined satisfies undefined)'),
		typeAware('declare const cache: {get(key: string): object | undefined; has(key: string): boolean}; cache.get(key) !== undefined'),
		typeAware('import {Map} from "immutable"; declare const map: Map<string, object>; map.get(key) !== undefined'),
		typeAware('export {}; class Map<K, V> {get(key: K): V | undefined {return undefined;} has(key: K) {return true;}} declare const map: Map<string, object>; map.get(key) !== undefined'),
		typeAware('export {}; interface Map<K, V> {get(key: K): V | undefined; has(key: K): boolean} declare const map: Map<string, object>; map.get(key) !== undefined'),
		'const map = new Map([[key, {}]]); map.get(key) !== undefined',
		typeAware('const map = new Map<string, object | undefined>([[key, {}]]); map.get(key) !== undefined'),
		typeAware('new Map<string, object | undefined>(iterable).get(key) !== undefined'),
		typeAware('new Map<string, object>([[key, undefined]]).get(key) !== undefined'),
		typeAware('new Map<string, object>([[key, {}], [otherKey, undefined]]).get(key) !== undefined'),
		'const map = new Map([[key, undefined]]); map.get(key) !== undefined',
		'const map = new Map([[key, {}]]); map.set(otherKey, undefined); map.get(key) !== undefined',
		'new Map([[key, undefined]]).get(key) !== undefined',
		'new Map([[key, {}], [otherKey, undefined]]).get(key) !== undefined',
		'if (new Map([[key, false]]).get(key)) {}',
		'if (new Map([[key, 0]]).get(key)) {}',
		'if (new Map([[key, ""]]).get(key)) {}',
		'new Map([[key, null]]).get(key) != null',
		'new Map([[key, null]]).get(key) != undefined',
		'new Map([[key, null]]).get(key) == void 0',
		'if (new Map([[key, null]]).get(key)) {}',
		'function foo(undefined) { new Map([[key, {}]]).get(key) !== undefined; }',
		'new Map(iterable).get(key) !== undefined',
		'new Map([[key]]).get(key) !== undefined',
		'new Map([entry]).get(key) !== undefined',
		'const Map = class {get() {} has() {}}; new Map([[key, {}]]).get(key) !== undefined',
		typeAnnotated('new ReadonlyMap<string, object>().get(key) !== undefined'),
		typeAnnotated('import {Map} from "immutable"; declare const map: Map<string, object>; map.get(key) !== undefined'),
		typeAnnotated('export {}; interface Map<K, V> {get(key: K): V | undefined; has(key: K): boolean} declare const map: Map<string, object>; map.get(key) !== undefined'),
		typeAnnotated('export {}; interface URLSearchParams {get(key: string): string | null; has(key: string): boolean} declare const parameters: URLSearchParams; parameters.get(key) !== null'),
		typeAnnotated('type Value = undefined; declare const map: Map<string, Value>; map.get(key) !== undefined'),
		typeAnnotated('type Value = false; declare const map: Map<string, Value>; if (map.get(key)) {}'),
		typeAnnotated('type Value = Value; declare const map: Map<string, Value>; map.get(key) !== undefined'),
		typeAnnotated('type ObjectMap<Value extends object> = Map<string, Value>; declare const map: ObjectMap<any>; map.get(key) !== undefined'),
		typeAware('function foo<Value>(map: Map<string, Value>) { map.get(key) !== undefined; }'),
		typeAware('function foo<Value>(map: Map<string, Value>) { map.get(key) != undefined; }'),
		typeAware('function foo<Value>(map: Map<string, Value>) { map.get(key) != null; }'),
		typeAnnotated('function foo<Value extends object>(map: Map<string, Value>) { map.get(key) !== undefined; }'),
		typeAnnotated('function foo<Value>(map: Map<string, Value>) { map.get(key) != undefined; }'),
		typeAnnotated('new Map<string, object>([[key, undefined]]).get(key) !== undefined'),
		typeAnnotated('class Empty {} declare const map: Map<string, Empty>; if (map.get(key)) {}'),
		typeAnnotated('interface Empty {} declare const map: Map<string, Empty>; if (map.get(key)) {}'),
		typeAnnotated('interface Value {id: string} declare const map: Map<string, Value>; if (map.get(key)) {}'),
		typeAnnotated('type Value = {id: string}; declare const map: Map<string, Value>; if (map.get(key)) {}'),
		typeAnnotated('class Value {id = 1;} declare const map: Map<string, Value>; if (map.get(key)) {}'),
		typeAnnotated('function foo(map: Map<string, object | undefined>) { map.get(key) !== undefined; }'),
		typeAware('declare const map: Map<string, object>; function foo(undefined: unknown) { map.get(key) !== undefined; }'),
	],
	invalid: [
		'new URLSearchParams().get(key) !== null',
		'new URLSearchParams().get(key) === null',
		'new URLSearchParams().get(key) != null',
		'new URLSearchParams().get(key) == null',
		'new URLSearchParams().get(key) != undefined',
		'new URLSearchParams().get(key) == void 0',
		'null !== new URLSearchParams().get(key)',
		'null === new URLSearchParams().get(key)',
		'new URLSearchParams().get(key) /* comment */ !== null',
		'new Headers().get(key) !== null',
		'new Headers().get(key) != undefined',
		'new FormData().get(key) !== null',
		'new FormData().get(key) == void 0',
		outdent`
			const parameters = new URLSearchParams();
			if (parameters.get(key) !== null) {}
		`,
		typeAware('declare const map: Map<string, object>; map.get(key) !== undefined'),
		typeAware('declare const map: Map<string, object>; map.get(key) === undefined'),
		typeAware('declare const map: Map<string, object>; map.get(key) !== void 0'),
		typeAware('declare const map: Map<string, object>; void 0 !== map.get(key)'),
		typeAware('declare const map: Map<string, object>; map.get(key) === void 0'),
		typeAware('declare const map: Map<string, object>; map.get(key) != undefined'),
		typeAware('declare const map: Map<string, object>; map.get(key) == void 0'),
		typeAware('declare const map: Map<string, object>; map.get(key) != null'),
		typeAware('declare const map: Map<string, object>; map.get(key) == null'),
		typeAware('declare const parameters: URLSearchParams | Headers; parameters.get(key) !== null'),
		typeAware('declare const data: URLSearchParams | FormData; data.get(key) == void 0'),
		typeAware('declare const map: Map<string, object> | ReadonlyMap<string, object>; map.get(key) !== undefined'),
		typeAware('declare const map: Map<string, object> & {brand: true}; map.get(key) !== undefined'),
		typeAware('declare const map: Map<string, object>; (map as Map<string, object>).get(key) !== undefined'),
		typeAware('declare const map: Map<string, object> | undefined; (map!).get(key) !== undefined'),
		typeAware('declare const map: Map<string, object>; declare const otherMap: Map<string, object>; (condition ? map : otherMap).get(key) !== undefined'),
		typeAware('new Map<string, object>().get(key) !== undefined'),
		typeAware('new Map<string, object | undefined>().get(key) !== undefined'),
		typeAware('new Map<string, object | undefined>([[key, {}]]).get(key) !== undefined'),
		typeAware('new WeakMap<object, object>().get(key) !== undefined'),
		typeAware('declare const map: Map<string, string[]>; if (map.get(key)) {}'),
		typeAware('declare const map: ReadonlyMap<string, object>; if (!map.get(key)) {}'),
		typeAware('declare const map: WeakMap<object, object>; Boolean(map.get(key))'),
		typeAware('type ObjectMap = Map<string, object>; declare const map: ObjectMap; map.get(key) !== undefined'),
		typeAware('declare const map: Map<string, true>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, symbol>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, "value">; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, 1>; if (map.get(key)) {}'),
		typeAware('declare const map: Map<string, 1n>; if (map.get(key)) {}'),
		'new Map().get(key) !== undefined',
		'new Map([[key, {}]]).get(key) !== undefined',
		'new Map([[key, []]]).get(key) !== undefined',
		'new Map([[key, null]]).get(key) !== undefined',
		'new Map([[key, null]]).get(key) === undefined',
		'new Map([[key, {}]]).get(key) != null',
		'new WeakMap([[key, {}]]).get(key) !== undefined',
		'if (new Map().get(key)) {}',
		'if (new Map([[key, {}]]).get(key)) {}',
		'if (new Map([[key, []]]).get(key)) {}',
		'if (new Map([[key, true]]).get(key)) {}',
		'if (new Map([[key, "value"]]).get(key)) {}',
		typeAnnotated('declare const map: Map<string, object>; map.get(key) !== undefined'),
		typeAnnotated('declare const map: Map<string, object>; map.get(key) != undefined'),
		typeAnnotated('new Map<string, object | undefined>().get(key) !== undefined'),
		typeAnnotated('new Map<string, object | undefined>([[key, {}]]).get(key) !== undefined'),
		typeAnnotated('declare const map: ReadonlyMap<string, object>; map.get(key) === undefined'),
		typeAnnotated('declare const map: WeakMap<object, object>; map.get(key) != null'),
		typeAnnotated('type ObjectMap = Map<string, object>; declare const map: ObjectMap; map.get(key) !== undefined'),
		typeAnnotated('declare const value: unknown; (value as Map<string, object>).get(key) !== undefined'),
		typeAnnotated('declare const map: Map<string, string[]>; if (map.get(key)) {}'),
		typeAnnotated('declare const map: Map<string, symbol>; if (map.get(key)) {}'),
		typeAnnotated('function foo(map: Map<string, object>) { map.get(key) !== undefined; }'),
		typeAnnotated('declare const parameters: URLSearchParams; parameters.get(key) !== null'),
		typeAnnotated('type Parameters = URLSearchParams; declare const parameters: Parameters; parameters.get(key) === null'),
	],
});
