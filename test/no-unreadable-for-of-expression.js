import outdent from 'outdent';
import {getTester, parsers} from './utils/test.js';

const {test} = getTester(import.meta);

const typescript = code => ({
	code,
	languageOptions: {
		parser: parsers.typescript,
	},
});

test.snapshot({
	valid: [
		'for (const item of items) {}',
		'for await (const item of items) {}',
		'for (const item of object.items) {}',
		'for (const item of this.items) {}',
		'class Foo extends Bar { method() { for (const item of super.items) {} } }',
		'for (const item of [first, second]) {}',
		'for (const [index, item] of [first, second].entries()) {}',
		'for (const item of getItems()) {}',
		'for (const item of getItems(argument)) {}',
		'for (const item of getItems(first, object.second, "third")) {}',
		'for (const item of getItems(`argument`)) {}',
		'for (const item of getItems(createArgument())) {}',
		'for (const item of object.getItems(argument)) {}',
		'for (const item of object.getItems()) {}',
		'for (const item of Object.keys(object)) {}',
		'for (const item of Object.values(object.items)) {}',
		'for (const [key, value] of Object.entries(getObject())) {}',
		'const Object = {keys: value => value}; for (const item of Object.keys(object)) {}',
		'for (const item of (items)) {}',
		'for (const item of ((object.items))) {}',
		'for (const item of items.map(item => item.value)) {}',
		'for (const item of items.map(callback)) {}',
		'for (const item of (items.map(callback))) {}',
		'for (const item of items.filter(item => item.isEnabled)) {}',
		'for await (const item of items.map(callback)) {}',
		'for await (const item of items.filter(callback)) {}',
		'const Iterator = {from: value => value}; for (const item of Iterator.from(items).map(callback)) {}',
		'const Iterator = {concat: value => value}; for (const item of Iterator.concat(items).map(callback)) {}',
		'const Iterator = {zip: value => value}; for (const item of Iterator.zip(items).filter(callback)) {}',
		'const Iterator = {zipKeyed: value => value}; for (const item of Iterator.zipKeyed(items).take(1)) {}',
		'const globalThis = {Iterator}; for (const item of globalThis.Iterator.from(items).map(callback)) {}',
		typescript('for (const item of items as string[]) {}'),
		typescript('for (const item of (items satisfies Iterable<string>)) {}'),
		typescript('for (const item of items!) {}'),
		typescript('for (const item of getItems() as string[]) {}'),
		typescript('for (const item of items.map(callback) as string[]) {}'),
		typescript('for (const item of items.filter(callback) as string[]) {}'),
	],
	invalid: [
		'for (const item of getItems(createArgument(seed))) {}',
		'for (const item of getItems(argument || fallback)) {}',
		'for (const item of getItems(`prefix-${argument}`)) {}', // eslint-disable-line no-template-curly-in-string
		'for (const item of items[method]()) {}',
		'for (const item of items[index]) {}',
		'for (const item of items?.values()) {}',
		'for (const item of condition ? first : second) {}',
		'for (const item of first || second) {}',
		'for (const item of new Set(items)) {}',
		'for (const item of tag`items`) {}',
		'for (const item of Object.keys(Object.fromEntries(entries))) {}',
		'for (const item of Object.values(items.filter(item => item.isEnabled))) {}',
		'for (const item of items?.map(item => item.value)) {}',
		'for (const item of items?.map(callback)) {}',
		'for (const item of items.filter?.(item => item.isEnabled)) {}',
		'for (const item of items.filter?.(callback)) {}',
		'for (const item of globalThis.Iterator.from(items).map(callback)) {}',
		'for (const item of (globalThis).Iterator.from(items).map(callback)) {}',
		'for (const item of (Iterator).from(items).map(callback)) {}',
		'for (const item of Iterator.from(items).map(item => item.value)) {}',
		'for (const item of Iterator.from(items).map(callback)) {}',
		'for (const item of Iterator.from(items).filter(item => item.isEnabled)) {}',
		'for (const item of Iterator.from(items).filter(callback)) {}',
		'for (const item of Iterator.from(items).take(1)) {}',
		'for (const item of Iterator.from(items).drop(1)) {}',
		'for (const item of Iterator.from(items).flatMap(callback)) {}',
		'for (const item of Iterator.concat(first, second).map(callback)) {}',
		'for (const item of Iterator.zip(iterables).filter(callback)) {}',
		'for (const item of Iterator.zipKeyed(iterables).take(1)) {}',
		'for (const item of Iterator.from(items).take(1).map(callback)) {}',
		'for (const item of items.values().map(item => item.value)) {}',
		'for (const item of items.values().map(callback)) {}',
		'for (const item of items.values().filter(callback)) {}',
		'for (const item of items.values().take(1)) {}',
		'for (const item of items.values().take(1).map(callback)) {}',
		'for (const item of items.keys().map(callback)) {}',
		'for (const item of items.entries().filter(callback)) {}',
		'for (const item of items.matchAll(pattern).map(callback)) {}',
		'for await (const item of getItems(createArgument(seed))) {}',
		typescript('for (const item of getItems(createArgument(seed)) as string[]) {}'),
		typescript('for (const item of (Iterator as typeof Iterator).from(items).map(callback)) {}'),
		typescript('for (const item of (Iterator.from(items) as Iterator<string>).map(callback)) {}'),
		typescript('for (const item of Iterator.from(items).take(1) as Iterator<string>) {}'),
		typescript('for (const item of (items[index] satisfies Iterable<string>)) {}'),
		outdent`
			for (
				const item of getItems(createArgument(seed))
			) {}
		`,
	],
});
