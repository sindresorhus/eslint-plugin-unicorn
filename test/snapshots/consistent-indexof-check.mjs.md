# Snapshot report for `test/consistent-indexof-check.mjs`

The actual snapshot is saved in `consistent-indexof-check.mjs.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): const index = foo.indexOf('bar'); if (index < 0) {}

> Input

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | if (index < 0) {}␊
    `

> Output

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | if (index === -1) {}␊
    `

> Error 1/1

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
    > 3 | if (index < 0) {}␊
        |     ^^^^^^^^^ Prefer \`index === -1\` over \`index < 0\`.␊
    `

## invalid(2): const index = foo.indexOf('bar'); if (0 > index) {}

> Input

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | if (0 > index) {}␊
    `

> Output

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | if (index === -1) {}␊
    `

> Error 1/1

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
    > 3 | if (0 > index) {}␊
        |     ^^^^^^^^^ Prefer \`index === -1\` over \`0 > index\`.␊
    `

## invalid(3): const index = foo.indexOf('bar'); if (index >= 0) {}

> Input

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | if (index >= 0) {}␊
    `

> Output

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | if (index !== -1) {}␊
    `

> Error 1/1

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
    > 3 | if (index >= 0) {}␊
        |     ^^^^^^^^^^ Prefer \`index !== -1\` over \`index >= 0\`.␊
    `

## invalid(4): const index = foo.indexOf('bar'); if (0 <= index) {}

> Input

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | if (0 <= index) {}␊
    `

> Output

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | if (index !== -1) {}␊
    `

> Error 1/1

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
    > 3 | if (0 <= index) {}␊
        |     ^^^^^^^^^^ Prefer \`index !== -1\` over \`0 <= index\`.␊
    `

## invalid(5): const index = foo.indexOf('bar'); function foo () { if (index < 0) {} }

> Input

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | function foo () {␊
      4 | 	if (index < 0) {}␊
      5 | }␊
    `

> Output

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | function foo () {␊
      4 | 	if (index === -1) {}␊
      5 | }␊
    `

> Error 1/1

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | function foo () {␊
    > 4 | 	if (index < 0) {}␊
        | 	    ^^^^^^^^^ Prefer \`index === -1\` over \`index < 0\`.␊
      5 | }␊
    `

## invalid(6): const index = foo.indexOf('bar'); if (index < -1) {}

> Input

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | if (index < -1) {}␊
    `

> Output

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | if (index === -1) {}␊
    `

> Error 1/1

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
    > 3 | if (index < -1) {}␊
        |     ^^^^^^^^^^ Prefer \`index === -1\` over \`index < -1\`.␊
    `

## invalid(7): const index = foo.indexOf('bar'); if (index >= +0) {}

> Input

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | if (index >= +0) {}␊
    `

> Output

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | if (index !== -1) {}␊
    `

> Error 1/1

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
    > 3 | if (index >= +0) {}␊
        |     ^^^^^^^^^^^ Prefer \`index !== -1\` over \`index >= +0\`.␊
    `

## invalid(8): const index = foo.indexOf('bar'); if (index > ~0) {}

> Input

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | if (index > ~0) {}␊
    `

> Output

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | if (index !== -1) {}␊
    `

> Error 1/1

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
    > 3 | if (index > ~0) {}␊
        |     ^^^^^^^^^^ Prefer \`index !== -1\` over \`index > ~0\`.␊
    `

## invalid(9): const index = foo.indexOf('bar'); if (index > -(+1)) {}

> Input

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | if (index > -(+1)) {}␊
    `

> Output

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
      3 | if (index !== -1) {}␊
    `

> Error 1/1

    `␊
      1 | const index = foo.indexOf('bar');␊
      2 |␊
    > 3 | if (index > -(+1)) {}␊
        |     ^^^^^^^^^^^^^ Prefer \`index !== -1\` over \`index > -(+1)\`.␊
    `

## invalid(10): const index = foo.lastIndexOf('bar'); if (index > -1) {}

> Input

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | if (index > -1) {}␊
    `

> Output

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | if (index !== -1) {}␊
    `

> Error 1/1

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
    > 3 | if (index > -1) {}␊
        |     ^^^^^^^^^^ Prefer \`index !== -1\` over \`index > -1\`.␊
    `

## invalid(11): const index = foo.lastIndexOf('bar'); index > -1 ? 'exists' : 'not exists';

> Input

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | index > -1 ? 'exists' : 'not exists';␊
    `

> Output

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | index !== -1 ? 'exists' : 'not exists';␊
    `

> Error 1/1

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
    > 3 | index > -1 ? 'exists' : 'not exists';␊
        | ^^^^^^^^^^ Prefer \`index !== -1\` over \`index > -1\`.␊
    `

## invalid(12): const index = foo.lastIndexOf('bar'); conditionLeft && index > -1 ? 'exists' : 'not exists';

> Input

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | conditionLeft && index > -1 ? 'exists' : 'not exists';␊
    `

> Output

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | conditionLeft && index !== -1 ? 'exists' : 'not exists';␊
    `

> Error 1/1

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
    > 3 | conditionLeft && index > -1 ? 'exists' : 'not exists';␊
        |                  ^^^^^^^^^^ Prefer \`index !== -1\` over \`index > -1\`.␊
    `

## invalid(13): const index = foo.lastIndexOf('bar'); index > -1 && conditionRight ? 'exists' : 'not exists';

> Input

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | index > -1 && conditionRight ? 'exists' : 'not exists';␊
    `

> Output

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | index !== -1 && conditionRight ? 'exists' : 'not exists';␊
    `

> Error 1/1

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
    > 3 | index > -1 && conditionRight ? 'exists' : 'not exists';␊
        | ^^^^^^^^^^ Prefer \`index !== -1\` over \`index > -1\`.␊
    `

## invalid(14): const index = foo.lastIndexOf('bar'); (conditionLeft || index > -1) ? 'exists' : 'not exists';

> Input

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | (conditionLeft || index > -1) ? 'exists' : 'not exists';␊
    `

> Output

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | (conditionLeft || index !== -1) ? 'exists' : 'not exists';␊
    `

> Error 1/1

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
    > 3 | (conditionLeft || index > -1) ? 'exists' : 'not exists';␊
        |                   ^^^^^^^^^^ Prefer \`index !== -1\` over \`index > -1\`.␊
    `

## invalid(15): const index = foo.lastIndexOf('bar'); (conditionLeft ?? index > -1) ? 'exists' : 'not exists';

> Input

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | (conditionLeft ?? index > -1) ? 'exists' : 'not exists';␊
    `

> Output

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | (conditionLeft ?? index !== -1) ? 'exists' : 'not exists';␊
    `

> Error 1/1

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
    > 3 | (conditionLeft ?? index > -1) ? 'exists' : 'not exists';␊
        |                   ^^^^^^^^^^ Prefer \`index !== -1\` over \`index > -1\`.␊
    `

## invalid(16): const index = foo.lastIndexOf('bar'); while (index > -1) { // Do something }

> Input

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | while (index > -1) {␊
      4 | 	// Do something␊
      5 | }␊
    `

> Output

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | while (index !== -1) {␊
      4 | 	// Do something␊
      5 | }␊
    `

> Error 1/1

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
    > 3 | while (index > -1) {␊
        |        ^^^^^^^^^^ Prefer \`index !== -1\` over \`index > -1\`.␊
      4 | 	// Do something␊
      5 | }␊
    `

## invalid(17): const index = foo.lastIndexOf('bar'); for (;index > -1;) { // Do something }

> Input

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | for (;index > -1;) {␊
      4 | 	// Do something␊
      5 | }␊
    `

> Output

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | for (;index !== -1;) {␊
      4 | 	// Do something␊
      5 | }␊
    `

> Error 1/1

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
    > 3 | for (;index > -1;) {␊
        |       ^^^^^^^^^^ Prefer \`index !== -1\` over \`index > -1\`.␊
      4 | 	// Do something␊
      5 | }␊
    `

## invalid(18): const index = foo.lastIndexOf('bar'); do { } while (index > -1);

> Input

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | do {␊
      4 |␊
      5 | } while (index > -1);␊
    `

> Output

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | do {␊
      4 |␊
      5 | } while (index !== -1);␊
    `

> Error 1/1

    `␊
      1 | const index = foo.lastIndexOf('bar');␊
      2 |␊
      3 | do {␊
      4 |␊
    > 5 | } while (index > -1);␊
        |          ^^^^^^^^^^ Prefer \`index !== -1\` over \`index > -1\`.␊
    `

## invalid(19): const arr = [5, 12, 8, 130, 44]; const index = arr.findIndex(element => element > 10); if (index > -1) {}

> Input

    `␊
      1 | const arr = [5, 12, 8, 130, 44];␊
      2 | const index = arr.findIndex(element => element > 10);␊
      3 |␊
      4 | if (index > -1) {}␊
    `

> Output

    `␊
      1 | const arr = [5, 12, 8, 130, 44];␊
      2 | const index = arr.findIndex(element => element > 10);␊
      3 |␊
      4 | if (index !== -1) {}␊
    `

> Error 1/1

    `␊
      1 | const arr = [5, 12, 8, 130, 44];␊
      2 | const index = arr.findIndex(element => element > 10);␊
      3 |␊
    > 4 | if (index > -1) {}␊
        |     ^^^^^^^^^^ Prefer \`index !== -1\` over \`index > -1\`.␊
    `

## invalid(20): const arr = [5, 12, 8, 130, 44]; const index = arr.findLastIndex(element => element > 10); if (index > -1) {}

> Input

    `␊
      1 | const arr = [5, 12, 8, 130, 44];␊
      2 | const index = arr.findLastIndex(element => element > 10);␊
      3 |␊
      4 | if (index > -1) {}␊
    `

> Output

    `␊
      1 | const arr = [5, 12, 8, 130, 44];␊
      2 | const index = arr.findLastIndex(element => element > 10);␊
      3 |␊
      4 | if (index !== -1) {}␊
    `

> Error 1/1

    `␊
      1 | const arr = [5, 12, 8, 130, 44];␊
      2 | const index = arr.findLastIndex(element => element > 10);␊
      3 |␊
    > 4 | if (index > -1) {}␊
        |     ^^^^^^^^^^ Prefer \`index !== -1\` over \`index > -1\`.␊
    `

## invalid(21): const arr = [5, 12, 8, 130, 44]; const index = arr.findLastIndex(element => element > 10); if (index > -1) {} function test () { if (index > -1) {} }

> Input

    `␊
      1 | const arr = [5, 12, 8, 130, 44];␊
      2 | const index = arr.findLastIndex(element => element > 10);␊
      3 |␊
      4 | if (index > -1) {}␊
      5 |␊
      6 | function test () {␊
      7 | 	if (index > -1) {}␊
      8 | }␊
    `

> Output

    `␊
      1 | const arr = [5, 12, 8, 130, 44];␊
      2 | const index = arr.findLastIndex(element => element > 10);␊
      3 |␊
      4 | if (index !== -1) {}␊
      5 |␊
      6 | function test () {␊
      7 | 	if (index !== -1) {}␊
      8 | }␊
    `

> Error 1/2

    `␊
      1 | const arr = [5, 12, 8, 130, 44];␊
      2 | const index = arr.findLastIndex(element => element > 10);␊
      3 |␊
    > 4 | if (index > -1) {}␊
        |     ^^^^^^^^^^ Prefer \`index !== -1\` over \`index > -1\`.␊
      5 |␊
      6 | function test () {␊
      7 | 	if (index > -1) {}␊
      8 | }␊
    `

> Error 2/2

    `␊
      1 | const arr = [5, 12, 8, 130, 44];␊
      2 | const index = arr.findLastIndex(element => element > 10);␊
      3 |␊
      4 | if (index > -1) {}␊
      5 |␊
      6 | function test () {␊
    > 7 | 	if (index > -1) {}␊
        | 	    ^^^^^^^^^^ Prefer \`index !== -1\` over \`index > -1\`.␊
      8 | }␊
    `

## invalid(22): let index; // do stuff index = arr.findLastIndex(element => element > 10); if (index > -1) {} function test () { if (index > -1) {} }

> Input

    `␊
       1 | let index;␊
       2 |␊
       3 | // do stuff␊
       4 |␊
       5 | index = arr.findLastIndex(element => element > 10);␊
       6 |␊
       7 | if (index > -1) {}␊
       8 |␊
       9 | function test () {␊
      10 | 	if (index > -1) {}␊
      11 | }␊
    `

> Output

    `␊
       1 | let index;␊
       2 |␊
       3 | // do stuff␊
       4 |␊
       5 | index = arr.findLastIndex(element => element > 10);␊
       6 |␊
       7 | if (index !== -1) {}␊
       8 |␊
       9 | function test () {␊
      10 | 	if (index !== -1) {}␊
      11 | }␊
    `

> Error 1/2

    `␊
       1 | let index;␊
       2 |␊
       3 | // do stuff␊
       4 |␊
       5 | index = arr.findLastIndex(element => element > 10);␊
       6 |␊
    >  7 | if (index > -1) {}␊
         |     ^^^^^^^^^^ Prefer \`index !== -1\` over \`index > -1\`.␊
       8 |␊
       9 | function test () {␊
      10 | 	if (index > -1) {}␊
      11 | }␊
    `

> Error 2/2

    `␊
       1 | let index;␊
       2 |␊
       3 | // do stuff␊
       4 |␊
       5 | index = arr.findLastIndex(element => element > 10);␊
       6 |␊
       7 | if (index > -1) {}␊
       8 |␊
       9 | function test () {␊
    > 10 | 	if (index > -1) {}␊
         | 	    ^^^^^^^^^^ Prefer \`index !== -1\` over \`index > -1\`.␊
      11 | }␊
    `
