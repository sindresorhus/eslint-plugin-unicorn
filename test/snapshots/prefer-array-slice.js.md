# Snapshot report for `test/prefer-array-slice.js`

The actual snapshot is saved in `prefer-array-slice.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): process.argv.splice(2)[0]

> Input

    `␊
      1 | process.argv.splice(2)[0]␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | process.argv.splice(2)[0]␊
        |              ^^^^^^ Prefer \`Array#slice()\` over \`Array#splice()\` when reading from the returned array.␊
    ␊
    Suggestion 1/1: Use \`Array#slice()\`.:␊
      1 | process.argv.slice(2)[0]␊
    `

## invalid(2): array.splice(index)[0]

> Input

    `␊
      1 | array.splice(index)[0]␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | array.splice(index)[0]␊
        |       ^^^^^^ Prefer \`Array#slice()\` over \`Array#splice()\` when reading from the returned array.␊
    ␊
    Suggestion 1/1: Use \`Array#slice()\`.:␊
      1 | array.slice(index)[0]␊
    `

## invalid(3): array.splice(index)[offset]

> Input

    `␊
      1 | array.splice(index)[offset]␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | array.splice(index)[offset]␊
        |       ^^^^^^ Prefer \`Array#slice()\` over \`Array#splice()\` when reading from the returned array.␊
    ␊
    Suggestion 1/1: Use \`Array#slice()\`.:␊
      1 | array.slice(index)[offset]␊
    `

## invalid(4): array.splice(index).at(0)

> Input

    `␊
      1 | array.splice(index).at(0)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | array.splice(index).at(0)␊
        |       ^^^^^^ Prefer \`Array#slice()\` over \`Array#splice()\` when reading from the returned array.␊
    ␊
    Suggestion 1/1: Use \`Array#slice()\`.:␊
      1 | array.slice(index).at(0)␊
    `

## invalid(5): object.array.splice(index)[0]

> Input

    `␊
      1 | object.array.splice(index)[0]␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | object.array.splice(index)[0]␊
        |              ^^^^^^ Prefer \`Array#slice()\` over \`Array#splice()\` when reading from the returned array.␊
    ␊
    Suggestion 1/1: Use \`Array#slice()\`.:␊
      1 | object.array.slice(index)[0]␊
    `

## invalid(6): array.splice(/* comment */ index)[0]

> Input

    `␊
      1 | array.splice(/* comment */ index)[0]␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | array.splice(/* comment */ index)[0]␊
        |       ^^^^^^ Prefer \`Array#slice()\` over \`Array#splice()\` when reading from the returned array.␊
    ␊
    Suggestion 1/1: Use \`Array#slice()\`.:␊
      1 | array.slice(/* comment */ index)[0]␊
    `

## invalid(1): array.splice(index as number)[0]

> Input

    `␊
      1 | array.splice(index as number)[0]␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | array.splice(index as number)[0]␊
        |       ^^^^^^ Prefer \`Array#slice()\` over \`Array#splice()\` when reading from the returned array.␊
    ␊
    Suggestion 1/1: Use \`Array#slice()\`.:␊
      1 | array.slice(index as number)[0]␊
    `

## invalid(2): array.splice(<number>index)[0]

> Input

    `␊
      1 | array.splice(<number>index)[0]␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | array.splice(<number>index)[0]␊
        |       ^^^^^^ Prefer \`Array#slice()\` over \`Array#splice()\` when reading from the returned array.␊
    ␊
    Suggestion 1/1: Use \`Array#slice()\`.:␊
      1 | array.slice(<number>index)[0]␊
    `

## invalid(3): array.splice(index!)[0]

> Input

    `␊
      1 | array.splice(index!)[0]␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | array.splice(index!)[0]␊
        |       ^^^^^^ Prefer \`Array#slice()\` over \`Array#splice()\` when reading from the returned array.␊
    ␊
    Suggestion 1/1: Use \`Array#slice()\`.:␊
      1 | array.slice(index!)[0]␊
    `

## invalid(4): array.splice(index satisfies number)[0]

> Input

    `␊
      1 | array.splice(index satisfies number)[0]␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | array.splice(index satisfies number)[0]␊
        |       ^^^^^^ Prefer \`Array#slice()\` over \`Array#splice()\` when reading from the returned array.␊
    ␊
    Suggestion 1/1: Use \`Array#slice()\`.:␊
      1 | array.slice(index satisfies number)[0]␊
    `

## invalid(5): (array as string[]).splice(index)[0]

> Input

    `␊
      1 | (array as string[]).splice(index)[0]␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (array as string[]).splice(index)[0]␊
        |                     ^^^^^^ Prefer \`Array#slice()\` over \`Array#splice()\` when reading from the returned array.␊
    ␊
    Suggestion 1/1: Use \`Array#slice()\`.:␊
      1 | (array as string[]).slice(index)[0]␊
    `
