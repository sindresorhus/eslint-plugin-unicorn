# Snapshot report for `test/prefer-set-methods.js`

The actual snapshot is saved in `prefer-set-methods.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): const a = new Set(); const b = new Set(); new Set([...a, ...b]);

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); new Set([...a, ...b]);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); new Set([...a, ...b]);␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^ Use \`Set#union()\` instead of spreading Sets into a new Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); a.union(b);␊
    `

## invalid(2): const a = new Set(); const b = new Set(); const c = new Set(); new Set([...a, ...b, ...c]);

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); const c = new Set(); new Set([...a, ...b, ...c]);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); const c = new Set(); new Set([...a, ...b, ...c]);␊
        |                                                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#union()\` instead of spreading Sets into a new Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); const c = new Set(); a.union(b).union(c);␊
    `

## invalid(3): const a = new Set(); const b = new Set(); const c = new Set(); new Set([...(condition ? a : b), ...c]);

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); const c = new Set(); new Set([...(condition ? a : b), ...c]);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); const c = new Set(); new Set([...(condition ? a : b), ...c]);␊
        |                                                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#union()\` instead of spreading Sets into a new Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); const c = new Set(); (condition ? a : b).union(c);␊
    `

## invalid(4): const a = new Set(); const c = new Set(); new Set([...(condition ? a : a), ...c]);

> Input

    `␊
      1 | const a = new Set(); const c = new Set(); new Set([...(condition ? a : a), ...c]);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const c = new Set(); new Set([...(condition ? a : a), ...c]);␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#union()\` instead of spreading Sets into a new Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const c = new Set(); (condition ? a : a).union(c);␊
    `

## invalid(5): const a = new Set(); const b = new Set(); const c = new Set(); foo new Set([...(condition ? a : b), ...c]);

> Input

    `␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | const c = new Set();␊
      4 | foo␊
      5 | new Set([...(condition ? a : b), ...c]);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | const c = new Set();␊
      4 | foo␊
    > 5 | new Set([...(condition ? a : b), ...c]);␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#union()\` instead of spreading Sets into a new Set.␊
    ␊
    Output:␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | const c = new Set();␊
      4 | foo␊
      5 | ;(condition ? a : b).union(c);␊
    `

## invalid(6): new Set([...(new Set()), ...(new Set())]);

> Input

    `␊
      1 | new Set([...(new Set()), ...(new Set())]);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | new Set([...(new Set()), ...(new Set())]);␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#union()\` instead of spreading Sets into a new Set.␊
    ␊
    Output:␊
      1 | (new Set()).union((new Set()));␊
    `

## invalid(7): function foo(a: Set<string>, b: Set<string>) { new Set([...a, ...b]); }

> Input

    `␊
      1 | function foo(a: Set<string>, b: Set<string>) { new Set([...a, ...b]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: Set<string>, b: Set<string>) { new Set([...a, ...b]); }␊
        |                                                ^^^^^^^^^^^^^^^^^^^^^ Use \`Set#union()\` instead of spreading Sets into a new Set.␊
    ␊
    Output:␊
      1 | function foo(a: Set<string>, b: Set<string>) { a.union(b); }␊
    `

## invalid(8): function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { new Set([...a, ...b]); }

> Input

    `␊
      1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { new Set([...a, ...b]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { new Set([...a, ...b]); }␊
        |                                                                ^^^^^^^^^^^^^^^^^^^^^ Use \`Set#union()\` instead of spreading Sets into a new Set.␊
    ␊
    Output:␊
      1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { a.union(b); }␊
    `

## invalid(9): function foo(a: Set<string>, b: Set<string>) { new Set([...(a satisfies Set<string>), ...(b as Set<string>)]); }

> Input

    `␊
      1 | function foo(a: Set<string>, b: Set<string>) { new Set([...(a satisfies Set<string>), ...(b as Set<string>)]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: Set<string>, b: Set<string>) { new Set([...(a satisfies Set<string>), ...(b as Set<string>)]); }␊
        |                                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#union()\` instead of spreading Sets into a new Set.␊
    ␊
    Output:␊
      1 | function foo(a: Set<string>, b: Set<string>) { (a satisfies Set<string>).union((b as Set<string>)); }␊
    `

## invalid(10): type Items = Set<string>; declare const a: Items; declare const b: Set<string>; new Set([...a, ...b]);

> Input 'file.ts'

    `␊
      1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; new Set([...a, ...b]);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; new Set([...a, ...b]);␊
        |                                                                                 ^^^^^^^^^^^^^^^^^^^^^ Use \`Set#union()\` instead of spreading Sets into a new Set.␊
    ␊
    Output:␊
      1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; a.union(b);␊
    `

## invalid(11): const a = new Set(); const b = new Set(); [...a].filter(value => b.has(value));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); [...a].filter(value => b.has(value));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); [...a].filter(value => b.has(value));␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#intersection()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#intersection()\`.:␊
      1 | const a = new Set(); const b = new Set(); a.intersection(b);␊
    `

## invalid(12): const a = new Set(); const b = new Set(); new Set([...a].filter(value => b.has(value)));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); new Set([...a].filter(value => b.has(value)));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); new Set([...a].filter(value => b.has(value)));␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#intersection()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#intersection()\`.:␊
      1 | const a = new Set(); const b = new Set(); a.intersection(b);␊
    `

## invalid(13): function foo(a: Set<string>, b: Set<string>) { return new Set(([...a].filter(value => b.has(value)) as string[])); }

> Input

    `␊
      1 | function foo(a: Set<string>, b: Set<string>) { return new Set(([...a].filter(value => b.has(value)) as string[])); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: Set<string>, b: Set<string>) { return new Set(([...a].filter(value => b.has(value)) as string[])); }␊
        |                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#intersection()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#intersection()\`.:␊
      1 | function foo(a: Set<string>, b: Set<string>) { return a.intersection(b); }␊
    `

## invalid(14): function foo(a: Set<string>, b: Set<string>) { return new Set((([...a].filter(value => b.has(value)) as string[])!)); }

> Input

    `␊
      1 | function foo(a: Set<string>, b: Set<string>) { return new Set((([...a].filter(value => b.has(value)) as string[])!)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: Set<string>, b: Set<string>) { return new Set((([...a].filter(value => b.has(value)) as string[])!)); }␊
        |                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#intersection()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#intersection()\`.:␊
      1 | function foo(a: Set<string>, b: Set<string>) { return a.intersection(b); }␊
    `

## invalid(15): const a = new Set(); const b = new Set(); [...a].filter(value => !b.has(value));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); [...a].filter(value => !b.has(value));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); [...a].filter(value => !b.has(value));␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#difference()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#difference()\`.:␊
      1 | const a = new Set(); const b = new Set(); a.difference(b);␊
    `

## invalid(16): const a = new Set(); const b = new Set(); new Set([...a].filter(value => !b.has(value)));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); new Set([...a].filter(value => !b.has(value)));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); new Set([...a].filter(value => !b.has(value)));␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#difference()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#difference()\`.:␊
      1 | const a = new Set(); const b = new Set(); a.difference(b);␊
    `

## invalid(17): function foo(a: Set<string>, b: Set<string>) { return [...a].filter(value => !b.has(value)); }

> Input

    `␊
      1 | function foo(a: Set<string>, b: Set<string>) { return [...a].filter(value => !b.has(value)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: Set<string>, b: Set<string>) { return [...a].filter(value => !b.has(value)); }␊
        |                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#difference()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#difference()\`.:␊
      1 | function foo(a: Set<string>, b: Set<string>) { return a.difference(b); }␊
    `

## invalid(18): function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return [...a].filter(value => !b.has(value)); }

> Input

    `␊
      1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return [...a].filter(value => !b.has(value)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return [...a].filter(value => !b.has(value)); }␊
        |                                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#difference()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#difference()\`.:␊
      1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return a.difference(b); }␊
    `

## invalid(19): type Items = Set<string>; declare const a: Items; declare const b: Set<string>; [...a].filter(value => !b.has(value));

> Input 'file.ts'

    `␊
      1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; [...a].filter(value => !b.has(value));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; [...a].filter(value => !b.has(value));␊
        |                                                                                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#difference()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#difference()\`.:␊
      1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; a.difference(b);␊
    `

## invalid(20): function foo(a: Set<string>, b: Set<string>) { return new Set(([...a].filter(value => !b.has(value)) as string[])); }

> Input

    `␊
      1 | function foo(a: Set<string>, b: Set<string>) { return new Set(([...a].filter(value => !b.has(value)) as string[])); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: Set<string>, b: Set<string>) { return new Set(([...a].filter(value => !b.has(value)) as string[])); }␊
        |                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#difference()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#difference()\`.:␊
      1 | function foo(a: Set<string>, b: Set<string>) { return a.difference(b); }␊
    `

## invalid(21): function foo(a: Set<string>, b: Set<string>) { return new Set((([...a].filter(value => !b.has(value)) as string[])!)); }

> Input

    `␊
      1 | function foo(a: Set<string>, b: Set<string>) { return new Set((([...a].filter(value => !b.has(value)) as string[])!)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: Set<string>, b: Set<string>) { return new Set((([...a].filter(value => !b.has(value)) as string[])!)); }␊
        |                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#difference()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#difference()\`.:␊
      1 | function foo(a: Set<string>, b: Set<string>) { return a.difference(b); }␊
    `

## invalid(22): const a = new Set(); const b = new Set(); const c = new Set(); foo new Set([...(condition ? a : b)].filter(value => !c.has(value)));

> Input

    `␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | const c = new Set();␊
      4 | foo␊
      5 | new Set([...(condition ? a : b)].filter(value => !c.has(value)));␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | const c = new Set();␊
      4 | foo␊
    > 5 | new Set([...(condition ? a : b)].filter(value => !c.has(value)));␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#difference()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#difference()\`.:␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | const c = new Set();␊
      4 | foo␊
      5 | ;(condition ? a : b).difference(c);␊
    `

## invalid(23): const a = new Set(); const b = new Set(); const c = new Set(); foo new Set([...(condition ? a : b)].filter(value => c.has(value)));

> Input

    `␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | const c = new Set();␊
      4 | foo␊
      5 | new Set([...(condition ? a : b)].filter(value => c.has(value)));␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | const c = new Set();␊
      4 | foo␊
    > 5 | new Set([...(condition ? a : b)].filter(value => c.has(value)));␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#intersection()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#intersection()\`.:␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | const c = new Set();␊
      4 | foo␊
      5 | ;(condition ? a : b).intersection(c);␊
    `

## invalid(24): const a = new Set(); const c = new Set(); new Set([...(condition ? a : a)].filter(value => c.has(value)));

> Input

    `␊
      1 | const a = new Set(); const c = new Set(); new Set([...(condition ? a : a)].filter(value => c.has(value)));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const c = new Set(); new Set([...(condition ? a : a)].filter(value => c.has(value)));␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#intersection()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#intersection()\`.:␊
      1 | const a = new Set(); const c = new Set(); (condition ? a : a).intersection(c);␊
    `

## invalid(25): function foo(a: Set<string>, b: Set<string>) { return [...a].filter(value => b.has(value)); }

> Input

    `␊
      1 | function foo(a: Set<string>, b: Set<string>) { return [...a].filter(value => b.has(value)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: Set<string>, b: Set<string>) { return [...a].filter(value => b.has(value)); }␊
        |                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#intersection()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#intersection()\`.:␊
      1 | function foo(a: Set<string>, b: Set<string>) { return a.intersection(b); }␊
    `

## invalid(26): function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return [...a].filter(value => b.has(value)); }

> Input

    `␊
      1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return [...a].filter(value => b.has(value)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return [...a].filter(value => b.has(value)); }␊
        |                                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#intersection()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#intersection()\`.:␊
      1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return a.intersection(b); }␊
    `

## invalid(27): type Items = Set<string>; declare const a: Items; declare const b: Set<string>; [...a].filter(value => b.has(value));

> Input 'file.ts'

    `␊
      1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; [...a].filter(value => b.has(value));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; [...a].filter(value => b.has(value));␊
        |                                                                                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#intersection()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#intersection()\`.:␊
      1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; a.intersection(b);␊
    `

## invalid(28): const a = new Set(); const b = new Set(); const intersection = [...a] .filter(value => b.has(value));

> Input

    `␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | const intersection = [...a]␊
      4 | 	.filter(value => b.has(value));␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
    > 3 | const intersection = [...a]␊
        |                      ^^^^^^␊
    > 4 | 	.filter(value => b.has(value));␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#intersection()\` instead of filtering by \`Set#has()\`.␊
    ␊
    Suggestion 1/1: Use \`Set#intersection()\`.:␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | const intersection = a.intersection(b);␊
    `

## invalid(1): const a = new Set(); const b = new Set(); [...a].every(value => b.has(value));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); [...a].every(value => b.has(value));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); [...a].every(value => b.has(value));␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); a.isSubsetOf(b);␊
    `

## invalid(2): const a = new Set(); const b = new Set(); foo([...a].every(value => b.has(value)));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); foo([...a].every(value => b.has(value)));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); foo([...a].every(value => b.has(value)));␊
        |                                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); foo(a.isSubsetOf(b));␊
    `

## invalid(3): const a = new Set(); const b = new Set(); new Foo([...a].every(value => b.has(value)));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); new Foo([...a].every(value => b.has(value)));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); new Foo([...a].every(value => b.has(value)));␊
        |                                                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); new Foo(a.isSubsetOf(b));␊
    `

## invalid(4): const a = new Set(); const b = new Set(); [...a].every(value => b.has(value)).toString();

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); [...a].every(value => b.has(value)).toString();␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); [...a].every(value => b.has(value)).toString();␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); a.isSubsetOf(b).toString();␊
    `

## invalid(5): const a = new Set(); const b = new Set(); [...a].every(value => b.has(value)) ** 2;

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); [...a].every(value => b.has(value)) ** 2;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); [...a].every(value => b.has(value)) ** 2;␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); a.isSubsetOf(b) ** 2;␊
    `

## invalid(6): const a = new Set(); const b = new Set(); ![...a].every(value => b.has(value));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); ![...a].every(value => b.has(value));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); ![...a].every(value => b.has(value));␊
        |                                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); !a.isSubsetOf(b);␊
    `

## invalid(7): const a = new Set(); const b = new Set(); [...a].every(value => b.has(value)) && other;

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); [...a].every(value => b.has(value)) && other;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); [...a].every(value => b.has(value)) && other;␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); a.isSubsetOf(b) && other;␊
    `

## invalid(8): const a = new Set(); const b = new Set(); ([...(a)].every(value => (b).has(value)));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); ([...(a)].every(value => (b).has(value)));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); ([...(a)].every(value => (b).has(value)));␊
        |                                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); ((a).isSubsetOf((b)));␊
    `

## invalid(9): const a = new Set(); const b = new Set(); function foo() { return[...a].every(value => b.has(value)); };

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); function foo() { return[...a].every(value => b.has(value)); };␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); function foo() { return[...a].every(value => b.has(value)); };␊
        |                                                                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); function foo() { return a.isSubsetOf(b); };␊
    `

## invalid(10): const a = new Set(); const b = new Set(); [...(condition ? a : b)].every(value => b.has(value));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); [...(condition ? a : b)].every(value => b.has(value));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); [...(condition ? a : b)].every(value => b.has(value));␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); (condition ? a : b).isSubsetOf(b);␊
    `

## invalid(11): function foo(a: Set<string>, b: Set<string>) { return [...a].every(value => b.has(value)); }

> Input

    `␊
      1 | function foo(a: Set<string>, b: Set<string>) { return [...a].every(value => b.has(value)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: Set<string>, b: Set<string>) { return [...a].every(value => b.has(value)); }␊
        |                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | function foo(a: Set<string>, b: Set<string>) { return a.isSubsetOf(b); }␊
    `

## invalid(12): function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return [...a].every(value => b.has(value)); }

> Input

    `␊
      1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return [...a].every(value => b.has(value)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return [...a].every(value => b.has(value)); }␊
        |                                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return a.isSubsetOf(b); }␊
    `

## invalid(13): function foo(a: Set<string>, b: Set<string>) { return [...(a as Set<string>)].every(value => (b satisfies Set<string>).has(value)); }

> Input

    `␊
      1 | function foo(a: Set<string>, b: Set<string>) { return [...(a as Set<string>)].every(value => (b satisfies Set<string>).has(value)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: Set<string>, b: Set<string>) { return [...(a as Set<string>)].every(value => (b satisfies Set<string>).has(value)); }␊
        |                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | function foo(a: Set<string>, b: Set<string>) { return (a as Set<string>).isSubsetOf((b satisfies Set<string>)); }␊
    `

## invalid(14): function foo(a: Set<string>, b: Set<string>) { return [...a!].every(value => b!.has(value))!; }

> Input

    `␊
      1 | function foo(a: Set<string>, b: Set<string>) { return [...a!].every(value => b!.has(value))!; }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: Set<string>, b: Set<string>) { return [...a!].every(value => b!.has(value))!; }␊
        |                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | function foo(a: Set<string>, b: Set<string>) { return (a!).isSubsetOf(b!)!; }␊
    `

## invalid(15): type Items = Set<string>; declare const a: Items; declare const b: Set<string>; [...a].every(value => b.has(value));

> Input 'file.ts'

    `␊
      1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; [...a].every(value => b.has(value));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; [...a].every(value => b.has(value));␊
        |                                                                                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; a.isSubsetOf(b);␊
    `

## invalid(1): const a = new Set(); const b = new Set(); [...a].some(value => b.has(value));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); [...a].some(value => b.has(value));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); [...a].some(value => b.has(value));␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); !a.isDisjointFrom(b);␊
    `

## invalid(2): const a = new Set(); const b = new Set(); foo([...a].some(value => b.has(value)));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); foo([...a].some(value => b.has(value)));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); foo([...a].some(value => b.has(value)));␊
        |                                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); foo(!a.isDisjointFrom(b));␊
    `

## invalid(3): const a = new Set(); const b = new Set(); new Foo([...a].some(value => b.has(value)));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); new Foo([...a].some(value => b.has(value)));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); new Foo([...a].some(value => b.has(value)));␊
        |                                                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); new Foo(!a.isDisjointFrom(b));␊
    `

## invalid(4): const a = new Set(); const b = new Set(); [...a].some(value => b.has(value)).toString();

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); [...a].some(value => b.has(value)).toString();␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); [...a].some(value => b.has(value)).toString();␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); (!a.isDisjointFrom(b)).toString();␊
    `

## invalid(5): const a = new Set(); const b = new Set(); [...a].some(value => b.has(value)) ** 2;

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); [...a].some(value => b.has(value)) ** 2;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); [...a].some(value => b.has(value)) ** 2;␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); (!a.isDisjointFrom(b)) ** 2;␊
    `

## invalid(6): const a = new Set(); const b = new Set(); ![...a].some(value => b.has(value));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); ![...a].some(value => b.has(value));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); ![...a].some(value => b.has(value));␊
        |                                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); !!a.isDisjointFrom(b);␊
    `

## invalid(7): const a = new Set(); const b = new Set(); [...a].some(value => b.has(value)) && other;

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); [...a].some(value => b.has(value)) && other;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); [...a].some(value => b.has(value)) && other;␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); !a.isDisjointFrom(b) && other;␊
    `

## invalid(8): const a = new Set(); const b = new Set(); ([...(a)].some(value => (b).has(value)));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); ([...(a)].some(value => (b).has(value)));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); ([...(a)].some(value => (b).has(value)));␊
        |                                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); (!(a).isDisjointFrom((b)));␊
    `

## invalid(9): const a = new Set(); const b = new Set(); function foo() { return[...a].some(value => b.has(value)); };

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); function foo() { return[...a].some(value => b.has(value)); };␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); function foo() { return[...a].some(value => b.has(value)); };␊
        |                                                                  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); function foo() { return !a.isDisjointFrom(b); };␊
    `

## invalid(10): const a = new Set(); const b = new Set(); [...(condition ? a : b)].some(value => b.has(value));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); [...(condition ? a : b)].some(value => b.has(value));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); [...(condition ? a : b)].some(value => b.has(value));␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); !(condition ? a : b).isDisjointFrom(b);␊
    `

## invalid(11): function foo(a: Set<string>, b: Set<string>) { return [...a].some(value => b.has(value)); }

> Input

    `␊
      1 | function foo(a: Set<string>, b: Set<string>) { return [...a].some(value => b.has(value)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: Set<string>, b: Set<string>) { return [...a].some(value => b.has(value)); }␊
        |                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | function foo(a: Set<string>, b: Set<string>) { return !a.isDisjointFrom(b); }␊
    `

## invalid(12): function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return [...a].some(value => b.has(value)); }

> Input

    `␊
      1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return [...a].some(value => b.has(value)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return [...a].some(value => b.has(value)); }␊
        |                                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return !a.isDisjointFrom(b); }␊
    `

## invalid(13): function foo(a: Set<string>, b: Set<string>) { return [...(a as Set<string>)].some(value => (b satisfies Set<string>).has(value)); }

> Input

    `␊
      1 | function foo(a: Set<string>, b: Set<string>) { return [...(a as Set<string>)].some(value => (b satisfies Set<string>).has(value)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: Set<string>, b: Set<string>) { return [...(a as Set<string>)].some(value => (b satisfies Set<string>).has(value)); }␊
        |                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | function foo(a: Set<string>, b: Set<string>) { return !(a as Set<string>).isDisjointFrom((b satisfies Set<string>)); }␊
    `

## invalid(14): function foo(a: Set<string>, b: Set<string>) { return [...a!].some(value => b!.has(value))!; }

> Input

    `␊
      1 | function foo(a: Set<string>, b: Set<string>) { return [...a!].some(value => b!.has(value))!; }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: Set<string>, b: Set<string>) { return [...a!].some(value => b!.has(value))!; }␊
        |                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | function foo(a: Set<string>, b: Set<string>) { return (!(a!).isDisjointFrom(b!))!; }␊
    `

## invalid(15): type Items = Set<string>; declare const a: Items; declare const b: Set<string>; [...a].some(value => b.has(value));

> Input 'file.ts'

    `␊
      1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; [...a].some(value => b.has(value));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; [...a].some(value => b.has(value));␊
        |                                                                                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; !a.isDisjointFrom(b);␊
    `

## invalid(1): const a = new Set(); const b = new Set(); a.intersection(b).size === 0;

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); a.intersection(b).size === 0;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); a.intersection(b).size === 0;␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); a.isDisjointFrom(b);␊
    `

## invalid(2): const a = new Set(); const b = new Set(); 0 === a.intersection(b).size;

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); 0 === a.intersection(b).size;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); 0 === a.intersection(b).size;␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); a.isDisjointFrom(b);␊
    `

## invalid(3): const a = new Set(); const b = new Set(); a.intersection(b).size !== 0;

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); a.intersection(b).size !== 0;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); a.intersection(b).size !== 0;␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); !a.isDisjointFrom(b);␊
    `

## invalid(4): const a = new Set(); const b = new Set(); 0 !== a.intersection(b).size;

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); 0 !== a.intersection(b).size;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); 0 !== a.intersection(b).size;␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); !a.isDisjointFrom(b);␊
    `

## invalid(5): const a = new Set(); const b = new Set(); foo((a.intersection(b).size) === (0));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); foo((a.intersection(b).size) === (0));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); foo((a.intersection(b).size) === (0));␊
        |                                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); foo(a.isDisjointFrom(b));␊
    `

## invalid(6): const a = new Set(); const b = new Set(); (a.intersection(b).size !== 0).toString();

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); (a.intersection(b).size !== 0).toString();␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); (a.intersection(b).size !== 0).toString();␊
        |                                            ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); (!a.isDisjointFrom(b)).toString();␊
    `

## invalid(7): const a = new Set(); const b = new Set(); !(a.intersection(b).size === 0);

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); !(a.intersection(b).size === 0);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); !(a.intersection(b).size === 0);␊
        |                                             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); !(a.isDisjointFrom(b));␊
    `

## invalid(8): function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return a.intersection(b).size === 0; }

> Input

    `␊
      1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return a.intersection(b).size === 0; }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return a.intersection(b).size === 0; }␊
        |                                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return a.isDisjointFrom(b); }␊
    `

## invalid(9): function foo(a: Set<string>, b: Set<string>) { return (a as Set<string>).intersection(b!).size !== 0; }

> Input

    `␊
      1 | function foo(a: Set<string>, b: Set<string>) { return (a as Set<string>).intersection(b!).size !== 0; }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: Set<string>, b: Set<string>) { return (a as Set<string>).intersection(b!).size !== 0; }␊
        |                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | function foo(a: Set<string>, b: Set<string>) { return !(a as Set<string>).isDisjointFrom(b!); }␊
    `

## invalid(10): type Items = Set<string>; declare const a: Items; declare const b: Set<string>; a.intersection(b).size === 0;

> Input 'file.ts'

    `␊
      1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; a.intersection(b).size === 0;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; a.intersection(b).size === 0;␊
        |                                                                                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; a.isDisjointFrom(b);␊
    `

## invalid(11): const a = new Set(); const b = new Set(); foo 0 === (condition ? a : b).intersection(b).size;

> Input

    `␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | foo␊
      4 | 0 === (condition ? a : b).intersection(b).size;␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | foo␊
    > 4 | 0 === (condition ? a : b).intersection(b).size;␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isDisjointFrom()\` to check whether Sets have any elements in common.␊
    ␊
    Output:␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | foo␊
      4 | ;(condition ? a : b).isDisjointFrom(b);␊
    `

## invalid(1): const a = new Set(); const b = new Set(); a.difference(b).size === 0;

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); a.difference(b).size === 0;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); a.difference(b).size === 0;␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); a.isSubsetOf(b);␊
    `

## invalid(2): const a = new Set(); const b = new Set(); 0 === a.difference(b).size;

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); 0 === a.difference(b).size;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); 0 === a.difference(b).size;␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); a.isSubsetOf(b);␊
    `

## invalid(3): const a = new Set(); const b = new Set(); a.difference(b).size !== 0;

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); a.difference(b).size !== 0;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); a.difference(b).size !== 0;␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); !a.isSubsetOf(b);␊
    `

## invalid(4): const a = new Set(); const b = new Set(); 0 !== a.difference(b).size;

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); 0 !== a.difference(b).size;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); 0 !== a.difference(b).size;␊
        |                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); !a.isSubsetOf(b);␊
    `

## invalid(5): const a = new Set(); const b = new Set(); foo((a.difference(b).size) === (0));

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); foo((a.difference(b).size) === (0));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); foo((a.difference(b).size) === (0));␊
        |                                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); foo(a.isSubsetOf(b));␊
    `

## invalid(6): const a = new Set(); const b = new Set(); (a.difference(b).size !== 0).toString();

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); (a.difference(b).size !== 0).toString();␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); (a.difference(b).size !== 0).toString();␊
        |                                            ^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); (!a.isSubsetOf(b)).toString();␊
    `

## invalid(7): const a = new Set(); const b = new Set(); !(a.difference(b).size === 0);

> Input

    `␊
      1 | const a = new Set(); const b = new Set(); !(a.difference(b).size === 0);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const a = new Set(); const b = new Set(); !(a.difference(b).size === 0);␊
        |                                             ^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set(); const b = new Set(); !(a.isSubsetOf(b));␊
    `

## invalid(8): function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return a.difference(b).size === 0; }

> Input

    `␊
      1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return a.difference(b).size === 0; }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return a.difference(b).size === 0; }␊
        |                                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | function foo(a: ReadonlySet<string>, b: ReadonlySet<string>) { return a.isSubsetOf(b); }␊
    `

## invalid(9): function foo(a: Set<string>, b: Set<string>) { return (a as Set<string>).difference(b!).size !== 0; }

> Input

    `␊
      1 | function foo(a: Set<string>, b: Set<string>) { return (a as Set<string>).difference(b!).size !== 0; }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(a: Set<string>, b: Set<string>) { return (a as Set<string>).difference(b!).size !== 0; }␊
        |                                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | function foo(a: Set<string>, b: Set<string>) { return !(a as Set<string>).isSubsetOf(b!); }␊
    `

## invalid(10): type Items = Set<string>; declare const a: Items; declare const b: Set<string>; a.difference(b).size === 0;

> Input 'file.ts'

    `␊
      1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; a.difference(b).size === 0;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; a.difference(b).size === 0;␊
        |                                                                                 ^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | type Items = Set<string>; declare const a: Items; declare const b: Set<string>; a.isSubsetOf(b);␊
    `

## invalid(11): const a = new Set(); const b = new Set(); foo 0 === (condition ? a : b).difference(b).size;

> Input

    `␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | foo␊
      4 | 0 === (condition ? a : b).difference(b).size;␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | foo␊
    > 4 | 0 === (condition ? a : b).difference(b).size;␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Set#isSubsetOf()\` to check whether a Set is a subset of another Set.␊
    ␊
    Output:␊
      1 | const a = new Set();␊
      2 | const b = new Set();␊
      3 | foo␊
      4 | ;(condition ? a : b).isSubsetOf(b);␊
    `
