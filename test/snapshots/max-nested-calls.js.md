# Snapshot report for `test/max-nested-calls.js`

The actual snapshot is saved in `max-nested-calls.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): foo(bar(baz(qux())));

> Input

    `␊
      1 | foo(bar(baz(qux())));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | foo(bar(baz(qux())));␊
        |             ^^^^^ Call is nested too deeply. Maximum allowed is 3.␊
    `

## invalid(2): foo(bar(baz()));

> Input

    `␊
    Options:␊
    - max: 2␊
    ␊
    Code:␊
      1 | foo(bar(baz()));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | foo(bar(baz()));␊
        |         ^^^^^ Call is nested too deeply. Maximum allowed is 2.␊
    `

## invalid(3): new Foo(new Bar(new Baz(new Qux())));

> Input

    `␊
      1 | new Foo(new Bar(new Baz(new Qux())));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | new Foo(new Bar(new Baz(new Qux())));␊
        |                         ^^^^^^^^^ Call is nested too deeply. Maximum allowed is 3.␊
    `

## invalid(4): await foo(await bar(await baz(await qux())));

> Input

    `␊
      1 | await foo(await bar(await baz(await qux())));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | await foo(await bar(await baz(await qux())));␊
        |                                     ^^^^^ Call is nested too deeply. Maximum allowed is 3.␊
    `

## invalid(5): foo?.(bar?.(baz?.(qux?.())));

> Input

    `␊
      1 | foo?.(bar?.(baz?.(qux?.())));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | foo?.(bar?.(baz?.(qux?.())));␊
        |                   ^^^^^^^ Call is nested too deeply. Maximum allowed is 3.␊
    `

## invalid(6): foo(...bar(baz(qux())));

> Input

    `␊
      1 | foo(...bar(baz(qux())));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | foo(...bar(baz(qux())));␊
        |                ^^^^^ Call is nested too deeply. Maximum allowed is 3.␊
    `

## invalid(7): foo(condition ? bar(baz(qux())) : zed());

> Input

    `␊
      1 | foo(condition ? bar(baz(qux())) : zed());␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | foo(condition ? bar(baz(qux())) : zed());␊
        |                         ^^^^^ Call is nested too deeply. Maximum allowed is 3.␊
    `

## invalid(8): foo(class {field = bar(baz(qux(zed())));});

> Input

    `␊
      1 | foo(class {field = bar(baz(qux(zed())));});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | foo(class {field = bar(baz(qux(zed())));});␊
        |                                ^^^^^ Call is nested too deeply. Maximum allowed is 3.␊
    `

## invalid(9): mergeReports(await pMap( await mergeWithFileConfigs(uniq(paths), inputOptions, configFiles), async ({files, options, prettierOptions}) => runEslint(files, buildConfig(options, prettierOptions), {isQuiet: options.quiet}), ));

> Input

    `␊
      1 | mergeReports(await pMap(␊
      2 | 	await mergeWithFileConfigs(uniq(paths), inputOptions, configFiles),␊
      3 | 	async ({files, options, prettierOptions}) => runEslint(files, buildConfig(options, prettierOptions), {isQuiet: options.quiet}),␊
      4 | ));␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | mergeReports(await pMap(␊
    > 2 | 	await mergeWithFileConfigs(uniq(paths), inputOptions, configFiles),␊
        | 	                           ^^^^^^^^^^^ Call is nested too deeply. Maximum allowed is 3.␊
      3 | 	async ({files, options, prettierOptions}) => runEslint(files, buildConfig(options, prettierOptions), {isQuiet: options.quiet}),␊
      4 | ));␊
    `
