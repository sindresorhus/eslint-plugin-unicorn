# Snapshot report for `test/prefer-abort-signal-any.js`

The actual snapshot is saved in `prefer-abort-signal-any.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): const abortController = new AbortController(); firstSignal.addEventListener('abort', () => abortController.abort()); secondSignal.addEventListener('abort', () => abortController.abort()); fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | firstSignal.addEventListener('abort', () => abortController.abort());␊
      3 | secondSignal.addEventListener('abort', () => abortController.abort());␊
      4 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      2 | firstSignal.addEventListener('abort', () => abortController.abort());␊
      3 | secondSignal.addEventListener('abort', () => abortController.abort());␊
      4 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | const abortSignal = AbortSignal.any([firstSignal, secondSignal]);␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(2): const abortController = new AbortController(); AbortSignal.timeout(1000).addEventListener('abort', () => abortController.abort()); secondSignal.addEventListener('abort', () => abortController.abort()); fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | AbortSignal.timeout(1000).addEventListener('abort', () => abortController.abort());␊
      3 | secondSignal.addEventListener('abort', () => abortController.abort());␊
      4 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      2 | AbortSignal.timeout(1000).addEventListener('abort', () => abortController.abort());␊
      3 | secondSignal.addEventListener('abort', () => abortController.abort());␊
      4 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | const abortSignal = AbortSignal.any([AbortSignal.timeout(1000), secondSignal]);␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(3): const abortController = new AbortController(); firstSignal.addEventListener('abort', () => { abortController.abort(); }); secondSignal.addEventListener('abort', function () { abortController.abort(); }); fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | firstSignal.addEventListener('abort', () => {␊
      3 | 	abortController.abort();␊
      4 | });␊
      5 | secondSignal.addEventListener('abort', function () {␊
      6 | 	abortController.abort();␊
      7 | });␊
      8 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      2 | firstSignal.addEventListener('abort', () => {␊
      3 | 	abortController.abort();␊
      4 | });␊
      5 | secondSignal.addEventListener('abort', function () {␊
      6 | 	abortController.abort();␊
      7 | });␊
      8 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | const abortSignal = AbortSignal.any([firstSignal, secondSignal]);␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(4): const abortController = new AbortController(); firstSignal.addEventListener('abort', () => abortController.abort(firstSignal.reason)); secondSignal.addEventListener('abort', () => abortController.abort(secondSignal.reason), {once: true}); fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | firstSignal.addEventListener('abort', () => abortController.abort(firstSignal.reason));␊
      3 | secondSignal.addEventListener('abort', () => abortController.abort(secondSignal.reason), {once: true});␊
      4 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      2 | firstSignal.addEventListener('abort', () => abortController.abort(firstSignal.reason));␊
      3 | secondSignal.addEventListener('abort', () => abortController.abort(secondSignal.reason), {once: true});␊
      4 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | const abortSignal = AbortSignal.any([firstSignal, secondSignal]);␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(5): const controller = new AbortController(); firstSignal.addEventListener('abort', () => controller.abort()); secondSignal.addEventListener('abort', () => controller.abort(), true); fetch(url, {signal: controller.signal});

> Input

    `␊
      1 | const controller = new AbortController();␊
      2 | firstSignal.addEventListener('abort', () => controller.abort());␊
      3 | secondSignal.addEventListener('abort', () => controller.abort(), true);␊
      4 | fetch(url, {signal: controller.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const controller = new AbortController();␊
        |       ^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      2 | firstSignal.addEventListener('abort', () => controller.abort());␊
      3 | secondSignal.addEventListener('abort', () => controller.abort(), true);␊
      4 | fetch(url, {signal: controller.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | const signal = AbortSignal.any([firstSignal, secondSignal]);␊
      2 | fetch(url, {signal: signal});␊
    `

## invalid(6): const abortController = new AbortController(); for (const signal of [firstSignal, secondSignal]) { signal.addEventListener('abort', () => abortController.abort()); } fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | for (const signal of [firstSignal, secondSignal]) {␊
      3 | 	signal.addEventListener('abort', () => abortController.abort());␊
      4 | }␊
      5 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      2 | for (const signal of [firstSignal, secondSignal]) {␊
      3 | 	signal.addEventListener('abort', () => abortController.abort());␊
      4 | }␊
      5 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | const abortSignal = AbortSignal.any([firstSignal, secondSignal]);␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(7): const abortController = new AbortController(); for (const signal of new Array(firstSignal, secondSignal)) { signal.addEventListener('abort', () => abortController.abort()); } fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | for (const signal of new Array(firstSignal, secondSignal)) {␊
      3 | 	signal.addEventListener('abort', () => abortController.abort());␊
      4 | }␊
      5 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      2 | for (const signal of new Array(firstSignal, secondSignal)) {␊
      3 | 	signal.addEventListener('abort', () => abortController.abort());␊
      4 | }␊
      5 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | const abortSignal = AbortSignal.any(new Array(firstSignal, secondSignal));␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(8): const abortController = new AbortController(); for (const signal of Array.of(firstSignal, secondSignal)) { signal.addEventListener('abort', () => abortController.abort()); } fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | for (const signal of Array.of(firstSignal, secondSignal)) {␊
      3 | 	signal.addEventListener('abort', () => abortController.abort());␊
      4 | }␊
      5 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      2 | for (const signal of Array.of(firstSignal, secondSignal)) {␊
      3 | 	signal.addEventListener('abort', () => abortController.abort());␊
      4 | }␊
      5 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | const abortSignal = AbortSignal.any(Array.of(firstSignal, secondSignal));␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(9): const abortController = new AbortController(); for (const signal of Array(firstSignal, secondSignal)) { signal.addEventListener('abort', () => abortController.abort()); } fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | for (const signal of Array(firstSignal, secondSignal)) {␊
      3 | 	signal.addEventListener('abort', () => abortController.abort());␊
      4 | }␊
      5 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      2 | for (const signal of Array(firstSignal, secondSignal)) {␊
      3 | 	signal.addEventListener('abort', () => abortController.abort());␊
      4 | }␊
      5 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | const abortSignal = AbortSignal.any(Array(firstSignal, secondSignal));␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(10): const abortController = new AbortController(); for (const signal of Array.from([firstSignal, secondSignal])) { signal.addEventListener('abort', () => abortController.abort()); } fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | for (const signal of Array.from([firstSignal, secondSignal])) {␊
      3 | 	signal.addEventListener('abort', () => abortController.abort());␊
      4 | }␊
      5 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      2 | for (const signal of Array.from([firstSignal, secondSignal])) {␊
      3 | 	signal.addEventListener('abort', () => abortController.abort());␊
      4 | }␊
      5 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | const abortSignal = AbortSignal.any(Array.from([firstSignal, secondSignal]));␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(11): const abortController: AbortController = new AbortController(); for (const signal of signals as AbortSignal[]) { signal.addEventListener('abort', () => abortController.abort(signal.reason)); } fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController: AbortController = new AbortController();␊
      2 | for (const signal of signals as AbortSignal[]) {␊
      3 | 	signal.addEventListener('abort', () => abortController.abort(signal.reason));␊
      4 | }␊
      5 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController: AbortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      2 | for (const signal of signals as AbortSignal[]) {␊
      3 | 	signal.addEventListener('abort', () => abortController.abort(signal.reason));␊
      4 | }␊
      5 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | const abortSignal: AbortSignal = AbortSignal.any(signals as AbortSignal[]);␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(12): function compose(signals: readonly AbortSignal[]) { const abortController = new AbortController(); for (const signal of signals) { signal.addEventListener('abort', () => abortController.abort()); } return abortController.signal; }

> Input

    `␊
      1 | function compose(signals: readonly AbortSignal[]) {␊
      2 | 	const abortController = new AbortController();␊
      3 | 	for (const signal of signals) {␊
      4 | 		signal.addEventListener('abort', () => abortController.abort());␊
      5 | 	}␊
      6 | 	return abortController.signal;␊
      7 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | function compose(signals: readonly AbortSignal[]) {␊
    > 2 | 	const abortController = new AbortController();␊
        | 	      ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      3 | 	for (const signal of signals) {␊
      4 | 		signal.addEventListener('abort', () => abortController.abort());␊
      5 | 	}␊
      6 | 	return abortController.signal;␊
      7 | }␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | function compose(signals: readonly AbortSignal[]) {␊
      2 | 	const abortSignal = AbortSignal.any([...signals]);␊
      3 | 	return abortSignal;␊
      4 | }␊
    `

## invalid(13): const signals: [AbortSignal, AbortSignal] = [firstSignal, secondSignal]; const abortController = new AbortController(); for (const signal of signals) { signal.addEventListener('abort', () => abortController.abort()); } fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const signals: [AbortSignal, AbortSignal] = [firstSignal, secondSignal];␊
      2 | const abortController = new AbortController();␊
      3 | for (const signal of signals) {␊
      4 | 	signal.addEventListener('abort', () => abortController.abort());␊
      5 | }␊
      6 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const signals: [AbortSignal, AbortSignal] = [firstSignal, secondSignal];␊
    > 2 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      3 | for (const signal of signals) {␊
      4 | 	signal.addEventListener('abort', () => abortController.abort());␊
      5 | }␊
      6 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | const signals: [AbortSignal, AbortSignal] = [firstSignal, secondSignal];␊
      2 | const abortSignal = AbortSignal.any(signals);␊
      3 | fetch(url, {signal: abortSignal});␊
    `

## invalid(14): const abortController = new AbortController(); for (const signal of signals as readonly AbortSignal[]) { signal.addEventListener('abort', () => abortController.abort(signal.reason)); } fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | for (const signal of signals as readonly AbortSignal[]) {␊
      3 | 	signal.addEventListener('abort', () => abortController.abort(signal.reason));␊
      4 | }␊
      5 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      2 | for (const signal of signals as readonly AbortSignal[]) {␊
      3 | 	signal.addEventListener('abort', () => abortController.abort(signal.reason));␊
      4 | }␊
      5 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | const abortSignal = AbortSignal.any([...(signals as readonly AbortSignal[])]);␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(15): function compose(signals: AbortSignal[]) { const abortController = new AbortController(); for (const signal of signals) { signal.addEventListener('abort', () => abortController.abort(signal.reason)); } return abortController.signal; }

> Input 'file.ts'

    `␊
      1 | function compose(signals: AbortSignal[]) {␊
      2 | 	const abortController = new AbortController();␊
      3 | 	for (const signal of signals) {␊
      4 | 		signal.addEventListener('abort', () => abortController.abort(signal.reason));␊
      5 | 	}␊
      6 | 	return abortController.signal;␊
      7 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | function compose(signals: AbortSignal[]) {␊
    > 2 | 	const abortController = new AbortController();␊
        | 	      ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      3 | 	for (const signal of signals) {␊
      4 | 		signal.addEventListener('abort', () => abortController.abort(signal.reason));␊
      5 | 	}␊
      6 | 	return abortController.signal;␊
      7 | }␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | function compose(signals: AbortSignal[]) {␊
      2 | 	const abortSignal = AbortSignal.any(signals);␊
      3 | 	return abortSignal;␊
      4 | }␊
    `

## invalid(16): declare const signals: AbortSignal[]; const abortController = new AbortController(); for (const signal of signals) { signal.addEventListener('abort', () => abortController.abort()); } fetch(url, {signal: abortController.signal});

> Input 'file.ts'

    `␊
      1 | declare const signals: AbortSignal[];␊
      2 | const abortController = new AbortController();␊
      3 | for (const signal of signals) {␊
      4 | 	signal.addEventListener('abort', () => abortController.abort());␊
      5 | }␊
      6 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | declare const signals: AbortSignal[];␊
    > 2 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      3 | for (const signal of signals) {␊
      4 | 	signal.addEventListener('abort', () => abortController.abort());␊
      5 | }␊
      6 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | declare const signals: AbortSignal[];␊
      2 | const abortSignal = AbortSignal.any(signals);␊
      3 | fetch(url, {signal: abortSignal});␊
    `

## invalid(17): type Signals = readonly AbortSignal[]; function compose(signals: Signals) { const abortController = new AbortController(); for (const signal of signals) { signal.addEventListener('abort', () => abortController.abort()); } return abortController.signal; }

> Input 'file.ts'

    `␊
      1 | type Signals = readonly AbortSignal[];␊
      2 | function compose(signals: Signals) {␊
      3 | 	const abortController = new AbortController();␊
      4 | 	for (const signal of signals) {␊
      5 | 		signal.addEventListener('abort', () => abortController.abort());␊
      6 | 	}␊
      7 |␊
      8 | 	return abortController.signal;␊
      9 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | type Signals = readonly AbortSignal[];␊
      2 | function compose(signals: Signals) {␊
    > 3 | 	const abortController = new AbortController();␊
        | 	      ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      4 | 	for (const signal of signals) {␊
      5 | 		signal.addEventListener('abort', () => abortController.abort());␊
      6 | 	}␊
      7 |␊
      8 | 	return abortController.signal;␊
      9 | }␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | type Signals = readonly AbortSignal[];␊
      2 | function compose(signals: Signals) {␊
      3 | 	const abortSignal = AbortSignal.any([...signals]);␊
      4 |␊
      5 | 	return abortSignal;␊
      6 | }␊
    `

## invalid(18): interface Signals extends ReadonlyArray<AbortSignal> {} function compose(signals: Signals) { const abortController = new AbortController(); for (const signal of signals) { signal.addEventListener('abort', () => abortController.abort()); } return abortController.signal; }

> Input 'file.ts'

    `␊
      1 | interface Signals extends ReadonlyArray<AbortSignal> {}␊
      2 | function compose(signals: Signals) {␊
      3 | 	const abortController = new AbortController();␊
      4 | 	for (const signal of signals) {␊
      5 | 		signal.addEventListener('abort', () => abortController.abort());␊
      6 | 	}␊
      7 |␊
      8 | 	return abortController.signal;␊
      9 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | interface Signals extends ReadonlyArray<AbortSignal> {}␊
      2 | function compose(signals: Signals) {␊
    > 3 | 	const abortController = new AbortController();␊
        | 	      ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      4 | 	for (const signal of signals) {␊
      5 | 		signal.addEventListener('abort', () => abortController.abort());␊
      6 | 	}␊
      7 |␊
      8 | 	return abortController.signal;␊
      9 | }␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | interface Signals extends ReadonlyArray<AbortSignal> {}␊
      2 | function compose(signals: Signals) {␊
      3 | 	const abortSignal = AbortSignal.any([...signals]);␊
      4 |␊
      5 | 	return abortSignal;␊
      6 | }␊
    `

## invalid(19): const signals = getSignals() as readonly AbortSignal[]; const abortController = new AbortController(); for (const signal of signals) { signal.addEventListener('abort', () => abortController.abort()); } fetch(url, {signal: abortController.signal});

> Input 'file.ts'

    `␊
      1 | const signals = getSignals() as readonly AbortSignal[];␊
      2 | const abortController = new AbortController();␊
      3 | for (const signal of signals) {␊
      4 | 	signal.addEventListener('abort', () => abortController.abort());␊
      5 | }␊
      6 |␊
      7 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const signals = getSignals() as readonly AbortSignal[];␊
    > 2 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      3 | for (const signal of signals) {␊
      4 | 	signal.addEventListener('abort', () => abortController.abort());␊
      5 | }␊
      6 |␊
      7 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | const signals = getSignals() as readonly AbortSignal[];␊
      2 | const abortSignal = AbortSignal.any([...signals]);␊
      3 |␊
      4 | fetch(url, {signal: abortSignal});␊
    `

## invalid(20): const abortSignal = existingSignal; const abortController = new AbortController(); firstSignal.addEventListener('abort', () => abortController.abort()); secondSignal.addEventListener('abort', () => abortController.abort()); fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortSignal = existingSignal;␊
      2 | const abortController = new AbortController();␊
      3 | firstSignal.addEventListener('abort', () => abortController.abort());␊
      4 | secondSignal.addEventListener('abort', () => abortController.abort());␊
      5 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const abortSignal = existingSignal;␊
    > 2 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      3 | firstSignal.addEventListener('abort', () => abortController.abort());␊
      4 | secondSignal.addEventListener('abort', () => abortController.abort());␊
      5 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | const abortSignal = existingSignal;␊
      2 | const abortController = AbortSignal.any([firstSignal, secondSignal]);␊
      3 | fetch(url, {signal: abortController});␊
    `

## invalid(21): const abortController = new AbortController(); firstSignal.addEventListener('abort', () => abortController.abort()); secondSignal.addEventListener('abort', () => abortController.abort()); fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | firstSignal.addEventListener('abort', () => abortController.abort());␊
      3 | secondSignal.addEventListener('abort', () => abortController.abort());␊
      4 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.any()\` over manually forwarding abort events between signals.␊
      2 | firstSignal.addEventListener('abort', () => abortController.abort());␊
      3 | secondSignal.addEventListener('abort', () => abortController.abort());␊
      4 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.any()\`.:␊
      1 | const abortSignal = AbortSignal.any([firstSignal, secondSignal]);␊
      2 | fetch(url, {signal: abortSignal});␊
    `
