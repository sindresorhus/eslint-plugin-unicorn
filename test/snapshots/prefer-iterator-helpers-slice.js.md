# Snapshot report for `test/prefer-iterator-helpers-slice.js`

The actual snapshot is saved in `prefer-iterator-helpers-slice.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): iterator.toArray().slice(0, 10)

> Input

    `␊
      1 | iterator.toArray().slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().slice(0, 10)␊
        |                    ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | iterator.take(10).toArray()␊
    `

## invalid(2): iterator.toArray().slice(20, 30)

> Input

    `␊
      1 | iterator.toArray().slice(20, 30)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().slice(20, 30)␊
        |                    ^^^^^ Prefer \`drop(20).take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`drop(20).take(10).toArray()\`.:␊
      1 | iterator.drop(20).take(10).toArray()␊
    `

## invalid(3): iterator.toArray().slice(20)

> Input

    `␊
      1 | iterator.toArray().slice(20)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().slice(20)␊
        |                    ^^^^^ Prefer \`drop(20)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`drop(20).toArray()\`.:␊
      1 | iterator.drop(20).toArray()␊
    `

## invalid(4): iterator.toArray().slice(0, 0)

> Input

    `␊
      1 | iterator.toArray().slice(0, 0)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().slice(0, 0)␊
        |                    ^^^^^ Prefer \`take(0)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(0).toArray()\`.:␊
      1 | iterator.take(0).toArray()␊
    `

## invalid(5): iterator.toArray().slice(20, 20)

> Input

    `␊
      1 | iterator.toArray().slice(20, 20)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().slice(20, 20)␊
        |                    ^^^^^ Prefer \`take(0)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(0).toArray()\`.:␊
      1 | iterator.take(0).toArray()␊
    `

## invalid(6): iterator.toArray().slice(20, 10)

> Input

    `␊
      1 | iterator.toArray().slice(20, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().slice(20, 10)␊
        |                    ^^^^^ Prefer \`take(0)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(0).toArray()\`.:␊
      1 | iterator.take(0).toArray()␊
    `

## invalid(7): iterator.toArray().slice(-0, 10)

> Input

    `␊
      1 | iterator.toArray().slice(-0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().slice(-0, 10)␊
        |                    ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | iterator.take(10).toArray()␊
    `

## invalid(8): iterator.toArray().slice(0, 0x10)

> Input

    `␊
      1 | iterator.toArray().slice(0, 0x10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().slice(0, 0x10)␊
        |                    ^^^^^ Prefer \`take(16)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(16).toArray()\`.:␊
      1 | iterator.take(16).toArray()␊
    `

## invalid(9): iterator.toArray().slice(1_000, 2_000)

> Input

    `␊
      1 | iterator.toArray().slice(1_000, 2_000)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().slice(1_000, 2_000)␊
        |                    ^^^^^ Prefer \`drop(1000).take(1000)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`drop(1000).take(1000).toArray()\`.:␊
      1 | iterator.drop(1000).take(1000).toArray()␊
    `

## invalid(10): iterator.toArray().slice(0, Number.MAX_SAFE_INTEGER)

> Input

    `␊
      1 | iterator.toArray().slice(0, Number.MAX_SAFE_INTEGER)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().slice(0, Number.MAX_SAFE_INTEGER)␊
        |                    ^^^^^ Prefer \`take(9007199254740991)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(9007199254740991).toArray()\`.:␊
      1 | iterator.take(9007199254740991).toArray()␊
    `

## invalid(11): const start = 20; const end = start + 10; iterator.toArray().slice(start, end)

> Input

    `␊
      1 | const start = 20; const end = start + 10; iterator.toArray().slice(start, end)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const start = 20; const end = start + 10; iterator.toArray().slice(start, end)␊
        |                                                              ^^^^^ Prefer \`drop(20).take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`drop(20).take(10).toArray()\`.:␊
      1 | const start = 20; const end = start + 10; iterator.drop(20).take(10).toArray()␊
    `

## invalid(12): iterator.toArray().slice(2 * 10, 3 * 10)

> Input

    `␊
      1 | iterator.toArray().slice(2 * 10, 3 * 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().slice(2 * 10, 3 * 10)␊
        |                    ^^^^^ Prefer \`drop(20).take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`drop(20).take(10).toArray()\`.:␊
      1 | iterator.drop(20).take(10).toArray()␊
    `

## invalid(13): [...map.values()].slice(0, 10)

> Input

    `␊
      1 | [...map.values()].slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | [...map.values()].slice(0, 10)␊
        |                   ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | map.values().take(10).toArray()␊
    `

## invalid(14): [...map.keys()].slice(20)

> Input

    `␊
      1 | [...map.keys()].slice(20)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | [...map.keys()].slice(20)␊
        |                 ^^^^^ Prefer \`drop(20)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`drop(20).toArray()\`.:␊
      1 | map.keys().drop(20).toArray()␊
    `

## invalid(15): [...map.entries()].slice(20, 30)

> Input

    `␊
      1 | [...map.entries()].slice(20, 30)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | [...map.entries()].slice(20, 30)␊
        |                    ^^^^^ Prefer \`drop(20).take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`drop(20).take(10).toArray()\`.:␊
      1 | map.entries().drop(20).take(10).toArray()␊
    `

## invalid(16): [...string.matchAll(pattern)].slice(0, 10)

> Input

    `␊
      1 | [...string.matchAll(pattern)].slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | [...string.matchAll(pattern)].slice(0, 10)␊
        |                               ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | string.matchAll(pattern).take(10).toArray()␊
    `

## invalid(17): Array.from(set.values()).slice(0, 10)

> Input

    `␊
      1 | Array.from(set.values()).slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Array.from(set.values()).slice(0, 10)␊
        |                          ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | set.values().take(10).toArray()␊
    `

## invalid(18): Array.from(map.keys()).slice(20)

> Input

    `␊
      1 | Array.from(map.keys()).slice(20)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Array.from(map.keys()).slice(20)␊
        |                        ^^^^^ Prefer \`drop(20)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`drop(20).toArray()\`.:␊
      1 | map.keys().drop(20).toArray()␊
    `

## invalid(19): Array.from(map.entries()).slice(20, 30)

> Input

    `␊
      1 | Array.from(map.entries()).slice(20, 30)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Array.from(map.entries()).slice(20, 30)␊
        |                           ^^^^^ Prefer \`drop(20).take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`drop(20).take(10).toArray()\`.:␊
      1 | map.entries().drop(20).take(10).toArray()␊
    `

## invalid(20): [...Iterator.from(iterable)].slice(0, 10)

> Input

    `␊
      1 | [...Iterator.from(iterable)].slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | [...Iterator.from(iterable)].slice(0, 10)␊
        |                              ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | Iterator.from(iterable).take(10).toArray()␊
    `

## invalid(21): Array.from(globalThis.Iterator.from(iterable)).slice(0, 10)

> Input

    `␊
      1 | Array.from(globalThis.Iterator.from(iterable)).slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Array.from(globalThis.Iterator.from(iterable)).slice(0, 10)␊
        |                                                ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | globalThis.Iterator.from(iterable).take(10).toArray()␊
    `

## invalid(22): [...Iterator.concat(first, second)].slice(0, 10)

> Input

    `␊
      1 | [...Iterator.concat(first, second)].slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | [...Iterator.concat(first, second)].slice(0, 10)␊
        |                                     ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | Iterator.concat(first, second).take(10).toArray()␊
    `

## invalid(23): [...map.values().filter(fn)].slice(0, 10)

> Input

    `␊
      1 | [...map.values().filter(fn)].slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | [...map.values().filter(fn)].slice(0, 10)␊
        |                              ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | map.values().filter(fn).take(10).toArray()␊
    `

## invalid(24): Array.from(map.values().map(fn)).slice(20, 30)

> Input

    `␊
      1 | Array.from(map.values().map(fn)).slice(20, 30)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Array.from(map.values().map(fn)).slice(20, 30)␊
        |                                  ^^^^^ Prefer \`drop(20).take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`drop(20).take(10).toArray()\`.:␊
      1 | map.values().map(fn).drop(20).take(10).toArray()␊
    `

## invalid(25): [...object?.map.values()].slice(0, 10)

> Input

    `␊
      1 | [...object?.map.values()].slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | [...object?.map.values()].slice(0, 10)␊
        |                           ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | (object?.map.values()).take(10).toArray()␊
    `

## invalid(26): Array.from(object?.map.values()).slice(0, 10)

> Input

    `␊
      1 | Array.from(object?.map.values()).slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Array.from(object?.map.values()).slice(0, 10)␊
        |                                  ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | (object?.map.values()).take(10).toArray()␊
    `

## invalid(27): Array.from({values() { return [1].values(); }}.values()).slice(0, 1)

> Input

    `␊
      1 | Array.from({values() { return [1].values(); }}.values()).slice(0, 1)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Array.from({values() { return [1].values(); }}.values()).slice(0, 1)␊
        |                                                          ^^^^^ Prefer \`take(1)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(1).toArray()\`.:␊
      1 | ({values() { return [1].values(); }}.values()).take(1).toArray()␊
    `

## invalid(28): Array.from(function () {}.values()).slice(0, 1)

> Input

    `␊
      1 | Array.from(function () {}.values()).slice(0, 1)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Array.from(function () {}.values()).slice(0, 1)␊
        |                                     ^^^^^ Prefer \`take(1)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(1).toArray()\`.:␊
      1 | (function () {}.values()).take(1).toArray()␊
    `

## invalid(29): Array.from(class {}.values()).slice(0, 1)

> Input

    `␊
      1 | Array.from(class {}.values()).slice(0, 1)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Array.from(class {}.values()).slice(0, 1)␊
        |                               ^^^^^ Prefer \`take(1)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(1).toArray()\`.:␊
      1 | (class {}.values()).take(1).toArray()␊
    `

## invalid(30): Array.from(async function () {}.values()).slice(0, 1)

> Input

    `␊
      1 | Array.from(async function () {}.values()).slice(0, 1)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Array.from(async function () {}.values()).slice(0, 1)␊
        |                                           ^^^^^ Prefer \`take(1)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(1).toArray()\`.:␊
      1 | (async function () {}.values()).take(1).toArray()␊
    `

## invalid(31): getIterator().toArray().slice(0, 10)

> Input

    `␊
      1 | getIterator().toArray().slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | getIterator().toArray().slice(0, 10)␊
        |                         ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | getIterator().take(10).toArray()␊
    `

## invalid(32): (condition ? first : second).toArray().slice(0, 10)

> Input

    `␊
      1 | (condition ? first : second).toArray().slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (condition ? first : second).toArray().slice(0, 10)␊
        |                                        ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | (condition ? first : second).take(10).toArray()␊
    `

## invalid(33): ((iterator).toArray()).slice((0), (10))

> Input

    `␊
      1 | ((iterator).toArray()).slice((0), (10))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | ((iterator).toArray()).slice((0), (10))␊
        |                        ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | (iterator).take(10).toArray()␊
    `

## invalid(34): [...(map.values()),].slice(0, 10,)

> Input

    `␊
      1 | [...(map.values()),].slice(0, 10,)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | [...(map.values()),].slice(0, 10,)␊
        |                      ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | (map.values()).take(10).toArray()␊
    `

## invalid(35): iterator.toArray().slice(0, 10).map(fn)

> Input

    `␊
      1 | iterator.toArray().slice(0, 10).map(fn)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().slice(0, 10).map(fn)␊
        |                    ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | iterator.take(10).toArray().map(fn)␊
    `

## invalid(36): const element = <div>{iterator.toArray().slice(0, 10)}</div>

> Input

    `␊
      1 | const element = <div>{iterator.toArray().slice(0, 10)}</div>␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const element = <div>{iterator.toArray().slice(0, 10)}</div>␊
        |                                          ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | const element = <div>{iterator.take(10).toArray()}</div>␊
    `

## invalid(37): function foo() { return[...map.values()].slice(0, 10); }

> Input

    `␊
      1 | function foo() { return[...map.values()].slice(0, 10); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo() { return[...map.values()].slice(0, 10); }␊
        |                                          ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | function foo() { return map.values().take(10).toArray(); }␊
    `

## invalid(38): function foo() { throw[...map.values()].slice(0, 10); }

> Input

    `␊
      1 | function foo() { throw[...map.values()].slice(0, 10); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo() { throw[...map.values()].slice(0, 10); }␊
        |                                         ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | function foo() { throw map.values().take(10).toArray(); }␊
    `

## invalid(39): previous() Array.from([1, 2].values()).slice(0, 1)

> Input

    `␊
      1 | previous()␊
      2 | Array.from([1, 2].values()).slice(0, 1)␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | previous()␊
    > 2 | Array.from([1, 2].values()).slice(0, 1)␊
        |                             ^^^^^ Prefer \`take(1)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(1).toArray()\`.:␊
      1 | previous()␊
      2 | ;[1, 2].values().take(1).toArray()␊
    `

## invalid(40): Array.from( map.values(), ).slice(20, 30)

> Input

    `␊
      1 | Array.from(␊
      2 | 	map.values(),␊
      3 | ).slice(20, 30)␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | Array.from(␊
      2 | 	map.values(),␊
    > 3 | ).slice(20, 30)␊
        |   ^^^^^ Prefer \`drop(20).take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`drop(20).take(10).toArray()\`.:␊
      1 | map.values().drop(20).take(10).toArray()␊
    `

## invalid(41): [...map.values(/* comment */)].slice(0, 10)

> Input

    `␊
      1 | [...map.values(/* comment */)].slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | [...map.values(/* comment */)].slice(0, 10)␊
        |                                ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | map.values(/* comment */).take(10).toArray()␊
    `

## invalid(42): Array.from((/* comment */ map.values())).slice(0, 10)

> Input

    `␊
      1 | Array.from((/* comment */ map.values())).slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Array.from((/* comment */ map.values())).slice(0, 10)␊
        |                                          ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | (/* comment */ map.values()).take(10).toArray()␊
    `

## invalid(43): [/* comment */ ...map.values()].slice(0, 10)

> Input

    `␊
      1 | [/* comment */ ...map.values()].slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | [/* comment */ ...map.values()].slice(0, 10)␊
        |                                 ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    `

## invalid(44): [...map.values() /* comment */].slice(0, 10)

> Input

    `␊
      1 | [...map.values() /* comment */].slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | [...map.values() /* comment */].slice(0, 10)␊
        |                                 ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    `

## invalid(45): Array.from(/* comment */ map.values()).slice(0, 10)

> Input

    `␊
      1 | Array.from(/* comment */ map.values()).slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Array.from(/* comment */ map.values()).slice(0, 10)␊
        |                                        ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    `

## invalid(46): iterator.toArray(/* comment */).slice(0, 10)

> Input

    `␊
      1 | iterator.toArray(/* comment */).slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray(/* comment */).slice(0, 10)␊
        |                                 ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    `

## invalid(47): iterator.toArray() /* comment */ .slice(0, 10)

> Input

    `␊
      1 | iterator.toArray() /* comment */ .slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray() /* comment */ .slice(0, 10)␊
        |                                   ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    `

## invalid(48): iterator.toArray().slice(/* comment */ 0, 10)

> Input

    `␊
      1 | iterator.toArray().slice(/* comment */ 0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().slice(/* comment */ 0, 10)␊
        |                    ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    `

## invalid(49): iterator.toArray().slice(0, /* comment */ 10)

> Input

    `␊
      1 | iterator.toArray().slice(0, /* comment */ 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().slice(0, /* comment */ 10)␊
        |                    ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    `

## invalid(50): iterator.toArray().slice(0, 10 /* comment */)

> Input

    `␊
      1 | iterator.toArray().slice(0, 10 /* comment */)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().slice(0, 10 /* comment */)␊
        |                    ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    `

## invalid(51): const limit = 10; function page(iterator) { return iterator.toArray().slice(0, limit); }

> Input

    `␊
      1 | const limit = 10; function page(iterator) { return iterator.toArray().slice(0, limit); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const limit = 10; function page(iterator) { return iterator.toArray().slice(0, limit); }␊
        |                                                                       ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | const limit = 10; function page(iterator) { return iterator.take(10).toArray(); }␊
    `

## invalid(1): (iterator as Iterator<number>).toArray().slice(0, 10)

> Input

    `␊
      1 | (iterator as Iterator<number>).toArray().slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (iterator as Iterator<number>).toArray().slice(0, 10)␊
        |                                          ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | (iterator as Iterator<number>).take(10).toArray()␊
    `

## invalid(2): iterator!.toArray().slice(0, 10)

> Input

    `␊
      1 | iterator!.toArray().slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator!.toArray().slice(0, 10)␊
        |                     ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | (iterator!).take(10).toArray()␊
    `

## invalid(3): [...map!.values()].slice(0 as number, 10 satisfies number)

> Input

    `␊
      1 | [...map!.values()].slice(0 as number, 10 satisfies number)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | [...map!.values()].slice(0 as number, 10 satisfies number)␊
        |                    ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | map!.values().take(10).toArray()␊
    `

## invalid(4): Array.from(map.values() satisfies Iterable<number>).slice(0, 10)

> Input

    `␊
      1 | Array.from(map.values() satisfies Iterable<number>).slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Array.from(map.values() satisfies Iterable<number>).slice(0, 10)␊
        |                                                     ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | (map.values() satisfies Iterable<number>).take(10).toArray()␊
    `

## invalid(5): Array.from(<Iterator<number>>map.values()).slice(0, 10)

> Input

    `␊
      1 | Array.from(<Iterator<number>>map.values()).slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Array.from(<Iterator<number>>map.values()).slice(0, 10)␊
        |                                            ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | (<Iterator<number>>map.values()).take(10).toArray()␊
    `

## invalid(6): [...(map.values() as Iterator<number>)].slice(0, 10)

> Input

    `␊
      1 | [...(map.values() as Iterator<number>)].slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | [...(map.values() as Iterator<number>)].slice(0, 10)␊
        |                                         ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | (map.values() as Iterator<number>).take(10).toArray()␊
    `

## invalid(7): function foo(iterator: Iterator<number>) { [...iterator].slice(0, 10); }

> Input

    `␊
      1 | function foo(iterator: Iterator<number>) { [...iterator].slice(0, 10); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(iterator: Iterator<number>) { [...iterator].slice(0, 10); }␊
        |                                                          ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | function foo(iterator: Iterator<number>) { iterator.take(10).toArray(); }␊
    `

## invalid(8): function foo(iterator: IterableIterator<number>) { Array.from(iterator).slice(20); }

> Input

    `␊
      1 | function foo(iterator: IterableIterator<number>) { Array.from(iterator).slice(20); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(iterator: IterableIterator<number>) { Array.from(iterator).slice(20); }␊
        |                                                                         ^^^^^ Prefer \`drop(20)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`drop(20).toArray()\`.:␊
      1 | function foo(iterator: IterableIterator<number>) { iterator.drop(20).toArray(); }␊
    `

## invalid(9): previous() Array.from(map.values() as Iterator<number>).slice(0, 10)

> Input

    `␊
      1 | previous()␊
      2 | Array.from(map.values() as Iterator<number>).slice(0, 10)␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | previous()␊
    > 2 | Array.from(map.values() as Iterator<number>).slice(0, 10)␊
        |                                              ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | previous()␊
      2 | ;(map.values() as Iterator<number>).take(10).toArray()␊
    `

## invalid(1): declare function getIterator(): IteratorObject<number>; [...getIterator()].slice(0, 10);

> Input 'file.ts'

    `␊
      1 | declare function getIterator(): IteratorObject<number>; [...getIterator()].slice(0, 10);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | declare function getIterator(): IteratorObject<number>; [...getIterator()].slice(0, 10);␊
        |                                                                            ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | declare function getIterator(): IteratorObject<number>; getIterator().take(10).toArray();␊
    `

## invalid(2): function * getIterator() { yield 1; } Array.from(getIterator()).slice(0, 10);

> Input 'file.ts'

    `␊
      1 | function * getIterator() { yield 1; } Array.from(getIterator()).slice(0, 10);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function * getIterator() { yield 1; } Array.from(getIterator()).slice(0, 10);␊
        |                                                                 ^^^^^ Prefer \`take(10)\` before materializing the iterator instead of slicing the array.␊
    ␊
    Suggestion 1/1: Use \`take(10).toArray()\`.:␊
      1 | function * getIterator() { yield 1; } getIterator().take(10).toArray();␊
    `
