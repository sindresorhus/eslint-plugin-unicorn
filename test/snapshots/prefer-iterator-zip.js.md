# Snapshot report for `test/prefer-iterator-zip.js`

The actual snapshot is saved in `prefer-iterator-zip.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): for (let index = 0; index < Math.min(Iterators.length, scores.length); index++) { save(Iterators[index], scores[index]); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(Iterators.length, scores.length); index++) { save(Iterators[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(Iterators.length, scores.length); index++) { save(Iterators[index], scores[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [Iterator_, score] of Iterator.zip([Iterators, scores])) { save(Iterator_, score); }␊
    `

## invalid(2): for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { save(name_, score); }␊
    `

## invalid(3): function pair(names, scores) { for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); } }

> Input

    `␊
      1 | function pair(names, scores) { for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); } }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function pair(names, scores) { for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); } }␊
        |                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | function pair(names, scores) { for (const [name_, score] of Iterator.zip([names, scores])) { save(name_, score); } }␊
    `

## invalid(4): const names = ['a', 'b']; const scores = [1]; for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }

> Input

    `␊
      1 | const names = ['a', 'b']; const scores = [1]; for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const names = ['a', 'b']; const scores = [1]; for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
        |                                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | const names = ['a', 'b']; const scores = [1]; for (const [name_, score] of Iterator.zip([names, scores])) { save(name_, score); }␊
    `

## invalid(5): const names = []; const scores = [1]; for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }

> Input

    `␊
      1 | const names = []; const scores = [1]; for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const names = []; const scores = [1]; for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
        |                                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | const names = []; const scores = [1]; for (const [name_, score] of Iterator.zip([names, scores])) { save(name_, score); }␊
    `

## invalid(6): const names = new Uint8Array(2); const scores = new Float64Array(3); for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }

> Input

    `␊
      1 | const names = new Uint8Array(2); const scores = new Float64Array(3); for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const names = new Uint8Array(2); const scores = new Float64Array(3); for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
        |                                                                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | const names = new Uint8Array(2); const scores = new Float64Array(3); for (const [name_, score] of Iterator.zip([names, scores])) { save(name_, score); }␊
    `

## invalid(7): const names = new BigInt64Array(2); const scores = new BigUint64Array(3); for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }

> Input

    `␊
      1 | const names = new BigInt64Array(2); const scores = new BigUint64Array(3); for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const names = new BigInt64Array(2); const scores = new BigUint64Array(3); for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
        |                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | const names = new BigInt64Array(2); const scores = new BigUint64Array(3); for (const [name_, score] of Iterator.zip([names, scores])) { save(name_, score); }␊
    `

## invalid(8): for (let index = 0; index < Math.min(names.length, scores.length); ++index) { save(names[index], scores[index]); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); ++index) { save(names[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); ++index) { save(names[index], scores[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { save(name_, score); }␊
    `

## invalid(9): for (let index = 0; index < Math.min(names.length, scores.length); index += 1) { save(names[index], scores[index]); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index += 1) { save(names[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index += 1) { save(names[index], scores[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { save(name_, score); }␊
    `

## invalid(10): for (let index = 0; Math.min(names.length, scores.length) > index; index++) { save(names[index], scores[index]); }

> Input

    `␊
      1 | for (let index = 0; Math.min(names.length, scores.length) > index; index++) { save(names[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; Math.min(names.length, scores.length) > index; index++) { save(names[index], scores[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { save(name_, score); }␊
    `

## invalid(11): for (let index = 0; index < Math.min(names.length, scores.length, ranks.length); index++) { save(names[index], scores[index], ranks[index]); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length, ranks.length); index++) { save(names[index], scores[index], ranks[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length, ranks.length); index++) { save(names[index], scores[index], ranks[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score, rank] of Iterator.zip([names, scores, ranks])) { save(name_, score, rank); }␊
    `

## invalid(12): for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index], names[index]); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index], names[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index], names[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { save(name_, score, name_); }␊
    `

## invalid(13): for (let index = 0; index < Math.min(names.length, scores.length); index++) { const name = names[index]; const score = scores[index]; save(name, score); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { const name = names[index]; const score = scores[index]; save(name, score); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { const name = names[index]; const score = scores[index]; save(name, score); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score_] of Iterator.zip([names, scores])) { const name = name_; const score = score_; save(name, score); }␊
    `

## invalid(14): for (let index = 0; index < Math.min(names.length, scores.length); index++) { if (names[index]) { save(names[index], scores[index]); } }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { if (names[index]) { save(names[index], scores[index]); } }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { if (names[index]) { save(names[index], scores[index]); } }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { if (name_) { save(name_, score); } }␊
    `

## invalid(15): for (let index = 0; index < Math.min(names.length, scores.length); index++) { if (!names[index]) { continue; } save(names[index], scores[index]); break; }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { if (!names[index]) { continue; } save(names[index], scores[index]); break; }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { if (!names[index]) { continue; } save(names[index], scores[index]); break; }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { if (!name_) { continue; } save(name_, score); break; }␊
    `

## invalid(16): for (let index = 0; index < Math.min(names.length, scores.length); index++) { save((names)[(index)], ((scores[index]))); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save((names)[(index)], ((scores[index]))); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save((names)[(index)], ((scores[index]))); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { save(name_, ((score))); }␊
    `

## invalid(17): for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index].value, scores[index].value); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index].value, scores[index].value); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index].value, scores[index].value); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { save(name_.value, score.value); }␊
    `

## invalid(18): for (let index = 0; index < Math.min(names.length, scores.length); index++) { names[index].save(scores[index]); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { names[index].save(scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { names[index].save(scores[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { name_.save(score); }␊
    `

## invalid(19): for (let index = 0; index < Math.min(names.length, scores.length); index++) { names[index].value = scores[index]; }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { names[index].value = scores[index]; }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { names[index].value = scores[index]; }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { name_.value = score; }␊
    `

## invalid(20): for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(name, names[index], scores[index]); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(name, names[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(name, names[index], scores[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { save(name, name_, score); }␊
    `

## invalid(21): for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); { const name = "inner"; use(name); } }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); { const name = "inner"; use(name); } }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); { const name = "inner"; use(name); } }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { save(name_, score); { const name = "inner"; use(name); } }␊
    `

## invalid(22): for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); { const index = 1; use(index); } }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); { const index = 1; use(index); } }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); { const index = 1; use(index); } }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { save(name_, score); { const index = 1; use(index); } }␊
    `

## invalid(23): for (let index = 0; index < Math.min(names.length, scores.length); index++) { /* keep body comment */ save(names[index], scores[index]); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { /* keep body comment */ save(names[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { /* keep body comment */ save(names[index], scores[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { /* keep body comment */ save(name_, score); }␊
    `

## invalid(24): for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[/* keep access comment */ index], scores[index]); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[/* keep access comment */ index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[/* keep access comment */ index], scores[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    `

## invalid(25): const name = 'existing'; for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(name, names[index], scores[index]); }

> Input

    `␊
      1 | const name = 'existing'; for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(name, names[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const name = 'existing'; for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(name, names[index], scores[index]); }␊
        |                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | const name = 'existing'; for (const [name_, score] of Iterator.zip([names, scores])) { save(name, name_, score); }␊
    `

## invalid(26): for (let index = 0; index < Math.min(items.length, item.length); index++) { save(items[index], item[index]); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(items.length, item.length); index++) { save(items[index], item[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(items.length, item.length); index++) { save(items[index], item[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [item_, element] of Iterator.zip([items, item])) { save(item_, element); }␊
    `

## invalid(27): for (let index = 0; index < Math.min(data.length, metadata.length); index++) { save(data[index], metadata[index]); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(data.length, metadata.length); index++) { save(data[index], metadata[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(data.length, metadata.length); index++) { save(data[index], metadata[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [datum, metadatum] of Iterator.zip([data, metadata])) { save(datum, metadatum); }␊
    `

## invalid(28): for (let index = 0; /* keep header comment */ index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }

> Input

    `␊
      1 | for (let index = 0; /* keep header comment */ index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; /* keep header comment */ index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    `

## invalid(29): for (/* before declaration */ let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }

> Input

    `␊
      1 | for (/* before declaration */ let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (/* before declaration */ let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    `

## invalid(30): for (let index = 0; index < Math.min(names.length, scores.length); index++ /* after update */) { save(names[index], scores[index]); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++ /* after update */) { save(names[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++ /* after update */) { save(names[index], scores[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    `

## invalid(31): function pair(names: readonly string[], scores: number[]) { for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); } }

> Input

    `␊
      1 | function pair(names: readonly string[], scores: number[]) { for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); } }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function pair(names: readonly string[], scores: number[]) { for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); } }␊
        |                                                             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | function pair(names: readonly string[], scores: number[]) { for (const [name_, score] of Iterator.zip([names, scores])) { save(name_, score); } }␊
    `

## invalid(32): for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index]!, scores[index] as number); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index]!, scores[index] as number); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index]!, scores[index] as number); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { save(name_!, score as number); }␊
    `

## invalid(33): for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(<string>names[index], scores[index] satisfies number); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(<string>names[index], scores[index] satisfies number); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(<string>names[index], scores[index] satisfies number); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { save(<string>name_, score satisfies number); }␊
    `

## invalid(34): for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index]<string>, scores[index]); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index]<string>, scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index]<string>, scores[index]); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [name_, score] of Iterator.zip([names, scores])) { save(name_<string>, score); }␊
    `

## invalid(35): for (let index = 0; index < Math.min(Cards.length, scores.length); index++) { render(<Card name={Cards[index]} score={scores[index]} />); }

> Input

    `␊
      1 | for (let index = 0; index < Math.min(Cards.length, scores.length); index++) { render(<Card name={Cards[index]} score={scores[index]} />); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (let index = 0; index < Math.min(Cards.length, scores.length); index++) { render(<Card name={Cards[index]} score={scores[index]} />); }␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | for (const [Card_, score] of Iterator.zip([Cards, scores])) { render(<Card name={Card_} score={score} />); }␊
    `

## invalid(36): declare const names: string[]; declare const scores: readonly number[]; for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }

> Input 'file.ts'

    `␊
      1 | declare const names: string[]; declare const scores: readonly number[]; for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | declare const names: string[]; declare const scores: readonly number[]; for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
        |                                                                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | declare const names: string[]; declare const scores: readonly number[]; for (const [name_, score] of Iterator.zip([names, scores])) { save(name_, score); }␊
    `

## invalid(37): declare const names: Uint8Array; declare const scores: Float64Array; for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }

> Input 'file.ts'

    `␊
      1 | declare const names: Uint8Array; declare const scores: Float64Array; for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | declare const names: Uint8Array; declare const scores: Float64Array; for (let index = 0; index < Math.min(names.length, scores.length); index++) { save(names[index], scores[index]); }␊
        |                                                                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`Iterator.zip(…)\` to iterate over these arrays together.␊
    ␊
    Suggestion 1/1: Use \`Iterator.zip(…)\`.:␊
      1 | declare const names: Uint8Array; declare const scores: Float64Array; for (const [name_, score] of Iterator.zip([names, scores])) { save(name_, score); }␊
    `
