# Snapshot report for `test/no-canvas-to-image.js`

The actual snapshot is saved in `no-canvas-to-image.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): context.drawImage(loadImage(canvas.toDataURL()), 0, 0);

> Input

    `␊
      1 | context.drawImage(loadImage(canvas.toDataURL()), 0, 0);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | context.drawImage(loadImage(canvas.toDataURL()), 0, 0);␊
        |                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer drawing the canvas directly with \`drawImage()\`.␊
    `

## invalid(2): context.drawImage(await loadImage(canvas.toDataURL()), 0, 0);

> Input

    `␊
      1 | context.drawImage(await loadImage(canvas.toDataURL()), 0, 0);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | context.drawImage(await loadImage(canvas.toDataURL()), 0, 0);␊
        |                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer drawing the canvas directly with \`drawImage()\`.␊
    `

## invalid(3): const image = await loadImage(canvas.toDataURL()); context.drawImage(image, 0, 0);

> Input

    `␊
      1 | const image = await loadImage(canvas.toDataURL());␊
      2 | context.drawImage(image, 0, 0);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const image = await loadImage(canvas.toDataURL());␊
    > 2 | context.drawImage(image, 0, 0);␊
        |                   ^^^^^ Prefer drawing the canvas directly with \`drawImage()\`.␊
    `

## invalid(4): const image = loadImage(canvas.toDataURL('image/jpeg')); context.drawImage(await image, 0, 0);

> Input

    `␊
      1 | const image = loadImage(canvas.toDataURL('image/jpeg'));␊
      2 | context.drawImage(await image, 0, 0);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const image = loadImage(canvas.toDataURL('image/jpeg'));␊
    > 2 | context.drawImage(await image, 0, 0);␊
        |                   ^^^^^^^^^^^ Prefer drawing the canvas directly with \`drawImage()\`.␊
    `

## invalid(5): context.putImageData(context.getImageData(0, 0, width, height), 0, 0);

> Input

    `␊
      1 | context.putImageData(context.getImageData(0, 0, width, height), 0, 0);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | context.putImageData(context.getImageData(0, 0, width, height), 0, 0);␊
        |                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer drawing the canvas directly with \`drawImage()\`.␊
    `

## invalid(6): context2.putImageData(context1.getImageData(0, 0, width, height), 0, 0);

> Input

    `␊
      1 | context2.putImageData(context1.getImageData(0, 0, width, height), 0, 0);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | context2.putImageData(context1.getImageData(0, 0, width, height), 0, 0);␊
        |                       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer drawing the canvas directly with \`drawImage()\`.␊
    `

## invalid(7): const imageData = context1.getImageData(0, 0, width, height); context2.putImageData(imageData, 0, 0);

> Input

    `␊
      1 | const imageData = context1.getImageData(0, 0, width, height);␊
      2 | context2.putImageData(imageData, 0, 0);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const imageData = context1.getImageData(0, 0, width, height);␊
    > 2 | context2.putImageData(imageData, 0, 0);␊
        |                       ^^^^^^^^^ Prefer drawing the canvas directly with \`drawImage()\`.␊
    `
