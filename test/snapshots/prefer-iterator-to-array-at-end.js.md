# Snapshot report for `test/prefer-iterator-to-array-at-end.js`

The actual snapshot is saved in `prefer-iterator-to-array-at-end.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): iterator.toArray().filter(x => x > 0)

> Input

    `␊
      1 | iterator.toArray().filter(x => x > 0)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().filter(x => x > 0)␊
        |          ^^^^^^^ Move \`.toArray()\` to the end of the iterator chain; use \`Iterator#filter()\` instead of \`Array#filter()\`.␊
    ␊
    Output:␊
      1 | iterator.filter(x => x > 0).toArray()␊
    `

## invalid(2): iterator.toArray().map(x => x * 2)

> Input

    `␊
      1 | iterator.toArray().map(x => x * 2)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().map(x => x * 2)␊
        |          ^^^^^^^ Move \`.toArray()\` to the end of the iterator chain; use \`Iterator#map()\` instead of \`Array#map()\`.␊
    ␊
    Output:␊
      1 | iterator.map(x => x * 2).toArray()␊
    `

## invalid(3): iter.values().toArray().filter(x => x > 0)

> Input

    `␊
      1 | iter.values().toArray().filter(x => x > 0)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iter.values().toArray().filter(x => x > 0)␊
        |               ^^^^^^^ Move \`.toArray()\` to the end of the iterator chain; use \`Iterator#filter()\` instead of \`Array#filter()\`.␊
    ␊
    Output:␊
      1 | iter.values().filter(x => x > 0).toArray()␊
    `

## invalid(4): iter.take(10).toArray().filter(x => x > 0)

> Input

    `␊
      1 | iter.take(10).toArray().filter(x => x > 0)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iter.take(10).toArray().filter(x => x > 0)␊
        |               ^^^^^^^ Move \`.toArray()\` to the end of the iterator chain; use \`Iterator#filter()\` instead of \`Array#filter()\`.␊
    ␊
    Output:␊
      1 | iter.take(10).filter(x => x > 0).toArray()␊
    `

## invalid(5): iter.toArray().filter(x => x > 0).map(x => x * 2)

> Input

    `␊
      1 | iter.toArray().filter(x => x > 0).map(x => x * 2)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iter.toArray().filter(x => x > 0).map(x => x * 2)␊
        |      ^^^^^^^ Move \`.toArray()\` to the end of the iterator chain; use \`Iterator#filter()\` instead of \`Array#filter()\`.␊
    ␊
    Output:␊
      1 | iter.filter(x => x > 0).toArray().map(x => x * 2)␊
    `

## invalid(6): (iterator.toArray()).filter(x => x > 0)

> Input

    `␊
      1 | (iterator.toArray()).filter(x => x > 0)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (iterator.toArray()).filter(x => x > 0)␊
        |           ^^^^^^^ Move \`.toArray()\` to the end of the iterator chain; use \`Iterator#filter()\` instead of \`Array#filter()\`.␊
    ␊
    Output:␊
      1 | (iterator).filter(x => x > 0).toArray()␊
    `

## invalid(7): iterator.toArray().filter((x, index) => index > 0)

> Input

    `␊
      1 | iterator.toArray().filter((x, index) => index > 0)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().filter((x, index) => index > 0)␊
        |          ^^^^^^^ Move \`.toArray()\` to the end of the iterator chain; use \`Iterator#filter()\` instead of \`Array#filter()\`.␊
    ␊
    Output:␊
      1 | iterator.filter((x, index) => index > 0).toArray()␊
    `

## invalid(8): iterator.toArray().filter(fn)

> Input

    `␊
      1 | iterator.toArray().filter(fn)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().filter(fn)␊
        |          ^^^^^^^ Move \`.toArray()\` to the end of the iterator chain; use \`Iterator#filter()\` instead of \`Array#filter()\`.␊
    `

## invalid(9): iterator.toArray().filter(function(x) { return x > 0; })

> Input

    `␊
      1 | iterator.toArray().filter(function(x) { return x > 0; })␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | iterator.toArray().filter(function(x) { return x > 0; })␊
        |          ^^^^^^^ Move \`.toArray()\` to the end of the iterator chain; use \`Iterator#filter()\` instead of \`Array#filter()\`.␊
    `
