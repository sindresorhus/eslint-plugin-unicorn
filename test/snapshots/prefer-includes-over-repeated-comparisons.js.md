# Snapshot report for `test/prefer-includes-over-repeated-comparisons.js`

The actual snapshot is saved in `prefer-includes-over-repeated-comparisons.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): value === "a" || value === "b";

> Input

    `␊
      1 | value === "a" || value === "b";␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | value === "a" || value === "b";␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `

## invalid(2): "a" === value || "b" === value;

> Input

    `␊
      1 | "a" === value || "b" === value;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "a" === value || "b" === value;␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `

## invalid(3): value === "a" || "b" === value;

> Input

    `␊
      1 | value === "a" || "b" === value;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | value === "a" || "b" === value;␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `

## invalid(4): value === first || value === second;

> Input

    `␊
      1 | value === first || value === second;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | value === first || value === second;␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `

## invalid(5): args[0] === "-h" || args[0] === "--help";

> Input

    `␊
      1 | args[0] === "-h" || args[0] === "--help";␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | args[0] === "-h" || args[0] === "--help";␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `

## invalid(6): object.value === "a" || object.value === "b";

> Input

    `␊
      1 | object.value === "a" || object.value === "b";␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | object.value === "a" || object.value === "b";␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `

## invalid(7): object.foo === "a" || object["foo"] === "b";

> Input

    `␊
      1 | object.foo === "a" || object["foo"] === "b";␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | object.foo === "a" || object["foo"] === "b";␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `

## invalid(8): object[key] === "a" || object[key] === "b";

> Input

    `␊
      1 | object[key] === "a" || object[key] === "b";␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | object[key] === "a" || object[key] === "b";␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `

## invalid(9): value === object.a || value === object.b;

> Input

    `␊
      1 | value === object.a || value === object.b;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | value === object.a || value === object.b;␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `

## invalid(10): (value === "a") || (value === "b");

> Input

    `␊
      1 | (value === "a") || (value === "b");␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (value === "a") || (value === "b");␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `

## invalid(11): (value === "a" || value === "b") && otherValue;

> Input

    `␊
      1 | (value === "a" || value === "b") && otherValue;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (value === "a" || value === "b") && otherValue;␊
        |  ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `

## invalid(12): value === "a" || value === "b" || value === "c";

> Input

    `␊
      1 | value === "a" || value === "b" || value === "c";␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | value === "a" || value === "b" || value === "c";␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `

## invalid(13): value === "a" || value === "b" || value === "c";

> Input

    `␊
    Options:␊
    - minimumComparisons: 3␊
    ␊
    Code:␊
      1 | value === "a" || value === "b" || value === "c";␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | value === "a" || value === "b" || value === "c";␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `

## invalid(14): value! === "a" || value! === "b";

> Input

    `␊
      1 | value! === "a" || value! === "b";␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | value! === "a" || value! === "b";␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `

## invalid(15): (value satisfies string) === "a" || (value satisfies string) === "b";

> Input

    `␊
      1 | (value satisfies string) === "a" || (value satisfies string) === "b";␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (value satisfies string) === "a" || (value satisfies string) === "b";␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `

## invalid(16): (value as {foo?: string}) === "a" || (value as {foo?: string}) === "b";

> Input

    `␊
      1 | (value as {foo?: string}) === "a" || (value as {foo?: string}) === "b";␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (value as {foo?: string}) === "a" || (value as {foo?: string}) === "b";␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `

## invalid(17): (object satisfies Foo).value === "a" || (object satisfies Foo).value === "b";

> Input

    `␊
      1 | (object satisfies Foo).value === "a" || (object satisfies Foo).value === "b";␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (object satisfies Foo).value === "a" || (object satisfies Foo).value === "b";␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use \`.includes()\` instead of repeated equality checks.␊
    `
