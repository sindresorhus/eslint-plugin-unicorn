# Snapshot report for `test/no-blob-to-file.js`

The actual snapshot is saved in `no-blob-to-file.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): const blob = new Blob(); const file = new File([blob], "image.jpg"); URL.createObjectURL(file);

> Input

    `␊
      1 | const blob = new Blob();␊
      2 | const file = new File([blob], "image.jpg");␊
      3 | URL.createObjectURL(file);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const blob = new Blob();␊
    > 2 | const file = new File([blob], "image.jpg");␊
        |              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use the original \`Blob\` instead of converting it to a \`File\`.␊
      3 | URL.createObjectURL(file);␊
    ␊
    Suggestion 1/1: Replace the \`File\` with the original \`Blob\`.:␊
      1 | const blob = new Blob();␊
      2 | URL.createObjectURL(blob);␊
    `

## invalid(2): const imageBlob = new Blob(); const file = new File([imageBlob], "image.jpg"); URL.createObjectURL(file);

> Input

    `␊
      1 | const imageBlob = new Blob();␊
      2 | const file = new File([imageBlob], "image.jpg");␊
      3 | URL.createObjectURL(file);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const imageBlob = new Blob();␊
    > 2 | const file = new File([imageBlob], "image.jpg");␊
        |              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use the original \`Blob\` instead of converting it to a \`File\`.␊
      3 | URL.createObjectURL(file);␊
    ␊
    Suggestion 1/1: Replace the \`File\` with the original \`Blob\`.:␊
      1 | const imageBlob = new Blob();␊
      2 | URL.createObjectURL(imageBlob);␊
    `

## invalid(3): const data = new Blob(); const file = new File([data], "image.jpg"); URL.createObjectURL(file);

> Input

    `␊
      1 | const data = new Blob();␊
      2 | const file = new File([data], "image.jpg");␊
      3 | URL.createObjectURL(file);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const data = new Blob();␊
    > 2 | const file = new File([data], "image.jpg");␊
        |              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use the original \`Blob\` instead of converting it to a \`File\`.␊
      3 | URL.createObjectURL(file);␊
    ␊
    Suggestion 1/1: Replace the \`File\` with the original \`Blob\`.:␊
      1 | const data = new Blob();␊
      2 | URL.createObjectURL(data);␊
    `

## invalid(4): const sourceFile = new File([blob], "source.jpg"); const file = new File([sourceFile], "image.jpg"); URL.createObjectURL(file);

> Input

    `␊
      1 | const sourceFile = new File([blob], "source.jpg");␊
      2 | const file = new File([sourceFile], "image.jpg");␊
      3 | URL.createObjectURL(file);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const sourceFile = new File([blob], "source.jpg");␊
    > 2 | const file = new File([sourceFile], "image.jpg");␊
        |              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use the original \`Blob\` instead of converting it to a \`File\`.␊
      3 | URL.createObjectURL(file);␊
    ␊
    Suggestion 1/1: Replace the \`File\` with the original \`Blob\`.:␊
      1 | const sourceFile = new File([blob], "source.jpg");␊
      2 | URL.createObjectURL(sourceFile);␊
    `

## invalid(5): const blob = new Blob(); const formData = new FormData(); const file = new File([blob], `image.jpg`); formData.append("file", file);

> Input

    `␊
      1 | const blob = new Blob();␊
      2 | const formData = new FormData();␊
      3 | const file = new File([blob], \`image.jpg\`);␊
      4 | formData.append("file", file);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const blob = new Blob();␊
      2 | const formData = new FormData();␊
    > 3 | const file = new File([blob], \`image.jpg\`);␊
        |              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use the original \`Blob\` instead of converting it to a \`File\`.␊
      4 | formData.append("file", file);␊
    ␊
    Suggestion 1/1: Replace the \`File\` with the original \`Blob\`.:␊
      1 | const blob = new Blob();␊
      2 | const formData = new FormData();␊
      3 | formData.append("file", blob, \`image.jpg\`);␊
    `

## invalid(6): const blob = new Blob(); const formData = new FormData(); const file = new File([blob], "image.jpg"); formData.set("file", file);

> Input

    `␊
      1 | const blob = new Blob();␊
      2 | const formData = new FormData();␊
      3 | const file = new File([blob], "image.jpg");␊
      4 | formData.set("file", file);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const blob = new Blob();␊
      2 | const formData = new FormData();␊
    > 3 | const file = new File([blob], "image.jpg");␊
        |              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use the original \`Blob\` instead of converting it to a \`File\`.␊
      4 | formData.set("file", file);␊
    ␊
    Suggestion 1/1: Replace the \`File\` with the original \`Blob\`.:␊
      1 | const blob = new Blob();␊
      2 | const formData = new FormData();␊
      3 | formData.set("file", blob, "image.jpg");␊
    `

## invalid(7): const blob = new Blob(); const formData = new FormData(); const file = new File([blob], "unused.jpg"); formData.append("file", file, "image.jpg");

> Input

    `␊
      1 | const blob = new Blob();␊
      2 | const formData = new FormData();␊
      3 | const file = new File([blob], "unused.jpg");␊
      4 | formData.append("file", file, "image.jpg");␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const blob = new Blob();␊
      2 | const formData = new FormData();␊
    > 3 | const file = new File([blob], "unused.jpg");␊
        |              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use the original \`Blob\` instead of converting it to a \`File\`.␊
      4 | formData.append("file", file, "image.jpg");␊
    ␊
    Suggestion 1/1: Replace the \`File\` with the original \`Blob\`.:␊
      1 | const blob = new Blob();␊
      2 | const formData = new FormData();␊
      3 | formData.append("file", blob, "image.jpg");␊
    `

## invalid(8): const blob = new Blob(); const formData = new FormData(); const file = new File([blob], "image.jpg"); formData.append("file", file);

> Input

    `␊
      1 | const blob = new Blob();␊
      2 | const formData = new FormData();␊
      3 | const file = new File([blob], "image.jpg");␊
      4 | formData.append("file", file);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const blob = new Blob();␊
      2 | const formData = new FormData();␊
    > 3 | const file = new File([blob], "image.jpg");␊
        |              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use the original \`Blob\` instead of converting it to a \`File\`.␊
      4 | formData.append("file", file);␊
    ␊
    Suggestion 1/1: Replace the \`File\` with the original \`Blob\`.:␊
      1 | const blob = new Blob();␊
      2 | const formData = new FormData();␊
      3 | formData.append("file", blob, "image.jpg");␊
    `

## invalid(9): const blob = new Blob(); const data = new FormData(); const file = new File([blob], "image.jpg"); data.append("file", file);

> Input

    `␊
      1 | const blob = new Blob();␊
      2 | const data = new FormData();␊
      3 | const file = new File([blob], "image.jpg");␊
      4 | data.append("file", file);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const blob = new Blob();␊
      2 | const data = new FormData();␊
    > 3 | const file = new File([blob], "image.jpg");␊
        |              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use the original \`Blob\` instead of converting it to a \`File\`.␊
      4 | data.append("file", file);␊
    ␊
    Suggestion 1/1: Replace the \`File\` with the original \`Blob\`.:␊
      1 | const blob = new Blob();␊
      2 | const data = new FormData();␊
      3 | data.append("file", blob, "image.jpg");␊
    `

## invalid(10): const blob = new Blob(); const formData = new FormData(); const file = new File([blob], "image.jpg"); formData.append("file", file, name);

> Input

    `␊
      1 | const blob = new Blob();␊
      2 | const formData = new FormData();␊
      3 | const file = new File([blob], "image.jpg");␊
      4 | formData.append("file", file, name);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const blob = new Blob();␊
      2 | const formData = new FormData();␊
    > 3 | const file = new File([blob], "image.jpg");␊
        |              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Use the original \`Blob\` instead of converting it to a \`File\`.␊
      4 | formData.append("file", file, name);␊
    ␊
    Suggestion 1/1: Replace the \`File\` with the original \`Blob\`.:␊
      1 | const blob = new Blob();␊
      2 | const formData = new FormData();␊
      3 | formData.append("file", blob, name);␊
    `
