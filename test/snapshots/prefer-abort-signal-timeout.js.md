# Snapshot report for `test/prefer-abort-signal-timeout.js`

The actual snapshot is saved in `prefer-abort-signal-timeout.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): const abortController = new AbortController(); setTimeout(() => abortController.abort(), delay); fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | setTimeout(() => abortController.abort(), delay);␊
      3 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.timeout()\` over manually aborting an \`AbortController\` with \`setTimeout()\`.␊
      2 | setTimeout(() => abortController.abort(), delay);␊
      3 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.timeout()\`.:␊
      1 | const abortSignal = AbortSignal.timeout(delay);␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(2): const abortController = new AbortController(); setTimeout(() => { abortController.abort(); }, delay); fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | setTimeout(() => {␊
      3 | 	abortController.abort();␊
      4 | }, delay);␊
      5 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.timeout()\` over manually aborting an \`AbortController\` with \`setTimeout()\`.␊
      2 | setTimeout(() => {␊
      3 | 	abortController.abort();␊
      4 | }, delay);␊
      5 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.timeout()\`.:␊
      1 | const abortSignal = AbortSignal.timeout(delay);␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(3): const abortController = new AbortController(); setTimeout(function () { abortController.abort(); }, delay); fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | setTimeout(function () {␊
      3 | 	abortController.abort();␊
      4 | }, delay);␊
      5 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.timeout()\` over manually aborting an \`AbortController\` with \`setTimeout()\`.␊
      2 | setTimeout(function () {␊
      3 | 	abortController.abort();␊
      4 | }, delay);␊
      5 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.timeout()\`.:␊
      1 | const abortSignal = AbortSignal.timeout(delay);␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(4): const abortController = new AbortController(); setTimeout(() => abortController.abort(), delay); fetch(url, {signal: abortController.signal}); read(abortController.signal);

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | setTimeout(() => abortController.abort(), delay);␊
      3 | fetch(url, {signal: abortController.signal});␊
      4 | read(abortController.signal);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.timeout()\` over manually aborting an \`AbortController\` with \`setTimeout()\`.␊
      2 | setTimeout(() => abortController.abort(), delay);␊
      3 | fetch(url, {signal: abortController.signal});␊
      4 | read(abortController.signal);␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.timeout()\`.:␊
      1 | const abortSignal = AbortSignal.timeout(delay);␊
      2 | fetch(url, {signal: abortSignal});␊
      3 | read(abortSignal);␊
    `

## invalid(5): function foo() { const abortController = new AbortController(); setTimeout(() => abortController.abort(), delay); if (abortController.signal.aborted) { return; } }

> Input

    `␊
      1 | function foo() {␊
      2 | 	const abortController = new AbortController();␊
      3 | 	setTimeout(() => abortController.abort(), delay);␊
      4 | 	if (abortController.signal.aborted) {␊
      5 | 		return;␊
      6 | 	}␊
      7 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | function foo() {␊
    > 2 | 	const abortController = new AbortController();␊
        | 	      ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.timeout()\` over manually aborting an \`AbortController\` with \`setTimeout()\`.␊
      3 | 	setTimeout(() => abortController.abort(), delay);␊
      4 | 	if (abortController.signal.aborted) {␊
      5 | 		return;␊
      6 | 	}␊
      7 | }␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.timeout()\`.:␊
      1 | function foo() {␊
      2 | 	const abortSignal = AbortSignal.timeout(delay);␊
      3 | 	if (abortSignal.aborted) {␊
      4 | 		return;␊
      5 | 	}␊
      6 | }␊
    `

## invalid(6): const abortController: AbortController = new AbortController(); setTimeout(() => abortController.abort(), delay); fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController: AbortController = new AbortController();␊
      2 | setTimeout(() => abortController.abort(), delay);␊
      3 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController: AbortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`AbortSignal.timeout()\` over manually aborting an \`AbortController\` with \`setTimeout()\`.␊
      2 | setTimeout(() => abortController.abort(), delay);␊
      3 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.timeout()\`.:␊
      1 | const abortSignal: AbortSignal = AbortSignal.timeout(delay);␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(7): const abortController = new AbortController(); setTimeout(() => abortController.abort(), 1000); fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | setTimeout(() => abortController.abort(), 1000);␊
      3 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.timeout()\` over manually aborting an \`AbortController\` with \`setTimeout()\`.␊
      2 | setTimeout(() => abortController.abort(), 1000);␊
      3 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.timeout()\`.:␊
      1 | const abortSignal = AbortSignal.timeout(1000);␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(8): const abortController = new AbortController(); setTimeout(() => abortController.abort(), options.timeout); fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | setTimeout(() => abortController.abort(), options.timeout);␊
      3 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.timeout()\` over manually aborting an \`AbortController\` with \`setTimeout()\`.␊
      2 | setTimeout(() => abortController.abort(), options.timeout);␊
      3 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.timeout()\`.:␊
      1 | const abortSignal = AbortSignal.timeout(options.timeout);␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(9): const abortController = new AbortController(); setTimeout(() => abortController.abort(), delay); fetch(url, {signal: (abortController).signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | setTimeout(() => abortController.abort(), delay);␊
      3 | fetch(url, {signal: (abortController).signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.timeout()\` over manually aborting an \`AbortController\` with \`setTimeout()\`.␊
      2 | setTimeout(() => abortController.abort(), delay);␊
      3 | fetch(url, {signal: (abortController).signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.timeout()\`.:␊
      1 | const abortSignal = AbortSignal.timeout(delay);␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(10): const abortController = new AbortController(); setTimeout(() => abortController.abort(), (timeout + extraDelay)); fetch(url, {signal: abortController.signal});

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | setTimeout(() => abortController.abort(), (timeout + extraDelay));␊
      3 | fetch(url, {signal: abortController.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.timeout()\` over manually aborting an \`AbortController\` with \`setTimeout()\`.␊
      2 | setTimeout(() => abortController.abort(), (timeout + extraDelay));␊
      3 | fetch(url, {signal: abortController.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.timeout()\`.:␊
      1 | const abortSignal = AbortSignal.timeout(timeout + extraDelay);␊
      2 | fetch(url, {signal: abortSignal});␊
    `

## invalid(11): const controller = new AbortController(); setTimeout(() => controller.abort(), delay); fetch(url, {signal: controller.signal});

> Input

    `␊
      1 | const controller = new AbortController();␊
      2 | setTimeout(() => controller.abort(), delay);␊
      3 | fetch(url, {signal: controller.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const controller = new AbortController();␊
        |       ^^^^^^^^^^ Prefer \`AbortSignal.timeout()\` over manually aborting an \`AbortController\` with \`setTimeout()\`.␊
      2 | setTimeout(() => controller.abort(), delay);␊
      3 | fetch(url, {signal: controller.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.timeout()\`.:␊
      1 | const signal = AbortSignal.timeout(delay);␊
      2 | fetch(url, {signal: signal});␊
    `

## invalid(12): const abortController = new AbortController(); setTimeout(() => abortController.abort(), delay); { const abortSignal = getSignal(); fetch(url, {signal: abortController.signal}); }

> Input

    `␊
      1 | const abortController = new AbortController();␊
      2 | setTimeout(() => abortController.abort(), delay);␊
      3 | {␊
      4 | 	const abortSignal = getSignal();␊
      5 | 	fetch(url, {signal: abortController.signal});␊
      6 | }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const abortController = new AbortController();␊
        |       ^^^^^^^^^^^^^^^ Prefer \`AbortSignal.timeout()\` over manually aborting an \`AbortController\` with \`setTimeout()\`.␊
      2 | setTimeout(() => abortController.abort(), delay);␊
      3 | {␊
      4 | 	const abortSignal = getSignal();␊
      5 | 	fetch(url, {signal: abortController.signal});␊
      6 | }␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.timeout()\`.:␊
      1 | const abortController = AbortSignal.timeout(delay);␊
      2 | {␊
      3 | 	const abortSignal = getSignal();␊
      4 | 	fetch(url, {signal: abortController});␊
      5 | }␊
    `

## invalid(13): const signal = getSignal(); const controller = new AbortController(); setTimeout(() => controller.abort(), delay); fetch(url, {signal: controller.signal});

> Input

    `␊
      1 | const signal = getSignal();␊
      2 | const controller = new AbortController();␊
      3 | setTimeout(() => controller.abort(), delay);␊
      4 | fetch(url, {signal: controller.signal});␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const signal = getSignal();␊
    > 2 | const controller = new AbortController();␊
        |       ^^^^^^^^^^ Prefer \`AbortSignal.timeout()\` over manually aborting an \`AbortController\` with \`setTimeout()\`.␊
      3 | setTimeout(() => controller.abort(), delay);␊
      4 | fetch(url, {signal: controller.signal});␊
    ␊
    Suggestion 1/1: Replace with \`AbortSignal.timeout()\`.:␊
      1 | const signal = getSignal();␊
      2 | const controller = AbortSignal.timeout(delay);␊
      3 | fetch(url, {signal: controller});␊
    `
