# Snapshot report for `test/prefer-array-flat-map.mjs`

The actual snapshot is saved in `prefer-array-flat-map.mjs.snap`.

Generated by [AVA](https://avajs.dev).

## Invalid(1): const bar = [[1],[2],[3]].map(i => [i]).flat()

> Input

    `␊
      1 | const bar = [[1],[2],[3]].map(i => [i]).flat()␊
    `

> Output

    `␊
      1 | const bar = [[1],[2],[3]].flatMap(i => [i])␊
    `

> Error 1/1

    `␊
    > 1 | const bar = [[1],[2],[3]].map(i => [i]).flat()␊
        |                           ^^^^^^^^^^^^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(2): const bar = [[1],[2],[3]].map(i => [i]).flat(1,)

> Input

    `␊
      1 | const bar = [[1],[2],[3]].map(i => [i]).flat(1,)␊
    `

> Output

    `␊
      1 | const bar = [[1],[2],[3]].flatMap(i => [i])␊
    `

> Error 1/1

    `␊
    > 1 | const bar = [[1],[2],[3]].map(i => [i]).flat(1,)␊
        |                           ^^^^^^^^^^^^^^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(3): const bar = [1,2,3].map(i => [i]).flat()

> Input

    `␊
      1 | const bar = [1,2,3].map(i => [i]).flat()␊
    `

> Output

    `␊
      1 | const bar = [1,2,3].flatMap(i => [i])␊
    `

> Error 1/1

    `␊
    > 1 | const bar = [1,2,3].map(i => [i]).flat()␊
        |                     ^^^^^^^^^^^^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(4): const bar = [1,2,3].map((i) => [i]).flat()

> Input

    `␊
      1 | const bar = [1,2,3].map((i) => [i]).flat()␊
    `

> Output

    `␊
      1 | const bar = [1,2,3].flatMap((i) => [i])␊
    `

> Error 1/1

    `␊
    > 1 | const bar = [1,2,3].map((i) => [i]).flat()␊
        |                     ^^^^^^^^^^^^^^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(5): const bar = [1,2,3].map((i) => { return [i]; }).flat()

> Input

    `␊
      1 | const bar = [1,2,3].map((i) => { return [i]; }).flat()␊
    `

> Output

    `␊
      1 | const bar = [1,2,3].flatMap((i) => { return [i]; })␊
    `

> Error 1/1

    `␊
    > 1 | const bar = [1,2,3].map((i) => { return [i]; }).flat()␊
        |                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(6): const bar = [1,2,3].map(foo).flat()

> Input

    `␊
      1 | const bar = [1,2,3].map(foo).flat()␊
    `

> Output

    `␊
      1 | const bar = [1,2,3].flatMap(foo)␊
    `

> Error 1/1

    `␊
    > 1 | const bar = [1,2,3].map(foo).flat()␊
        |                     ^^^^^^^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(7): const bar = foo.map(i => [i]).flat()

> Input

    `␊
      1 | const bar = foo.map(i => [i]).flat()␊
    `

> Output

    `␊
      1 | const bar = foo.flatMap(i => [i])␊
    `

> Error 1/1

    `␊
    > 1 | const bar = foo.map(i => [i]).flat()␊
        |                 ^^^^^^^^^^^^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(8): const bar = { map: () => {} }.map(i => [i]).flat()

> Input

    `␊
      1 | const bar = { map: () => {} }.map(i => [i]).flat()␊
    `

> Output

    `␊
      1 | const bar = { map: () => {} }.flatMap(i => [i])␊
    `

> Error 1/1

    `␊
    > 1 | const bar = { map: () => {} }.map(i => [i]).flat()␊
        |                               ^^^^^^^^^^^^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(9): const bar = [1,2,3].map(i => i).map(i => [i]).flat()

> Input

    `␊
      1 | const bar = [1,2,3].map(i => i).map(i => [i]).flat()␊
    `

> Output

    `␊
      1 | const bar = [1,2,3].map(i => i).flatMap(i => [i])␊
    `

> Error 1/1

    `␊
    > 1 | const bar = [1,2,3].map(i => i).map(i => [i]).flat()␊
        |                                 ^^^^^^^^^^^^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(10): const bar = [1,2,3].sort().map(i => [i]).flat()

> Input

    `␊
      1 | const bar = [1,2,3].sort().map(i => [i]).flat()␊
    `

> Output

    `␊
      1 | const bar = [1,2,3].sort().flatMap(i => [i])␊
    `

> Error 1/1

    `␊
    > 1 | const bar = [1,2,3].sort().map(i => [i]).flat()␊
        |                            ^^^^^^^^^^^^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(11): const bar = (([1,2,3].map(i => [i]))).flat()

> Input

    `␊
      1 | const bar = (([1,2,3].map(i => [i]))).flat()␊
    `

> Output

    `␊
      1 | const bar = (([1,2,3].flatMap(i => [i])))␊
    `

> Error 1/1

    `␊
    > 1 | const bar = (([1,2,3].map(i => [i]))).flat()␊
        |                       ^^^^^^^^^^^^^^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(12): let bar = [1,2,3].map(i => { return [i]; }).flat();

> Input

    `␊
      1 | let bar = [1,2,3].map(i => {␊
      2 | 	return [i];␊
      3 | }).flat();␊
    `

> Output

    `␊
      1 | let bar = [1,2,3].flatMap(i => {␊
      2 | 	return [i];␊
      3 | });␊
    `

> Error 1/1

    `␊
    > 1 | let bar = [1,2,3].map(i => {␊
        |                   ^^^^^^^^^^␊
    > 2 | 	return [i];␊
        | ^^^^^^^^^^^^␊
    > 3 | }).flat();␊
        | ^^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(13): let bar = [1,2,3].map(i => { return [i]; }) .flat();

> Input

    `␊
      1 | let bar = [1,2,3].map(i => {␊
      2 | 	return [i];␊
      3 | })␊
      4 | .flat();␊
    `

> Output

    `␊
      1 | let bar = [1,2,3].flatMap(i => {␊
      2 | 	return [i];␊
      3 | });␊
    `

> Error 1/1

    `␊
    > 1 | let bar = [1,2,3].map(i => {␊
        |                   ^^^^^^^^^^␊
    > 2 | 	return [i];␊
        | ^^^^^^^^^^^^␊
    > 3 | })␊
        | ^^^^^^^^^^^^␊
    > 4 | .flat();␊
        | ^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(14): let bar = [1,2,3].map(i => { return [i]; }) // comment .flat();

> Input

    `␊
      1 | let bar = [1,2,3].map(i => {␊
      2 | 	return [i];␊
      3 | }) // comment␊
      4 | .flat();␊
    `

> Output

    `␊
      1 | let bar = [1,2,3].flatMap(i => {␊
      2 | 	return [i];␊
      3 | });␊
    `

> Error 1/1

    `␊
    > 1 | let bar = [1,2,3].map(i => {␊
        |                   ^^^^^^^^^^␊
    > 2 | 	return [i];␊
        | ^^^^^^^^^^^^␊
    > 3 | }) // comment␊
        | ^^^^^^^^^^^^␊
    > 4 | .flat();␊
        | ^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(15): let bar = [1,2,3].map(i => { return [i]; }) // comment .flat(); // other

> Input

    `␊
      1 | let bar = [1,2,3].map(i => {␊
      2 | 	return [i];␊
      3 | }) // comment␊
      4 | .flat(); // other␊
    `

> Output

    `␊
      1 | let bar = [1,2,3].flatMap(i => {␊
      2 | 	return [i];␊
      3 | }); // other␊
    `

> Error 1/1

    `␊
    > 1 | let bar = [1,2,3].map(i => {␊
        |                   ^^^^^^^^^^␊
    > 2 | 	return [i];␊
        | ^^^^^^^^^^^^␊
    > 3 | }) // comment␊
        | ^^^^^^^^^^^^␊
    > 4 | .flat(); // other␊
        | ^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(16): let bar = [1,2,3] .map(i => { return [i]; }) .flat();

> Input

    `␊
      1 | let bar = [1,2,3]␊
      2 | 	.map(i => { return [i]; })␊
      3 | 	.flat();␊
    `

> Output

    `␊
      1 | let bar = [1,2,3]␊
      2 | 	.flatMap(i => { return [i]; });␊
    `

> Error 1/1

    `␊
      1 | let bar = [1,2,3]␊
    > 2 | 	.map(i => { return [i]; })␊
        | 	 ^^^^^^^^^^^^^^^^^^^^^^^^^␊
    > 3 | 	.flat();␊
        | ^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(17): let bar = [1,2,3].map(i => { return [i]; }) .flat();

> Input

    `␊
      1 | let bar = [1,2,3].map(i => { return [i]; })␊
      2 | 	.flat();␊
    `

> Output

    `␊
      1 | let bar = [1,2,3].flatMap(i => { return [i]; });␊
    `

> Error 1/1

    `␊
    > 1 | let bar = [1,2,3].map(i => { return [i]; })␊
        |                   ^^^^^^^^^^^^^^^^^^^^^^^^^␊
    > 2 | 	.flat();␊
        | ^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(18): let bar = [1,2,3] . map( x => y ) . flat () // 🤪

> Input

    `␊
      1 | let bar = [1,2,3] . map( x => y ) . flat () // 🤪␊
    `

> Output

    `␊
      1 | let bar = [1,2,3] . flatMap( x => y ) // 🤪␊
    `

> Error 1/1

    `␊
    > 1 | let bar = [1,2,3] . map( x => y ) . flat () // 🤪␊
        |                     ^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(19): const bar = [1,2,3].map(i => [i]).flat(1);

> Input

    `␊
      1 | const bar = [1,2,3].map(i => [i]).flat(1);␊
    `

> Output

    `␊
      1 | const bar = [1,2,3].flatMap(i => [i]);␊
    `

> Error 1/1

    `␊
    > 1 | const bar = [1,2,3].map(i => [i]).flat(1);␊
        |                     ^^^^^^^^^^^^^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `

## Invalid(20): const foo = bars .filter(foo => !!foo.zaz) .map(foo => doFoo(foo)) .flat();

> Input

    `␊
      1 | const foo = bars␊
      2 | 	.filter(foo => !!foo.zaz)␊
      3 | 	.map(foo => doFoo(foo))␊
      4 | 	.flat();␊
    `

> Output

    `␊
      1 | const foo = bars␊
      2 | 	.filter(foo => !!foo.zaz)␊
      3 | 	.flatMap(foo => doFoo(foo));␊
    `

> Error 1/1

    `␊
      1 | const foo = bars␊
      2 | 	.filter(foo => !!foo.zaz)␊
    > 3 | 	.map(foo => doFoo(foo))␊
        | 	 ^^^^^^^^^^^^^^^^^^^^^^␊
    > 4 | 	.flat();␊
        | ^^^^^^^^^ Prefer \`.flatMap(…)\` over \`.map(…).flat()\`.␊
    `
