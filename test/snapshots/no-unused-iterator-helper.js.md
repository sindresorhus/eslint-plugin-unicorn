# Snapshot report for `test/no-unused-iterator-helper.js`

The actual snapshot is saved in `no-unused-iterator-helper.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): items.values().map(transform);

> Input

    `␊
      1 | items.values().map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.values().map(transform);␊
        |                ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | items.values().forEach(transform);␊
    `

## invalid(2): items.values().filter(predicate);

> Input

    `␊
      1 | items.values().filter(predicate);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.values().filter(predicate);␊
        |                ^^^^^^ Consume the iterator returned by \`.filter(…)\`; iterator helpers are lazy.␊
    `

## invalid(3): items.values().flatMap(transform);

> Input

    `␊
      1 | items.values().flatMap(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.values().flatMap(transform);␊
        |                ^^^^^^^ Consume the iterator returned by \`.flatMap(…)\`; iterator helpers are lazy.␊
    `

## invalid(4): items.values().take(2);

> Input

    `␊
      1 | items.values().take(2);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.values().take(2);␊
        |                ^^^^ Consume the iterator returned by \`.take(…)\`; iterator helpers are lazy.␊
    `

## invalid(5): items.values().drop(2);

> Input

    `␊
      1 | items.values().drop(2);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.values().drop(2);␊
        |                ^^^^ Consume the iterator returned by \`.drop(…)\`; iterator helpers are lazy.␊
    `

## invalid(6): items.keys().map(transform);

> Input

    `␊
      1 | items.keys().map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.keys().map(transform);␊
        |              ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | items.keys().forEach(transform);␊
    `

## invalid(7): items.entries().filter(predicate);

> Input

    `␊
      1 | items.entries().filter(predicate);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.entries().filter(predicate);␊
        |                 ^^^^^^ Consume the iterator returned by \`.filter(…)\`; iterator helpers are lazy.␊
    `

## invalid(8): text.matchAll(pattern).map(transform);

> Input

    `␊
      1 | text.matchAll(pattern).map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | text.matchAll(pattern).map(transform);␊
        |                        ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | text.matchAll(pattern).forEach(transform);␊
    `

## invalid(9): Iterator.from(items).map(transform);

> Input

    `␊
      1 | Iterator.from(items).map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Iterator.from(items).map(transform);␊
        |                      ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | Iterator.from(items).forEach(transform);␊
    `

## invalid(10): globalThis.Iterator.from(items).map(transform);

> Input

    `␊
      1 | globalThis.Iterator.from(items).map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | globalThis.Iterator.from(items).map(transform);␊
        |                                 ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | globalThis.Iterator.from(items).forEach(transform);␊
    `

## invalid(11): Iterator.concat(first, second).take(2);

> Input

    `␊
      1 | Iterator.concat(first, second).take(2);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Iterator.concat(first, second).take(2);␊
        |                                ^^^^ Consume the iterator returned by \`.take(…)\`; iterator helpers are lazy.␊
    `

## invalid(12): Iterator.zip(iterables).filter(predicate);

> Input

    `␊
      1 | Iterator.zip(iterables).filter(predicate);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Iterator.zip(iterables).filter(predicate);␊
        |                         ^^^^^^ Consume the iterator returned by \`.filter(…)\`; iterator helpers are lazy.␊
    `

## invalid(13): Iterator.zipKeyed(iterables).drop(1);

> Input

    `␊
      1 | Iterator.zipKeyed(iterables).drop(1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Iterator.zipKeyed(iterables).drop(1);␊
        |                              ^^^^ Consume the iterator returned by \`.drop(…)\`; iterator helpers are lazy.␊
    `

## invalid(14): items.values().map(transform).filter(predicate).take(2);

> Input

    `␊
      1 | items.values().map(transform).filter(predicate).take(2);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.values().map(transform).filter(predicate).take(2);␊
        |                                                 ^^^^ Consume the iterator returned by \`.take(…)\`; iterator helpers are lazy.␊
    `

## invalid(15): items.values().filter(predicate).map(transform);

> Input

    `␊
      1 | items.values().filter(predicate).map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.values().filter(predicate).map(transform);␊
        |                                  ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | items.values().filter(predicate).forEach(transform);␊
    `

## invalid(16): items?.values().map(transform);

> Input

    `␊
      1 | items?.values().map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items?.values().map(transform);␊
        |                 ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | items?.values().forEach(transform);␊
    `

## invalid(17): items.values()?.map(transform);

> Input

    `␊
      1 | items.values()?.map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.values()?.map(transform);␊
        |                 ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | items.values()?.forEach(transform);␊
    `

## invalid(18): items.values().map?.(transform);

> Input

    `␊
      1 | items.values().map?.(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.values().map?.(transform);␊
        |                ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | items.values().forEach?.(transform);␊
    `

## invalid(19): void items.values().map(transform);

> Input

    `␊
      1 | void items.values().map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | void items.values().map(transform);␊
        |                     ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | void items.values().forEach(transform);␊
    `

## invalid(20): await items.values().map(transform);

> Input

    `␊
      1 | await items.values().map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | await items.values().map(transform);␊
        |                      ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | await items.values().forEach(transform);␊
    `

## invalid(21): const unused = void items.values().map(transform);

> Input

    `␊
      1 | const unused = void items.values().map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const unused = void items.values().map(transform);␊
        |                                    ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | const unused = void items.values().forEach(transform);␊
    `

## invalid(22): function run() { return void items.values().map(transform); }

> Input

    `␊
      1 | function run() { return void items.values().map(transform); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function run() { return void items.values().map(transform); }␊
        |                                             ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | function run() { return void items.values().forEach(transform); }␊
    `

## invalid(23): const view = <div>{void items.values().map(transform)}</div>;

> Input

    `␊
      1 | const view = <div>{void items.values().map(transform)}</div>;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const view = <div>{void items.values().map(transform)}</div>;␊
        |                                        ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | const view = <div>{void items.values().forEach(transform)}</div>;␊
    `

## invalid(24): for (items.values().map(transform); condition;) {}

> Input

    `␊
      1 | for (items.values().map(transform); condition;) {}␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (items.values().map(transform); condition;) {}␊
        |                     ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | for (items.values().forEach(transform); condition;) {}␊
    `

## invalid(25): for (; condition; items.values().drop(1)) {}

> Input

    `␊
      1 | for (; condition; items.values().drop(1)) {}␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (; condition; items.values().drop(1)) {}␊
        |                                  ^^^^ Consume the iterator returned by \`.drop(…)\`; iterator helpers are lazy.␊
    `

## invalid(26): ((items.values()).map(transform));

> Input

    `␊
      1 | ((items.values()).map(transform));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | ((items.values()).map(transform));␊
        |                   ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | ((items.values()).forEach(transform));␊
    `

## invalid(27): items.values() /* receiver */ .map /* method */ (/* callback */ transform);

> Input

    `␊
      1 | items.values() /* receiver */ .map /* method */ (/* callback */ transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.values() /* receiver */ .map /* method */ (/* callback */ transform);␊
        |                                ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | items.values() /* receiver */ .forEach /* method */ (/* callback */ transform);␊
    `

## invalid(28): items.values().map(transform, extra);

> Input

    `␊
      1 | items.values().map(transform, extra);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.values().map(transform, extra);␊
        |                ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    `

## invalid(29): items.values().map(...callbacks);

> Input

    `␊
      1 | items.values().map(...callbacks);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.values().map(...callbacks);␊
        |                ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    `

## invalid(30): const iterator = items.values(); iterator.map(transform);

> Input

    `␊
      1 | const iterator = items.values(); iterator.map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const iterator = items.values(); iterator.map(transform);␊
        |                                           ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | const iterator = items.values(); iterator.forEach(transform);␊
    `

## invalid(31): const iterator = items.values(); const alias = iterator; alias.drop(1);

> Input

    `␊
      1 | const iterator = items.values(); const alias = iterator; alias.drop(1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const iterator = items.values(); const alias = iterator; alias.drop(1);␊
        |                                                                ^^^^ Consume the iterator returned by \`.drop(…)\`; iterator helpers are lazy.␊
    `

## invalid(32): const iterator = items.values().map(transform); iterator.filter(predicate);

> Input

    `␊
      1 | const iterator = items.values().map(transform); iterator.filter(predicate);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const iterator = items.values().map(transform); iterator.filter(predicate);␊
        |                                                          ^^^^^^ Consume the iterator returned by \`.filter(…)\`; iterator helpers are lazy.␊
    `

## invalid(33): function * generate() { yield 1; } generate().map(transform);

> Input

    `␊
      1 | function * generate() { yield 1; } generate().map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function * generate() { yield 1; } generate().map(transform);␊
        |                                               ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | function * generate() { yield 1; } generate().forEach(transform);␊
    `

## invalid(34): const generate = function * () { yield 1; }; generate().take(1);

> Input

    `␊
      1 | const generate = function * () { yield 1; }; generate().take(1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const generate = function * () { yield 1; }; generate().take(1);␊
        |                                                         ^^^^ Consume the iterator returned by \`.take(…)\`; iterator helpers are lazy.␊
    `

## invalid(35): function * generate() { yield 1; } const alias = generate; alias().map(transform);

> Input

    `␊
      1 | function * generate() { yield 1; } const alias = generate; alias().map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function * generate() { yield 1; } const alias = generate; alias().map(transform);␊
        |                                                                    ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | function * generate() { yield 1; } const alias = generate; alias().forEach(transform);␊
    `

## invalid(36): function run(iterator: Iterator<number>) { iterator.map(transform); }

> Input

    `␊
      1 | function run(iterator: Iterator<number>) { iterator.map(transform); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function run(iterator: Iterator<number>) { iterator.map(transform); }␊
        |                                                     ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | function run(iterator: Iterator<number>) { iterator.forEach(transform); }␊
    `

## invalid(37): function run(iterator: IterableIterator<number>) { iterator.drop(1); }

> Input

    `␊
      1 | function run(iterator: IterableIterator<number>) { iterator.drop(1); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function run(iterator: IterableIterator<number>) { iterator.drop(1); }␊
        |                                                             ^^^^ Consume the iterator returned by \`.drop(…)\`; iterator helpers are lazy.␊
    `

## invalid(38): function run(iterator: StringIterator<string>) { iterator.map(transform); }

> Input

    `␊
      1 | function run(iterator: StringIterator<string>) { iterator.map(transform); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function run(iterator: StringIterator<string>) { iterator.map(transform); }␊
        |                                                           ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | function run(iterator: StringIterator<string>) { iterator.forEach(transform); }␊
    `

## invalid(39): const iterator = items!.values(); iterator.map(transform);

> Input

    `␊
      1 | const iterator = items!.values(); iterator.map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const iterator = items!.values(); iterator.map(transform);␊
        |                                            ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | const iterator = items!.values(); iterator.forEach(transform);␊
    `

## invalid(40): (items.values() as Iterator<number>).map(transform);

> Input

    `␊
      1 | (items.values() as Iterator<number>).map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (items.values() as Iterator<number>).map(transform);␊
        |                                      ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | (items.values() as Iterator<number>).forEach(transform);␊
    `

## invalid(41): items.values().map<number>(transform);

> Input

    `␊
      1 | items.values().map<number>(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.values().map<number>(transform);␊
        |                ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    `

## invalid(42): items.values().map(transform) as Iterator<number>;

> Input

    `␊
      1 | items.values().map(transform) as Iterator<number>;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.values().map(transform) as Iterator<number>;␊
        |                ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    `

## invalid(43): <Iterator<number>>items.values().map(transform);

> Input

    `␊
      1 | <Iterator<number>>items.values().map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | <Iterator<number>>items.values().map(transform);␊
        |                                  ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    `

## invalid(44): items.values().map(transform)!;

> Input

    `␊
      1 | items.values().map(transform)!;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.values().map(transform)!;␊
        |                ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    `

## invalid(45): items.values().map(transform) satisfies Iterator<number>;

> Input

    `␊
      1 | items.values().map(transform) satisfies Iterator<number>;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.values().map(transform) satisfies Iterator<number>;␊
        |                ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    `

## invalid(46): (await items.values().map(transform)) as Iterator<number>;

> Input

    `␊
      1 | (await items.values().map(transform)) as Iterator<number>;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (await items.values().map(transform)) as Iterator<number>;␊
        |                       ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    `

## invalid(47): void (items.values().map(transform) as Iterator<number>);

> Input

    `␊
      1 | void (items.values().map(transform) as Iterator<number>);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | void (items.values().map(transform) as Iterator<number>);␊
        |                      ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    `

## invalid(48): declare function getIterator(): IteratorObject<number>; getIterator().map(value => value);

> Input 'file.ts'

    `␊
      1 | declare function getIterator(): IteratorObject<number>; getIterator().map(value => value);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | declare function getIterator(): IteratorObject<number>; getIterator().map(value => value);␊
        |                                                                       ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | declare function getIterator(): IteratorObject<number>; getIterator().forEach(value => value);␊
    `

## invalid(49): const iterator = "value"[Symbol.iterator](); iterator.map(value => value);

> Input 'file.ts'

    `␊
      1 | const iterator = "value"[Symbol.iterator](); iterator.map(value => value);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const iterator = "value"[Symbol.iterator](); iterator.map(value => value);␊
        |                                                       ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | const iterator = "value"[Symbol.iterator](); iterator.forEach(value => value);␊
    `

## invalid(50): const iterator = new URLSearchParams()[Symbol.iterator](); iterator.map(value => value);

> Input 'file.ts'

    `␊
      1 | const iterator = new URLSearchParams()[Symbol.iterator](); iterator.map(value => value);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const iterator = new URLSearchParams()[Symbol.iterator](); iterator.map(value => value);␊
        |                                                                     ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | const iterator = new URLSearchParams()[Symbol.iterator](); iterator.forEach(value => value);␊
    `

## invalid(51): function * generate() { yield 1; } generate().map(value => value);

> Input 'file.ts'

    `␊
      1 | function * generate() { yield 1; } generate().map(value => value);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function * generate() { yield 1; } generate().map(value => value);␊
        |                                               ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | function * generate() { yield 1; } generate().forEach(value => value);␊
    `

## invalid(52): (source as Iterator<number>).map(transform);

> Input

    `␊
      1 | (source as Iterator<number>).map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (source as Iterator<number>).map(transform);␊
        |                              ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | (source as Iterator<number>).forEach(transform);␊
    `

## invalid(53): (<IteratorObject<number>>source).drop(1);

> Input

    `␊
      1 | (<IteratorObject<number>>source).drop(1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (<IteratorObject<number>>source).drop(1);␊
        |                                  ^^^^ Consume the iterator returned by \`.drop(…)\`; iterator helpers are lazy.␊
    `

## invalid(54): (source as Iterator<number>)!.map(transform);

> Input

    `␊
      1 | (source as Iterator<number>)!.map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (source as Iterator<number>)!.map(transform);␊
        |                               ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | (source as Iterator<number>)!.forEach(transform);␊
    `

## invalid(55): void await (items.values().map(transform) as Iterator<number>);

> Input

    `␊
      1 | void await (items.values().map(transform) as Iterator<number>);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | void await (items.values().map(transform) as Iterator<number>);␊
        |                            ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    `

## invalid(56): for (; condition; items.values().filter(predicate).map(transform)) {}

> Input

    `␊
      1 | for (; condition; items.values().filter(predicate).map(transform)) {}␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | for (; condition; items.values().filter(predicate).map(transform)) {}␊
        |                                                    ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | for (; condition; items.values().filter(predicate).forEach(transform)) {}␊
    `

## invalid(57): type SetIterator<T> = T[]; function run(iterator: IteratorObject<number>) { iterator.map(transform); }

> Input

    `␊
      1 | type SetIterator<T> = T[]; function run(iterator: IteratorObject<number>) { iterator.map(transform); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | type SetIterator<T> = T[]; function run(iterator: IteratorObject<number>) { iterator.map(transform); }␊
        |                                                                                      ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | type SetIterator<T> = T[]; function run(iterator: IteratorObject<number>) { iterator.forEach(transform); }␊
    `

## invalid(58): type SetIterator<T> = T[]; declare function getIterator(): IteratorObject<number>; getIterator().map(transform);

> Input 'file.ts'

    `␊
      1 | type SetIterator<T> = T[]; declare function getIterator(): IteratorObject<number>; getIterator().map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | type SetIterator<T> = T[]; declare function getIterator(): IteratorObject<number>; getIterator().map(transform);␊
        |                                                                                                  ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | type SetIterator<T> = T[]; declare function getIterator(): IteratorObject<number>; getIterator().forEach(transform);␊
    `

## invalid(59): text.matchAll().map(transform);

> Input

    `␊
      1 | text.matchAll().map(transform);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | text.matchAll().map(transform);␊
        |                 ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | text.matchAll().forEach(transform);␊
    `

## invalid(60): function run(iterator: IteratorObject<number> | undefined) { iterator!.map(transform); }

> Input

    `␊
      1 | function run(iterator: IteratorObject<number> | undefined) { iterator!.map(transform); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function run(iterator: IteratorObject<number> | undefined) { iterator!.map(transform); }␊
        |                                                                        ^^^ Consume the iterator returned by \`.map(…)\`; iterator helpers are lazy.␊
    ␊
    Suggestion 1/1: Use \`.forEach()\` to run the callback.:␊
      1 | function run(iterator: IteratorObject<number> | undefined) { iterator!.forEach(transform); }␊
    `
