# Snapshot report for `test/no-invalid-character-comparison.js`

The actual snapshot is saved in `no-invalid-character-comparison.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): "abc".charAt(4) === "ab"

> Input

    `␊
      1 | "abc".charAt(4) === "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc".charAt(4) === "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(2): "abc".charAt(4) !== "ab"

> Input

    `␊
      1 | "abc".charAt(4) !== "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc".charAt(4) !== "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always true.␊
    `

## invalid(3): "abc".charAt(0) == "ab"

> Input

    `␊
      1 | "abc".charAt(0) == "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc".charAt(0) == "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(4): "abc".charAt(0) != "ab"

> Input

    `␊
      1 | "abc".charAt(0) != "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc".charAt(0) != "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always true.␊
    `

## invalid(5): "ab" === "abc".charAt(4)

> Input

    `␊
      1 | "ab" === "abc".charAt(4)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "ab" === "abc".charAt(4)␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(6): ("abc".charAt(0)) === "ab"

> Input

    `␊
      1 | ("abc".charAt(0)) === "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | ("abc".charAt(0)) === "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(7): "abc".charAt(0) === ("ab")

> Input

    `␊
      1 | "abc".charAt(0) === ("ab")␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc".charAt(0) === ("ab")␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(8): "abc".charAt(3) === "/n"

> Input

    `␊
      1 | "abc".charAt(3) === "/n"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc".charAt(3) === "/n"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`/n\`, so this comparison is always false.␊
    `

## invalid(9): "abc".charAt(822) !== "foo"

> Input

    `␊
      1 | "abc".charAt(822) !== "foo"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc".charAt(822) !== "foo"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`foo\`, so this comparison is always true.␊
    `

## invalid(10): "abc".charAt() === "ab"

> Input

    `␊
      1 | "abc".charAt() === "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc".charAt() === "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(11): "abc".at() !== "ab"

> Input

    `␊
      1 | "abc".at() !== "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc".at() !== "ab"␊
        | ^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always true.␊
    `

## invalid(12): "abc".charAt(0) === "😀"

> Input

    `␊
      1 | "abc".charAt(0) === "😀"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc".charAt(0) === "😀"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`😀\`, so this comparison is always false.␊
    `

## invalid(13): "abc".charAt(0) === "\u{1F600}"

> Input

    `␊
      1 | "abc".charAt(0) === "\\u{1F600}"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc".charAt(0) === "\\u{1F600}"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`😀\`, so this comparison is always false.␊
    `

## invalid(14): "abc".charAt(0) === "\r\n"

> Input

    `␊
      1 | "abc".charAt(0) === "\\r\\n"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc".charAt(0) === "\\r\\n"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`␍␊
    \`, so this comparison is always false.␊
    `

## invalid(15): "abc".charAt(0) === `ab`

> Input

    `␊
      1 | "abc".charAt(0) === \`ab\`␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc".charAt(0) === \`ab\`␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(16): String(foo).charAt(0) === "ab"

> Input

    `␊
      1 | String(foo).charAt(0) === "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | String(foo).charAt(0) === "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(17): String.fromCharCode(65).charAt(0) === "ab"

> Input

    `␊
      1 | String.fromCharCode(65).charAt(0) === "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | String.fromCharCode(65).charAt(0) === "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(18): `${foo}`.charAt(0) === "ab"

> Input

    `␊
      1 | \`${foo}\`.charAt(0) === "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | \`${foo}\`.charAt(0) === "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(19): (foo + "bar").charAt(0) === "ab"

> Input

    `␊
      1 | (foo + "bar").charAt(0) === "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (foo + "bar").charAt(0) === "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(20): "abc".at(0) === "ab"

> Input

    `␊
      1 | "abc".at(0) === "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc".at(0) === "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(21): "abc".at(0) !== "ab"

> Input

    `␊
      1 | "abc".at(0) !== "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc".at(0) !== "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always true.␊
    `

## invalid(22): "abc".at(-1) === "ab"

> Input

    `␊
      1 | "abc".at(-1) === "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc".at(-1) === "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(23): String(foo).at(0) === "ab"

> Input

    `␊
      1 | String(foo).at(0) === "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | String(foo).at(0) === "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(24): String.fromCharCode(65).at(0) === "ab"

> Input

    `␊
      1 | String.fromCharCode(65).at(0) === "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | String.fromCharCode(65).at(0) === "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(25): `${foo}`.at(0) === "ab"

> Input

    `␊
      1 | \`${foo}\`.at(0) === "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | \`${foo}\`.at(0) === "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(26): (foo + "bar").at(0) === "ab"

> Input

    `␊
      1 | (foo + "bar").at(0) === "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (foo + "bar").at(0) === "ab"␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(27): "abc"[0] === "ab"

> Input

    `␊
      1 | "abc"[0] === "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc"[0] === "ab"␊
        | ^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(28): "abc"[0] != "ab"

> Input

    `␊
      1 | "abc"[0] != "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc"[0] != "ab"␊
        | ^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always true.␊
    `

## invalid(29): "abc"["0"] === "ab"

> Input

    `␊
      1 | "abc"["0"] === "ab"␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | "abc"["0"] === "ab"␊
        | ^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(30): declare const s: string; const x = s.at(0) === "ab";

> Input

    `␊
      1 | declare const s: string;␊
      2 | const x = s.at(0) === "ab";␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | declare const s: string;␊
    > 2 | const x = s.at(0) === "ab";␊
        |           ^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(31): declare const s: string; const x = s.charAt(0) === "ab";

> Input

    `␊
      1 | declare const s: string;␊
      2 | const x = s.charAt(0) === "ab";␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | declare const s: string;␊
    > 2 | const x = s.charAt(0) === "ab";␊
        |           ^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `

## invalid(32): function isAb(s: string) { return s["0"] === "ab"; }

> Input

    `␊
      1 | function isAb(s: string) {␊
      2 | 	return s["0"] === "ab";␊
      3 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | function isAb(s: string) {␊
    > 2 | 	return s["0"] === "ab";␊
        | 	       ^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
      3 | }␊
    `

## invalid(33): (foo as string)[0] === "ab";

> Input

    `␊
      1 | (foo as string)[0] === "ab";␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | (foo as string)[0] === "ab";␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^ A single character can never equal the multi-character string \`ab\`, so this comparison is always false.␊
    `
