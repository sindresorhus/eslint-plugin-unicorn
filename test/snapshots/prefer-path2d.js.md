# Snapshot report for `test/prefer-path2d.js`

The actual snapshot is saved in `prefer-path2d.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): function draw() { context.moveTo(220, 60); context.arc(170, 60, 50, 0, 2 * Math.PI); context.stroke(); } function step() { draw(); if (foo) { requestAnimationFrame(step); } } requestAnimationFrame(step);

> Input

    `␊
       1 | function draw() {␊
       2 | 	context.moveTo(220, 60);␊
       3 | 	context.arc(170, 60, 50, 0, 2 * Math.PI);␊
       4 | 	context.stroke();␊
       5 | }␊
       6 |␊
       7 | function step() {␊
       8 | 	draw();␊
       9 |␊
      10 | 	if (foo) {␊
      11 | 		requestAnimationFrame(step);␊
      12 | 	}␊
      13 | }␊
      14 |␊
      15 | requestAnimationFrame(step);␊
    `

> Error 1/1

    `␊
    Message:␊
       1 | function draw() {␊
    >  2 | 	context.moveTo(220, 60);␊
         | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
       3 | 	context.arc(170, 60, 50, 0, 2 * Math.PI);␊
       4 | 	context.stroke();␊
       5 | }␊
       6 |␊
       7 | function step() {␊
       8 | 	draw();␊
       9 |␊
      10 | 	if (foo) {␊
      11 | 		requestAnimationFrame(step);␊
      12 | 	}␊
      13 | }␊
      14 |␊
      15 | requestAnimationFrame(step);␊
    `

## invalid(2): requestAnimationFrame(() => { context.moveTo(220, 60); context.lineTo(170, 60); context.stroke(); });

> Input

    `␊
      1 | requestAnimationFrame(() => {␊
      2 | 	context.moveTo(220, 60);␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | });␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | requestAnimationFrame(() => {␊
    > 2 | 	context.moveTo(220, 60);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | });␊
    `

## invalid(3): requestAnimationFrame(function draw() { context.moveTo(220, 60); context.lineTo(170, 60); context.stroke(); });

> Input

    `␊
      1 | requestAnimationFrame(function draw() {␊
      2 | 	context.moveTo(220, 60);␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | });␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | requestAnimationFrame(function draw() {␊
    > 2 | 	context.moveTo(220, 60);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | });␊
    `

## invalid(4): window.requestAnimationFrame(() => { context.moveTo(220, 60); context.lineTo(170, 60); context.stroke(); });

> Input

    `␊
      1 | window.requestAnimationFrame(() => {␊
      2 | 	context.moveTo(220, 60);␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | });␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | window.requestAnimationFrame(() => {␊
    > 2 | 	context.moveTo(220, 60);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | });␊
    `

## invalid(5): setInterval(() => { context.moveTo(220, 60); context.lineTo(170, 60); context.stroke(); }, 1000);

> Input

    `␊
      1 | setInterval(() => {␊
      2 | 	context.moveTo(220, 60);␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | }, 1000);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | setInterval(() => {␊
    > 2 | 	context.moveTo(220, 60);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | }, 1000);␊
    `

## invalid(6): function draw() { context.moveTo(220, 60); context.lineTo(170, 60); context.stroke(); } setInterval(draw, 1000);

> Input

    `␊
      1 | function draw() {␊
      2 | 	context.moveTo(220, 60);␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | }␊
      6 |␊
      7 | setInterval(draw, 1000);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | function draw() {␊
    > 2 | 	context.moveTo(220, 60);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | }␊
      6 |␊
      7 | setInterval(draw, 1000);␊
    `

## invalid(7): const draw = () => { context.moveTo(220, 60); context.lineTo(170, 60); context.stroke(); }; setInterval(draw, 1000);

> Input

    `␊
      1 | const draw = () => {␊
      2 | 	context.moveTo(220, 60);␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | };␊
      6 |␊
      7 | setInterval(draw, 1000);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const draw = () => {␊
    > 2 | 	context.moveTo(220, 60);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | };␊
      6 |␊
      7 | setInterval(draw, 1000);␊
    `

## invalid(8): function draw() { context.moveTo(220, 60); context.lineTo(170, 60); context.stroke(); } globalThis.setInterval(draw, 1000);

> Input

    `␊
      1 | function draw() {␊
      2 | 	context.moveTo(220, 60);␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | }␊
      6 |␊
      7 | globalThis.setInterval(draw, 1000);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | function draw() {␊
    > 2 | 	context.moveTo(220, 60);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | }␊
      6 |␊
      7 | globalThis.setInterval(draw, 1000);␊
    `

## invalid(9): function draw() { context.moveTo(220, 60); context.lineTo(170, 60); context.stroke(); } self.setInterval(draw, 1000);

> Input

    `␊
      1 | function draw() {␊
      2 | 	context.moveTo(220, 60);␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | }␊
      6 |␊
      7 | self.setInterval(draw, 1000);␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | function draw() {␊
    > 2 | 	context.moveTo(220, 60);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | }␊
      6 |␊
      7 | self.setInterval(draw, 1000);␊
    `

## invalid(10): function draw() { context.moveTo(220, 60); context.lineTo(170, 60); context.stroke(); } for (const item of items) { draw(item); }

> Input

    `␊
      1 | function draw() {␊
      2 | 	context.moveTo(220, 60);␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | }␊
      6 |␊
      7 | for (const item of items) {␊
      8 | 	draw(item);␊
      9 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | function draw() {␊
    > 2 | 	context.moveTo(220, 60);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.lineTo(170, 60);␊
      4 | 	context.stroke();␊
      5 | }␊
      6 |␊
      7 | for (const item of items) {␊
      8 | 	draw(item);␊
      9 | }␊
    `

## invalid(11): const context = canvas.getContext('2d'); for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.stroke(); }

> Input

    `␊
      1 | const context = canvas.getContext('2d');␊
      2 |␊
      3 | for (const item of items) {␊
      4 | 	context.moveTo(item.x, item.y);␊
      5 | 	context.lineTo(item.x + 1, item.y + 1);␊
      6 | 	context.stroke();␊
      7 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const context = canvas.getContext('2d');␊
      2 |␊
      3 | for (const item of items) {␊
    > 4 | 	context.moveTo(item.x, item.y);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      5 | 	context.lineTo(item.x + 1, item.y + 1);␊
      6 | 	context.stroke();␊
      7 | }␊
    `

## invalid(12): let renderingContext = canvas.getContext('2d'); for (const item of items) { renderingContext.moveTo(item.x, item.y); renderingContext.lineTo(item.x + 1, item.y + 1); renderingContext.stroke(); }

> Input

    `␊
      1 | let renderingContext = canvas.getContext('2d');␊
      2 |␊
      3 | for (const item of items) {␊
      4 | 	renderingContext.moveTo(item.x, item.y);␊
      5 | 	renderingContext.lineTo(item.x + 1, item.y + 1);␊
      6 | 	renderingContext.stroke();␊
      7 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | let renderingContext = canvas.getContext('2d');␊
      2 |␊
      3 | for (const item of items) {␊
    > 4 | 	renderingContext.moveTo(item.x, item.y);␊
        | 	                 ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      5 | 	renderingContext.lineTo(item.x + 1, item.y + 1);␊
      6 | 	renderingContext.stroke();␊
      7 | }␊
    `

## invalid(13): while (enabled) { ctx.beginPath(); ctx.moveTo(220, 60); ctx.arc(170, 60, 50, 0, 2 * Math.PI); ctx.fill('evenodd'); }

> Input

    `␊
      1 | while (enabled) {␊
      2 | 	ctx.beginPath();␊
      3 | 	ctx.moveTo(220, 60);␊
      4 | 	ctx.arc(170, 60, 50, 0, 2 * Math.PI);␊
      5 | 	ctx.fill('evenodd');␊
      6 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | while (enabled) {␊
      2 | 	ctx.beginPath();␊
    > 3 | 	ctx.moveTo(220, 60);␊
        | 	    ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      4 | 	ctx.arc(170, 60, 50, 0, 2 * Math.PI);␊
      5 | 	ctx.fill('evenodd');␊
      6 | }␊
    `

## invalid(14): context.moveTo(220, 60); for (const item of items) { context.lineTo(item.x, item.y); context.arc(item.x, item.y, 50, 0, 2 * Math.PI); context.stroke(); }

> Input

    `␊
      1 | context.moveTo(220, 60);␊
      2 |␊
      3 | for (const item of items) {␊
      4 | 	context.lineTo(item.x, item.y);␊
      5 | 	context.arc(item.x, item.y, 50, 0, 2 * Math.PI);␊
      6 | 	context.stroke();␊
      7 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | context.moveTo(220, 60);␊
      2 |␊
      3 | for (const item of items) {␊
    > 4 | 	context.lineTo(item.x, item.y);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      5 | 	context.arc(item.x, item.y, 50, 0, 2 * Math.PI);␊
      6 | 	context.stroke();␊
      7 | }␊
    `

## invalid(15): for (const item of items) { context.rect(item.x, item.y, item.width, item.height); context.closePath(); context.stroke(); }

> Input

    `␊
      1 | for (const item of items) {␊
      2 | 	context.rect(item.x, item.y, item.width, item.height);␊
      3 | 	context.closePath();␊
      4 | 	context.stroke();␊
      5 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | for (const item of items) {␊
    > 2 | 	context.rect(item.x, item.y, item.width, item.height);␊
        | 	        ^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.closePath();␊
      4 | 	context.stroke();␊
      5 | }␊
    `

## invalid(16): for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.stroke(); context.beginPath(); context.moveTo(item.x + 2, item.y + 2); context.stroke(); }

> Input

    `␊
      1 | for (const item of items) {␊
      2 | 	context.moveTo(item.x, item.y);␊
      3 | 	context.lineTo(item.x + 1, item.y + 1);␊
      4 | 	context.stroke();␊
      5 | 	context.beginPath();␊
      6 | 	context.moveTo(item.x + 2, item.y + 2);␊
      7 | 	context.stroke();␊
      8 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | for (const item of items) {␊
    > 2 | 	context.moveTo(item.x, item.y);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.lineTo(item.x + 1, item.y + 1);␊
      4 | 	context.stroke();␊
      5 | 	context.beginPath();␊
      6 | 	context.moveTo(item.x + 2, item.y + 2);␊
      7 | 	context.stroke();␊
      8 | }␊
    `

## invalid(17): for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.clip(); }

> Input

    `␊
      1 | for (const item of items) {␊
      2 | 	context.moveTo(item.x, item.y);␊
      3 | 	context.lineTo(item.x + 1, item.y + 1);␊
      4 | 	context.clip();␊
      5 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | for (const item of items) {␊
    > 2 | 	context.moveTo(item.x, item.y);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.lineTo(item.x + 1, item.y + 1);␊
      4 | 	context.clip();␊
      5 | }␊
    `

## invalid(18): for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.clip('evenodd'); }

> Input

    `␊
      1 | for (const item of items) {␊
      2 | 	context.moveTo(item.x, item.y);␊
      3 | 	context.lineTo(item.x + 1, item.y + 1);␊
      4 | 	context.clip('evenodd');␊
      5 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | for (const item of items) {␊
    > 2 | 	context.moveTo(item.x, item.y);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.lineTo(item.x + 1, item.y + 1);␊
      4 | 	context.clip('evenodd');␊
      5 | }␊
    `

## invalid(19): for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.isPointInPath(item.x, item.y); }

> Input

    `␊
      1 | for (const item of items) {␊
      2 | 	context.moveTo(item.x, item.y);␊
      3 | 	context.lineTo(item.x + 1, item.y + 1);␊
      4 | 	context.isPointInPath(item.x, item.y);␊
      5 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | for (const item of items) {␊
    > 2 | 	context.moveTo(item.x, item.y);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.lineTo(item.x + 1, item.y + 1);␊
      4 | 	context.isPointInPath(item.x, item.y);␊
      5 | }␊
    `

## invalid(20): for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.isPointInPath(item.x, item.y, 'evenodd'); }

> Input

    `␊
      1 | for (const item of items) {␊
      2 | 	context.moveTo(item.x, item.y);␊
      3 | 	context.lineTo(item.x + 1, item.y + 1);␊
      4 | 	context.isPointInPath(item.x, item.y, 'evenodd');␊
      5 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | for (const item of items) {␊
    > 2 | 	context.moveTo(item.x, item.y);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.lineTo(item.x + 1, item.y + 1);␊
      4 | 	context.isPointInPath(item.x, item.y, 'evenodd');␊
      5 | }␊
    `

## invalid(21): for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.isPointInStroke(item.x, item.y); }

> Input

    `␊
      1 | for (const item of items) {␊
      2 | 	context.moveTo(item.x, item.y);␊
      3 | 	context.lineTo(item.x + 1, item.y + 1);␊
      4 | 	context.isPointInStroke(item.x, item.y);␊
      5 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | for (const item of items) {␊
    > 2 | 	context.moveTo(item.x, item.y);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.lineTo(item.x + 1, item.y + 1);␊
      4 | 	context.isPointInStroke(item.x, item.y);␊
      5 | }␊
    `

## invalid(22): for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.stroke(); ctx.moveTo(item.x, item.y); ctx.lineTo(item.x + 1, item.y + 1); ctx.stroke(); }

> Input

    `␊
      1 | for (const item of items) {␊
      2 | 	context.moveTo(item.x, item.y);␊
      3 | 	context.lineTo(item.x + 1, item.y + 1);␊
      4 | 	context.stroke();␊
      5 |␊
      6 | 	ctx.moveTo(item.x, item.y);␊
      7 | 	ctx.lineTo(item.x + 1, item.y + 1);␊
      8 | 	ctx.stroke();␊
      9 | }␊
    `

> Error 1/2

    `␊
    Message:␊
      1 | for (const item of items) {␊
    > 2 | 	context.moveTo(item.x, item.y);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      3 | 	context.lineTo(item.x + 1, item.y + 1);␊
      4 | 	context.stroke();␊
      5 |␊
      6 | 	ctx.moveTo(item.x, item.y);␊
      7 | 	ctx.lineTo(item.x + 1, item.y + 1);␊
      8 | 	ctx.stroke();␊
      9 | }␊
    `

> Error 2/2

    `␊
    Message:␊
      1 | for (const item of items) {␊
      2 | 	context.moveTo(item.x, item.y);␊
      3 | 	context.lineTo(item.x + 1, item.y + 1);␊
      4 | 	context.stroke();␊
      5 |␊
    > 6 | 	ctx.moveTo(item.x, item.y);␊
        | 	    ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      7 | 	ctx.lineTo(item.x + 1, item.y + 1);␊
      8 | 	ctx.stroke();␊
      9 | }␊
    `

## invalid(23): function draw(context: CanvasRenderingContext2D) { for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.stroke(); } }

> Input

    `␊
      1 | function draw(context: CanvasRenderingContext2D) {␊
      2 | 	for (const item of items) {␊
      3 | 		context.moveTo(item.x, item.y);␊
      4 | 		context.lineTo(item.x + 1, item.y + 1);␊
      5 | 		context.stroke();␊
      6 | 	}␊
      7 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | function draw(context: CanvasRenderingContext2D) {␊
      2 | 	for (const item of items) {␊
    > 3 | 		context.moveTo(item.x, item.y);␊
        | 		        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      4 | 		context.lineTo(item.x + 1, item.y + 1);␊
      5 | 		context.stroke();␊
      6 | 	}␊
      7 | }␊
    `

## invalid(24): function draw<Context extends CanvasRenderingContext2D>(context: Context) { for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.stroke(); } }

> Input

    `␊
      1 | function draw<Context extends CanvasRenderingContext2D>(context: Context) {␊
      2 | 	for (const item of items) {␊
      3 | 		context.moveTo(item.x, item.y);␊
      4 | 		context.lineTo(item.x + 1, item.y + 1);␊
      5 | 		context.stroke();␊
      6 | 	}␊
      7 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | function draw<Context extends CanvasRenderingContext2D>(context: Context) {␊
      2 | 	for (const item of items) {␊
    > 3 | 		context.moveTo(item.x, item.y);␊
        | 		        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      4 | 		context.lineTo(item.x + 1, item.y + 1);␊
      5 | 		context.stroke();␊
      6 | 	}␊
      7 | }␊
    `

## invalid(25): function draw(context: CanvasRenderingContext2D) { for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.stroke(); } }

> Input 'file.ts'

    `␊
      1 | function draw(context: CanvasRenderingContext2D) {␊
      2 | 	for (const item of items) {␊
      3 | 		context.moveTo(item.x, item.y);␊
      4 | 		context.lineTo(item.x + 1, item.y + 1);␊
      5 | 		context.stroke();␊
      6 | 	}␊
      7 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | function draw(context: CanvasRenderingContext2D) {␊
      2 | 	for (const item of items) {␊
    > 3 | 		context.moveTo(item.x, item.y);␊
        | 		        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      4 | 		context.lineTo(item.x + 1, item.y + 1);␊
      5 | 		context.stroke();␊
      6 | 	}␊
      7 | }␊
    `

## invalid(26): function draw(context: CanvasRenderingContext2D | undefined) { if (!context) { return; } for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.stroke(); } }

> Input 'file.ts'

    `␊
       1 | function draw(context: CanvasRenderingContext2D | undefined) {␊
       2 | 	if (!context) {␊
       3 | 		return;␊
       4 | 	}␊
       5 |␊
       6 | 	for (const item of items) {␊
       7 | 		context.moveTo(item.x, item.y);␊
       8 | 		context.lineTo(item.x + 1, item.y + 1);␊
       9 | 		context.stroke();␊
      10 | 	}␊
      11 | }␊
    `

> Error 1/1

    `␊
    Message:␊
       1 | function draw(context: CanvasRenderingContext2D | undefined) {␊
       2 | 	if (!context) {␊
       3 | 		return;␊
       4 | 	}␊
       5 |␊
       6 | 	for (const item of items) {␊
    >  7 | 		context.moveTo(item.x, item.y);␊
         | 		        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
       8 | 		context.lineTo(item.x + 1, item.y + 1);␊
       9 | 		context.stroke();␊
      10 | 	}␊
      11 | }␊
    `

## invalid(27): function draw(context: CanvasRenderingContext2D | string) { if (typeof context === 'string') { return; } for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.stroke(); } }

> Input 'file.ts'

    `␊
       1 | function draw(context: CanvasRenderingContext2D | string) {␊
       2 | 	if (typeof context === 'string') {␊
       3 | 		return;␊
       4 | 	}␊
       5 |␊
       6 | 	for (const item of items) {␊
       7 | 		context.moveTo(item.x, item.y);␊
       8 | 		context.lineTo(item.x + 1, item.y + 1);␊
       9 | 		context.stroke();␊
      10 | 	}␊
      11 | }␊
    `

> Error 1/1

    `␊
    Message:␊
       1 | function draw(context: CanvasRenderingContext2D | string) {␊
       2 | 	if (typeof context === 'string') {␊
       3 | 		return;␊
       4 | 	}␊
       5 |␊
       6 | 	for (const item of items) {␊
    >  7 | 		context.moveTo(item.x, item.y);␊
         | 		        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
       8 | 		context.lineTo(item.x + 1, item.y + 1);␊
       9 | 		context.stroke();␊
      10 | 	}␊
      11 | }␊
    `

## invalid(28): function draw(context: CanvasRenderingContext2D & {scaleFactor: number}) { for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.stroke(); } }

> Input 'file.ts'

    `␊
      1 | function draw(context: CanvasRenderingContext2D & {scaleFactor: number}) {␊
      2 | 	for (const item of items) {␊
      3 | 		context.moveTo(item.x, item.y);␊
      4 | 		context.lineTo(item.x + 1, item.y + 1);␊
      5 | 		context.stroke();␊
      6 | 	}␊
      7 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | function draw(context: CanvasRenderingContext2D & {scaleFactor: number}) {␊
      2 | 	for (const item of items) {␊
    > 3 | 		context.moveTo(item.x, item.y);␊
        | 		        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      4 | 		context.lineTo(item.x + 1, item.y + 1);␊
      5 | 		context.stroke();␊
      6 | 	}␊
      7 | }␊
    `

## invalid(29): interface Canvas2DContext extends CanvasRenderingContext2D {} function draw(renderingContext: Canvas2DContext) { for (const item of items) { renderingContext.moveTo(item.x, item.y); renderingContext.lineTo(item.x + 1, item.y + 1); renderingContext.stroke(); } }

> Input 'file.ts'

    `␊
      1 | interface Canvas2DContext extends CanvasRenderingContext2D {}␊
      2 |␊
      3 | function draw(renderingContext: Canvas2DContext) {␊
      4 | 	for (const item of items) {␊
      5 | 		renderingContext.moveTo(item.x, item.y);␊
      6 | 		renderingContext.lineTo(item.x + 1, item.y + 1);␊
      7 | 		renderingContext.stroke();␊
      8 | 	}␊
      9 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | interface Canvas2DContext extends CanvasRenderingContext2D {}␊
      2 |␊
      3 | function draw(renderingContext: Canvas2DContext) {␊
      4 | 	for (const item of items) {␊
    > 5 | 		renderingContext.moveTo(item.x, item.y);␊
        | 		                 ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      6 | 		renderingContext.lineTo(item.x + 1, item.y + 1);␊
      7 | 		renderingContext.stroke();␊
      8 | 	}␊
      9 | }␊
    `

## invalid(30): function draw(context: OffscreenCanvasRenderingContext2D) { for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.stroke(); } }

> Input 'file.ts'

    `␊
      1 | function draw(context: OffscreenCanvasRenderingContext2D) {␊
      2 | 	for (const item of items) {␊
      3 | 		context.moveTo(item.x, item.y);␊
      4 | 		context.lineTo(item.x + 1, item.y + 1);␊
      5 | 		context.stroke();␊
      6 | 	}␊
      7 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | function draw(context: OffscreenCanvasRenderingContext2D) {␊
      2 | 	for (const item of items) {␊
    > 3 | 		context.moveTo(item.x, item.y);␊
        | 		        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      4 | 		context.lineTo(item.x + 1, item.y + 1);␊
      5 | 		context.stroke();␊
      6 | 	}␊
      7 | }␊
    `

## invalid(31): const canvas = document.createElement('canvas'); const context = canvas.getContext('2d')!; for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.stroke(); }

> Input 'file.ts'

    `␊
      1 | const canvas = document.createElement('canvas');␊
      2 | const context = canvas.getContext('2d')!;␊
      3 |␊
      4 | for (const item of items) {␊
      5 | 	context.moveTo(item.x, item.y);␊
      6 | 	context.lineTo(item.x + 1, item.y + 1);␊
      7 | 	context.stroke();␊
      8 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const canvas = document.createElement('canvas');␊
      2 | const context = canvas.getContext('2d')!;␊
      3 |␊
      4 | for (const item of items) {␊
    > 5 | 	context.moveTo(item.x, item.y);␊
        | 	        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      6 | 	context.lineTo(item.x + 1, item.y + 1);␊
      7 | 	context.stroke();␊
      8 | }␊
    `

## invalid(32): function draw<Context extends CanvasRenderingContext2D>(context: Context) { for (const item of items) { context.moveTo(item.x, item.y); context.lineTo(item.x + 1, item.y + 1); context.stroke(); } }

> Input 'file.ts'

    `␊
      1 | function draw<Context extends CanvasRenderingContext2D>(context: Context) {␊
      2 | 	for (const item of items) {␊
      3 | 		context.moveTo(item.x, item.y);␊
      4 | 		context.lineTo(item.x + 1, item.y + 1);␊
      5 | 		context.stroke();␊
      6 | 	}␊
      7 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | function draw<Context extends CanvasRenderingContext2D>(context: Context) {␊
      2 | 	for (const item of items) {␊
    > 3 | 		context.moveTo(item.x, item.y);␊
        | 		        ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      4 | 		context.lineTo(item.x + 1, item.y + 1);␊
      5 | 		context.stroke();␊
      6 | 	}␊
      7 | }␊
    `

## invalid(33): function draw(renderingContext: unknown) { for (const item of items) { (renderingContext satisfies CanvasRenderingContext2D).moveTo(item.x, item.y); (renderingContext satisfies CanvasRenderingContext2D).lineTo(item.x + 1, item.y + 1); (renderingContext satisfies CanvasRenderingContext2D).stroke(); } }

> Input

    `␊
      1 | function draw(renderingContext: unknown) {␊
      2 | 	for (const item of items) {␊
      3 | 		(renderingContext satisfies CanvasRenderingContext2D).moveTo(item.x, item.y);␊
      4 | 		(renderingContext satisfies CanvasRenderingContext2D).lineTo(item.x + 1, item.y + 1);␊
      5 | 		(renderingContext satisfies CanvasRenderingContext2D).stroke();␊
      6 | 	}␊
      7 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | function draw(renderingContext: unknown) {␊
      2 | 	for (const item of items) {␊
    > 3 | 		(renderingContext satisfies CanvasRenderingContext2D).moveTo(item.x, item.y);␊
        | 		                                                      ^^^^^^ Prefer reusing a \`Path2D\` object instead of rebuilding the canvas path in repeated draws.␊
      4 | 		(renderingContext satisfies CanvasRenderingContext2D).lineTo(item.x + 1, item.y + 1);␊
      5 | 		(renderingContext satisfies CanvasRenderingContext2D).stroke();␊
      6 | 	}␊
      7 | }␊
    `
