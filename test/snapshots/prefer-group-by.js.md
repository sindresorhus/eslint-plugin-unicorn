# Snapshot report for `test/prefer-group-by.js`

The actual snapshot is saved in `prefer-group-by.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): items.reduce((groups, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});

> Input

    `␊
      1 | items.reduce((groups, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Object.groupBy(items, item => item.type);␊
    `

## invalid(2): items.reduce((groups, item) => {groups[item.type] = groups[item.type] || []; groups[item.type].push(item); return groups;}, {});

> Input

    `␊
      1 | items.reduce((groups, item) => {groups[item.type] = groups[item.type] || []; groups[item.type].push(item); return groups;}, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {groups[item.type] = groups[item.type] || []; groups[item.type].push(item); return groups;}, {});␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Object.groupBy(items, item => item.type);␊
    `

## invalid(3): items.reduce((groups, item) => {groups[item.type] = groups[item.type] ?? []; groups[item.type].push(item); return groups;}, {});

> Input

    `␊
      1 | items.reduce((groups, item) => {groups[item.type] = groups[item.type] ?? []; groups[item.type].push(item); return groups;}, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {groups[item.type] = groups[item.type] ?? []; groups[item.type].push(item); return groups;}, {});␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Object.groupBy(items, item => item.type);␊
    `

## invalid(4): items.reduce((groups, item) => {groups[item.type] ||= []; groups[item.type].push(item); return groups;}, {});

> Input

    `␊
      1 | items.reduce((groups, item) => {groups[item.type] ||= []; groups[item.type].push(item); return groups;}, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {groups[item.type] ||= []; groups[item.type].push(item); return groups;}, {});␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Object.groupBy(items, item => item.type);␊
    `

## invalid(5): items.reduce((groups, item) => {(groups[item.type] ??= []).push(item); return groups;}, {});

> Input

    `␊
      1 | items.reduce((groups, item) => {(groups[item.type] ??= []).push(item); return groups;}, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {(groups[item.type] ??= []).push(item); return groups;}, {});␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Object.groupBy(items, item => item.type);␊
    `

## invalid(6): items.reduce((groups, item) => {(groups[item.type] ||= []).push(item); return groups;}, {});

> Input

    `␊
      1 | items.reduce((groups, item) => {(groups[item.type] ||= []).push(item); return groups;}, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {(groups[item.type] ||= []).push(item); return groups;}, {});␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Object.groupBy(items, item => item.type);␊
    `

## invalid(7): items.reduce((groups, item) => {(groups[item.type] = groups[item.type] || []).push(item); return groups;}, {});

> Input

    `␊
      1 | items.reduce((groups, item) => {(groups[item.type] = groups[item.type] || []).push(item); return groups;}, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {(groups[item.type] = groups[item.type] || []).push(item); return groups;}, {});␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Object.groupBy(items, item => item.type);␊
    `

## invalid(8): items.reduce(function (groups, item) {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});

> Input

    `␊
      1 | items.reduce(function (groups, item) {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce(function (groups, item) {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Object.groupBy(items, item => item.type);␊
    `

## invalid(9): items.reduce(function (groups, item) {(groups[arguments[1].type] ??= []).push(item); return groups;}, {});

> Input

    `␊
      1 | items.reduce(function (groups, item) {(groups[arguments[1].type] ??= []).push(item); return groups;}, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce(function (groups, item) {(groups[arguments[1].type] ??= []).push(item); return groups;}, {});␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    `

## invalid(10): items.reduce(function (groups, item) {const key = this.getKey(item); groups[key] ??= []; groups[key].push(item); return groups;}, {});

> Input

    `␊
      1 | items.reduce(function (groups, item) {const key = this.getKey(item); groups[key] ??= []; groups[key].push(item); return groups;}, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce(function (groups, item) {const key = this.getKey(item); groups[key] ??= []; groups[key].push(item); return groups;}, {});␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    `

## invalid(11): items.reduce((groups, item) => {const key = getKey(item); groups[key] ??= []; groups[key].push(item); return groups;}, {});

> Input

    `␊
      1 | items.reduce((groups, item) => {const key = getKey(item); groups[key] ??= []; groups[key].push(item); return groups;}, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {const key = getKey(item); groups[key] ??= []; groups[key].push(item); return groups;}, {});␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Object.groupBy(items, item => getKey(item));␊
    `

## invalid(12): items.reduce((groups, item) => {groups[item.groups] ??= []; groups[item.groups].push(item); return groups;}, {});

> Input

    `␊
      1 | items.reduce((groups, item) => {groups[item.groups] ??= []; groups[item.groups].push(item); return groups;}, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {groups[item.groups] ??= []; groups[item.groups].push(item); return groups;}, {});␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Object.groupBy(items, item => item.groups);␊
    `

## invalid(13): items.reduce((groups, item, index) => {groups[item.index] ??= []; groups[item.index].push(item); return groups;}, {});

> Input

    `␊
      1 | items.reduce((groups, item, index) => {groups[item.index] ??= []; groups[item.index].push(item); return groups;}, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item, index) => {groups[item.index] ??= []; groups[item.index].push(item); return groups;}, {});␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Object.groupBy(items, item => item.index);␊
    `

## invalid(14): items.reduce((groups, item, index, array) => {groups[item.array] ??= []; groups[item.array].push(item); return groups;}, {});

> Input

    `␊
      1 | items.reduce((groups, item, index, array) => {groups[item.array] ??= []; groups[item.array].push(item); return groups;}, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item, index, array) => {groups[item.array] ??= []; groups[item.array].push(item); return groups;}, {});␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Object.groupBy(items, item => item.array);␊
    `

## invalid(15): items.reduce((groups, item) => {groups[item[type]] ??= []; groups[item[type]].push(item); return groups;}, {});

> Input

    `␊
      1 | items.reduce((groups, item) => {groups[item[type]] ??= []; groups[item[type]].push(item); return groups;}, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {groups[item[type]] ??= []; groups[item[type]].push(item); return groups;}, {});␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Object.groupBy(items, item => item[type]);␊
    `

## invalid(16): foo.items.reduce((groups, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, Object.create(null));

> Input

    `␊
      1 | foo.items.reduce((groups, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, Object.create(null));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | foo.items.reduce((groups, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, Object.create(null));␊
        |           ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Object.groupBy(foo.items, item => item.type);␊
    `

## invalid(17): items.reduce((groups, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, Object.create(null));

> Input

    `␊
      1 | items.reduce((groups, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, Object.create(null));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, Object.create(null));␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Object.groupBy(items, item => item.type);␊
    `

## invalid(18): items.reduce((groups, item) => { // Keep this comment. groups[item.type] ??= []; groups[item.type].push(item); return groups; }, {});

> Input

    `␊
      1 | items.reduce((groups, item) => {␊
      2 | 	// Keep this comment.␊
      3 | 	groups[item.type] ??= [];␊
      4 | 	groups[item.type].push(item);␊
      5 | 	return groups;␊
      6 | }, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
      2 | 	// Keep this comment.␊
      3 | 	groups[item.type] ??= [];␊
      4 | 	groups[item.type].push(item);␊
      5 | 	return groups;␊
      6 | }, {});␊
    `

## invalid(19): items.reduce((groups, item) => {if (groups.has(item.type)) {groups.get(item.type).push(item);} else {groups.set(item.type, [item]);} return groups;}, new Map());

> Input

    `␊
      1 | items.reduce((groups, item) => {if (groups.has(item.type)) {groups.get(item.type).push(item);} else {groups.set(item.type, [item]);} return groups;}, new Map());␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {if (groups.has(item.type)) {groups.get(item.type).push(item);} else {groups.set(item.type, [item]);} return groups;}, new Map());␊
        |       ^^^^^^ Prefer \`Map.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Map.groupBy(items, item => item.type);␊
    `

## invalid(20): items.reduce((groups, item) => {const key = getKey(item); if (groups.has(key)) {groups.get(key).push(item);} else {groups.set(key, [item]);} return groups;}, new Map());

> Input

    `␊
      1 | items.reduce((groups, item) => {const key = getKey(item); if (groups.has(key)) {groups.get(key).push(item);} else {groups.set(key, [item]);} return groups;}, new Map());␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {const key = getKey(item); if (groups.has(key)) {groups.get(key).push(item);} else {groups.set(key, [item]);} return groups;}, new Map());␊
        |       ^^^^^^ Prefer \`Map.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Map.groupBy(items, item => getKey(item));␊
    `

## invalid(21): items.reduce((groups, item) => {const group = groups.get(item.type) ?? []; group.push(item); groups.set(item.type, group); return groups;}, new Map());

> Input

    `␊
      1 | items.reduce((groups, item) => {const group = groups.get(item.type) ?? []; group.push(item); groups.set(item.type, group); return groups;}, new Map());␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {const group = groups.get(item.type) ?? []; group.push(item); groups.set(item.type, group); return groups;}, new Map());␊
        |       ^^^^^^ Prefer \`Map.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Map.groupBy(items, item => item.type);␊
    `

## invalid(22): items.reduce((groups, item) => {const group = groups.get(item.type) || []; group.push(item); groups.set(item.type, group); return groups;}, new Map());

> Input

    `␊
      1 | items.reduce((groups, item) => {const group = groups.get(item.type) || []; group.push(item); groups.set(item.type, group); return groups;}, new Map());␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {const group = groups.get(item.type) || []; group.push(item); groups.set(item.type, group); return groups;}, new Map());␊
        |       ^^^^^^ Prefer \`Map.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Map.groupBy(items, item => item.type);␊
    `

## invalid(23): function foo(items: Item[]) {items.reduce((groups, item) => {groups[(item.type as string)] ??= []; groups[(item.type as string)].push(item); return groups;}, {});}

> Input

    `␊
      1 | function foo(items: Item[]) {items.reduce((groups, item) => {groups[(item.type as string)] ??= []; groups[(item.type as string)].push(item); return groups;}, {});}␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(items: Item[]) {items.reduce((groups, item) => {groups[(item.type as string)] ??= []; groups[(item.type as string)].push(item); return groups;}, {});}␊
        |                                    ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | function foo(items: Item[]) {Object.groupBy(items, item => (item.type as string));}␊
    `

## invalid(24): function foo(items: Item[]) {items.reduce((groups, item) => {groups[(item.type satisfies string)] ??= []; groups[(item.type satisfies string)].push(item); return groups;}, {});}

> Input

    `␊
      1 | function foo(items: Item[]) {items.reduce((groups, item) => {groups[(item.type satisfies string)] ??= []; groups[(item.type satisfies string)].push(item); return groups;}, {});}␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(items: Item[]) {items.reduce((groups, item) => {groups[(item.type satisfies string)] ??= []; groups[(item.type satisfies string)].push(item); return groups;}, {});}␊
        |                                    ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | function foo(items: Item[]) {Object.groupBy(items, item => (item.type satisfies string));}␊
    `

## invalid(25): function foo(items: Item[]) {items.reduce((groups, item) => {groups[item!.type] ??= []; groups[item!.type].push(item); return groups;}, {});}

> Input

    `␊
      1 | function foo(items: Item[]) {items.reduce((groups, item) => {groups[item!.type] ??= []; groups[item!.type].push(item); return groups;}, {});}␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(items: Item[]) {items.reduce((groups, item) => {groups[item!.type] ??= []; groups[item!.type].push(item); return groups;}, {});}␊
        |                                    ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | function foo(items: Item[]) {Object.groupBy(items, item => item!.type);}␊
    `

## invalid(26): function foo(items: Item[]) {items.reduce((groups, item: Item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});}

> Input

    `␊
      1 | function foo(items: Item[]) {items.reduce((groups, item: Item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});}␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(items: Item[]) {items.reduce((groups, item: Item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});}␊
        |                                    ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | function foo(items: Item[]) {Object.groupBy(items, (item: Item) => item.type);}␊
    `

## invalid(27): function foo(items: Item[]) {items.reduce((groups: Record<string, Item[]>, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});}

> Input

    `␊
      1 | function foo(items: Item[]) {items.reduce((groups: Record<string, Item[]>, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});}␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(items: Item[]) {items.reduce((groups: Record<string, Item[]>, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});}␊
        |                                    ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    `

## invalid(28): function foo(items: Item[]) {items.reduce((groups, item): Record<string, Item[]> => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});}

> Input

    `␊
      1 | function foo(items: Item[]) {items.reduce((groups, item): Record<string, Item[]> => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});}␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(items: Item[]) {items.reduce((groups, item): Record<string, Item[]> => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});}␊
        |                                    ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    `

## invalid(29): function foo(items: Item[]) {items.reduce<Record<string, Item[]>>((groups, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});}

> Input

    `␊
      1 | function foo(items: Item[]) {items.reduce<Record<string, Item[]>>((groups, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});}␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(items: Item[]) {items.reduce<Record<string, Item[]>>((groups, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});}␊
        |                                    ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    `

## invalid(30): items.reduce((groups, item) => {const key = {}; groups[key] ??= []; groups[key].push(item); return groups;}, {});

> Input

    `␊
      1 | items.reduce((groups, item) => {const key = {}; groups[key] ??= []; groups[key].push(item); return groups;}, {});␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {const key = {}; groups[key] ??= []; groups[key].push(item); return groups;}, {});␊
        |       ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Object.groupBy(items, item => ({}));␊
    `

## invalid(31): items.reduce((groups, item) => {const key = {}; const group = groups.get(key) ?? []; group.push(item); groups.set(key, group); return groups;}, new Map());

> Input

    `␊
      1 | items.reduce((groups, item) => {const key = {}; const group = groups.get(key) ?? []; group.push(item); groups.set(key, group); return groups;}, new Map());␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | items.reduce((groups, item) => {const key = {}; const group = groups.get(key) ?? []; group.push(item); groups.set(key, group); return groups;}, new Map());␊
        |       ^^^^^^ Prefer \`Map.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | Map.groupBy(items, item => ({}));␊
    `

## invalid(32): function foo(items: Item[]) {items.reduce((groups, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});}

> Input 'file.ts'

    `␊
      1 | function foo(items: Item[]) {items.reduce((groups, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});}␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function foo(items: Item[]) {items.reduce((groups, item) => {groups[item.type] ??= []; groups[item.type].push(item); return groups;}, {});}␊
        |                                    ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | function foo(items: Item[]) {Object.groupBy(items, item => item.type);}␊
    `

## invalid(33): function f(items: Int8Array) { items.reduce((groups, item) => {groups[item] ??= []; groups[item].push(item); return groups;}, {}); }

> Input

    `␊
      1 | function f(items: Int8Array) { items.reduce((groups, item) => {groups[item] ??= []; groups[item].push(item); return groups;}, {}); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function f(items: Int8Array) { items.reduce((groups, item) => {groups[item] ??= []; groups[item].push(item); return groups;}, {}); }␊
        |                                      ^^^^^^ Prefer \`Object.groupBy()\` over \`Array#reduce()\`.␊
    ␊
    Output:␊
      1 | function f(items: Int8Array) { Object.groupBy(items, item => item); }␊
    `
