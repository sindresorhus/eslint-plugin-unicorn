# Snapshot report for `test/prefer-regexp-escape.js`

The actual snapshot is saved in `prefer-regexp-escape.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): const escaped = string.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

> Input

    `␊
      1 | const escaped = string.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = string.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Output:␊
      1 | const escaped = RegExp.escape(string);␊
    `

## invalid(2): const escaped = string.replace(/[.*+?^${}()|[\]\\]/gi, '\\$&');

> Input

    `␊
      1 | const escaped = string.replace(/[.*+?^${}()|[\\]\\\\]/gi, '\\\\$&');␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = string.replace(/[.*+?^${}()|[\\]\\\\]/gi, '\\\\$&');␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Output:␊
      1 | const escaped = RegExp.escape(string);␊
    `

## invalid(3): const escaped = string.replace(/[.*+?^${}()|[\]\\/]/g, '\\$&');

> Input

    `␊
      1 | const escaped = string.replace(/[.*+?^${}()|[\\]\\\\/]/g, '\\\\$&');␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = string.replace(/[.*+?^${}()|[\\]\\\\/]/g, '\\\\$&');␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Output:␊
      1 | const escaped = RegExp.escape(string);␊
    `

## invalid(4): const escaped = string.replace(/[\]\\.*+?^${}()|[]/g, '\\$&');

> Input

    `␊
      1 | const escaped = string.replace(/[\\]\\\\.*+?^${}()|[]/g, '\\\\$&');␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = string.replace(/[\\]\\\\.*+?^${}()|[]/g, '\\\\$&');␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Output:␊
      1 | const escaped = RegExp.escape(string);␊
    `

## invalid(5): const escaped = string.replace(/[.*+?^$()]/g, '\\$&');

> Input

    `␊
      1 | const escaped = string.replace(/[.*+?^$()]/g, '\\\\$&');␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = string.replace(/[.*+?^$()]/g, '\\\\$&');␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Output:␊
      1 | const escaped = RegExp.escape(string);␊
    `

## invalid(6): const escaped = string.replace(/[\\^$.*+?()[\]{}|]/g, '\\$&');

> Input

    `␊
      1 | const escaped = string.replace(/[\\\\^$.*+?()[\\]{}|]/g, '\\\\$&');␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = string.replace(/[\\\\^$.*+?()[\\]{}|]/g, '\\\\$&');␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Output:␊
      1 | const escaped = RegExp.escape(string);␊
    `

## invalid(7): const escaped = string.replace(/[|\\{}()[\]^$+*?.]/g, '\\$&');

> Input

    `␊
      1 | const escaped = string.replace(/[|\\\\{}()[\\]^$+*?.]/g, '\\\\$&');␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = string.replace(/[|\\\\{}()[\\]^$+*?.]/g, '\\\\$&');␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Output:␊
      1 | const escaped = RegExp.escape(string);␊
    `

## invalid(8): const escaped = string.replace(/[-\/\\^$*+?.()|[\]{}]/g, '\\$&');

> Input

    `␊
      1 | const escaped = string.replace(/[-\\/\\\\^$*+?.()|[\\]{}]/g, '\\\\$&');␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = string.replace(/[-\\/\\\\^$*+?.()|[\\]{}]/g, '\\\\$&');␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Output:␊
      1 | const escaped = RegExp.escape(string);␊
    `

## invalid(9): const escaped = string.replace(/[\/\-\\^$*+?.()|[\]{}]/g, '\\$&');

> Input

    `␊
      1 | const escaped = string.replace(/[\\/\\-\\\\^$*+?.()|[\\]{}]/g, '\\\\$&');␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = string.replace(/[\\/\\-\\\\^$*+?.()|[\\]{}]/g, '\\\\$&');␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Output:␊
      1 | const escaped = RegExp.escape(string);␊
    `

## invalid(10): const escaped = string.replace(/[\/\\^$*+?.()|[\]{}-]/g, '\\$&');

> Input

    `␊
      1 | const escaped = string.replace(/[\\/\\\\^$*+?.()|[\\]{}-]/g, '\\\\$&');␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = string.replace(/[\\/\\\\^$*+?.()|[\\]{}-]/g, '\\\\$&');␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Output:␊
      1 | const escaped = RegExp.escape(string);␊
    `

## invalid(11): const escaped = object.value.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

> Input

    `␊
      1 | const escaped = object.value.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = object.value.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Output:␊
      1 | const escaped = RegExp.escape(object.value);␊
    `

## invalid(12): const escaped = getString().replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

> Input

    `␊
      1 | const escaped = getString().replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = getString().replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Output:␊
      1 | const escaped = RegExp.escape(getString());␊
    `

## invalid(13): const escaped = (string).replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

> Input

    `␊
      1 | const escaped = (string).replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = (string).replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Output:␊
      1 | const escaped = RegExp.escape((string));␊
    `

## invalid(14): const escaped = (0, string).replace(/[.*+?^${}()|[\]\\]/g, '\\$&');

> Input

    `␊
      1 | const escaped = (0, string).replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = (0, string).replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Output:␊
      1 | const escaped = RegExp.escape((0, string));␊
    `

## invalid(15): const escaped = string.replace(/[.*+?^${}()|[\]\\]/g, "\\$&");

> Input

    `␊
      1 | const escaped = string.replace(/[.*+?^${}()|[\\]\\\\]/g, "\\\\$&");␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = string.replace(/[.*+?^${}()|[\\]\\\\]/g, "\\\\$&");␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Output:␊
      1 | const escaped = RegExp.escape(string);␊
    `

## invalid(16): const escaped = string.replace(/* pattern */ /[.*+?^${}()|[\]\\]/g, '\\$&');

> Input

    `␊
      1 | const escaped = string.replace(/* pattern */ /[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = string.replace(/* pattern */ /[.*+?^${}()|[\\]\\\\]/g, '\\\\$&');␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    `

## invalid(17): class StringSubclass extends String { method() { return super.replace(/[.*+?^${}()|[\]\\]/g, '\\$&'); } }

> Input

    `␊
      1 | class StringSubclass extends String { method() { return super.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&'); } }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | class StringSubclass extends String { method() { return super.replace(/[.*+?^${}()|[\\]\\\\]/g, '\\\\$&'); } }␊
        |                                                         ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    `

## invalid(18): import escapeStringRegexp from 'escape-string-regexp'; const escaped = escapeStringRegexp(string);

> Input

    `␊
      1 | import escapeStringRegexp from 'escape-string-regexp'; const escaped = escapeStringRegexp(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | import escapeStringRegexp from 'escape-string-regexp'; const escaped = escapeStringRegexp(string);␊
        |                                                                        ^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | import escapeStringRegexp from 'escape-string-regexp'; const escaped = RegExp.escape(string);␊
    `

## invalid(19): import escapeRegex from 'escape-string-regexp'; const escaped = escapeRegex(string);

> Input

    `␊
      1 | import escapeRegex from 'escape-string-regexp'; const escaped = escapeRegex(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | import escapeRegex from 'escape-string-regexp'; const escaped = escapeRegex(string);␊
        |                                                                 ^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | import escapeRegex from 'escape-string-regexp'; const escaped = RegExp.escape(string);␊
    `

## invalid(20): import {default as escapeStringRegexp} from 'escape-string-regexp'; const escaped = escapeStringRegexp(string);

> Input

    `␊
      1 | import {default as escapeStringRegexp} from 'escape-string-regexp'; const escaped = escapeStringRegexp(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | import {default as escapeStringRegexp} from 'escape-string-regexp'; const escaped = escapeStringRegexp(string);␊
        |                                                                                     ^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | import {default as escapeStringRegexp} from 'escape-string-regexp'; const escaped = RegExp.escape(string);␊
    `

## invalid(21): const escapeStringRegexp = require('escape-string-regexp'); const escaped = escapeStringRegexp(string);

> Input

    `␊
      1 | const escapeStringRegexp = require('escape-string-regexp'); const escaped = escapeStringRegexp(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escapeStringRegexp = require('escape-string-regexp'); const escaped = escapeStringRegexp(string);␊
        |                                                                             ^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | const escapeStringRegexp = require('escape-string-regexp'); const escaped = RegExp.escape(string);␊
    `

## invalid(22): import escapeRegExp from 'lodash.escaperegexp'; const escaped = escapeRegExp(string);

> Input

    `␊
      1 | import escapeRegExp from 'lodash.escaperegexp'; const escaped = escapeRegExp(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | import escapeRegExp from 'lodash.escaperegexp'; const escaped = escapeRegExp(string);␊
        |                                                                 ^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | import escapeRegExp from 'lodash.escaperegexp'; const escaped = RegExp.escape(string);␊
    `

## invalid(23): import {default as escapeRegExp} from 'lodash.escaperegexp'; const escaped = escapeRegExp(string);

> Input

    `␊
      1 | import {default as escapeRegExp} from 'lodash.escaperegexp'; const escaped = escapeRegExp(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | import {default as escapeRegExp} from 'lodash.escaperegexp'; const escaped = escapeRegExp(string);␊
        |                                                                              ^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | import {default as escapeRegExp} from 'lodash.escaperegexp'; const escaped = RegExp.escape(string);␊
    `

## invalid(24): const escapeRegExp = require('lodash.escaperegexp'); const escaped = escapeRegExp(string);

> Input

    `␊
      1 | const escapeRegExp = require('lodash.escaperegexp'); const escaped = escapeRegExp(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escapeRegExp = require('lodash.escaperegexp'); const escaped = escapeRegExp(string);␊
        |                                                                      ^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | const escapeRegExp = require('lodash.escaperegexp'); const escaped = RegExp.escape(string);␊
    `

## invalid(25): import escapeRegExp from 'lodash/escapeRegExp'; const escaped = escapeRegExp(string);

> Input

    `␊
      1 | import escapeRegExp from 'lodash/escapeRegExp'; const escaped = escapeRegExp(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | import escapeRegExp from 'lodash/escapeRegExp'; const escaped = escapeRegExp(string);␊
        |                                                                 ^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | import escapeRegExp from 'lodash/escapeRegExp'; const escaped = RegExp.escape(string);␊
    `

## invalid(26): import {default as escapeRegExp} from 'lodash/escapeRegExp'; const escaped = escapeRegExp(string);

> Input

    `␊
      1 | import {default as escapeRegExp} from 'lodash/escapeRegExp'; const escaped = escapeRegExp(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | import {default as escapeRegExp} from 'lodash/escapeRegExp'; const escaped = escapeRegExp(string);␊
        |                                                                              ^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | import {default as escapeRegExp} from 'lodash/escapeRegExp'; const escaped = RegExp.escape(string);␊
    `

## invalid(27): const escapeRegExp = require('lodash/escapeRegExp'); const escaped = escapeRegExp(string);

> Input

    `␊
      1 | const escapeRegExp = require('lodash/escapeRegExp'); const escaped = escapeRegExp(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escapeRegExp = require('lodash/escapeRegExp'); const escaped = escapeRegExp(string);␊
        |                                                                      ^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | const escapeRegExp = require('lodash/escapeRegExp'); const escaped = RegExp.escape(string);␊
    `

## invalid(28): import {escapeRegExp} from 'lodash-es'; const escaped = escapeRegExp(string);

> Input

    `␊
      1 | import {escapeRegExp} from 'lodash-es'; const escaped = escapeRegExp(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | import {escapeRegExp} from 'lodash-es'; const escaped = escapeRegExp(string);␊
        |                                                         ^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | import {escapeRegExp} from 'lodash-es'; const escaped = RegExp.escape(string);␊
    `

## invalid(29): import {escapeRegExp as escapeRegex} from 'lodash'; const escaped = escapeRegex(string);

> Input

    `␊
      1 | import {escapeRegExp as escapeRegex} from 'lodash'; const escaped = escapeRegex(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | import {escapeRegExp as escapeRegex} from 'lodash'; const escaped = escapeRegex(string);␊
        |                                                                     ^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | import {escapeRegExp as escapeRegex} from 'lodash'; const escaped = RegExp.escape(string);␊
    `

## invalid(30): const {escapeRegExp} = require('lodash'); const escaped = escapeRegExp(string);

> Input

    `␊
      1 | const {escapeRegExp} = require('lodash'); const escaped = escapeRegExp(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const {escapeRegExp} = require('lodash'); const escaped = escapeRegExp(string);␊
        |                                                           ^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | const {escapeRegExp} = require('lodash'); const escaped = RegExp.escape(string);␊
    `

## invalid(31): import lodash from 'lodash'; const escaped = lodash.escapeRegExp(string);

> Input

    `␊
      1 | import lodash from 'lodash'; const escaped = lodash.escapeRegExp(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | import lodash from 'lodash'; const escaped = lodash.escapeRegExp(string);␊
        |                                              ^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | import lodash from 'lodash'; const escaped = RegExp.escape(string);␊
    `

## invalid(32): import * as lodash from 'lodash-es'; const escaped = lodash.escapeRegExp(string);

> Input

    `␊
      1 | import * as lodash from 'lodash-es'; const escaped = lodash.escapeRegExp(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | import * as lodash from 'lodash-es'; const escaped = lodash.escapeRegExp(string);␊
        |                                                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | import * as lodash from 'lodash-es'; const escaped = RegExp.escape(string);␊
    `

## invalid(33): const lodash = require('lodash'); const escaped = lodash.escapeRegExp(string);

> Input

    `␊
      1 | const lodash = require('lodash'); const escaped = lodash.escapeRegExp(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const lodash = require('lodash'); const escaped = lodash.escapeRegExp(string);␊
        |                                                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | const lodash = require('lodash'); const escaped = RegExp.escape(string);␊
    `

## invalid(34): const escaped = _.escapeRegExp(string);

> Input

    `␊
      1 | const escaped = _.escapeRegExp(string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const escaped = _.escapeRegExp(string);␊
        |                 ^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    ␊
    Suggestion 1/1: Replace with \`RegExp.escape()\`.:␊
      1 | const escaped = RegExp.escape(string);␊
    `

## invalid(35): import escapeStringRegexp from 'escape-string-regexp'; const escaped = escapeStringRegexp(/* string */ string);

> Input

    `␊
      1 | import escapeStringRegexp from 'escape-string-regexp'; const escaped = escapeStringRegexp(/* string */ string);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | import escapeStringRegexp from 'escape-string-regexp'; const escaped = escapeStringRegexp(/* string */ string);␊
        |                                                                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer \`RegExp.escape()\` for escaping strings to use in regular expressions.␊
    `
