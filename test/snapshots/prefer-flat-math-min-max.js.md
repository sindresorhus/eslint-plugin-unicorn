# Snapshot report for `test/prefer-flat-math-min-max.js`

The actual snapshot is saved in `prefer-flat-math-min-max.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): Math.max(Math.max(a, b), c);

> Input

    `␊
      1 | Math.max(Math.max(a, b), c);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Math.max(Math.max(a, b), c);␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer a flat \`Math.max()\` call instead of nested calls.␊
    ␊
    Output:␊
      1 | Math.max(a, b, c);␊
    `

## invalid(2): Math.min(a, Math.min(b, c));

> Input

    `␊
      1 | Math.min(a, Math.min(b, c));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Math.min(a, Math.min(b, c));␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer a flat \`Math.min()\` call instead of nested calls.␊
    ␊
    Output:␊
      1 | Math.min(a, b, c);␊
    `

## invalid(3): Math.max(Math.max(a, b), Math.max(c, d), e);

> Input

    `␊
      1 | Math.max(Math.max(a, b), Math.max(c, d), e);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Math.max(Math.max(a, b), Math.max(c, d), e);␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer a flat \`Math.max()\` call instead of nested calls.␊
    ␊
    Output:␊
      1 | Math.max(a, b, c, d, e);␊
    `

## invalid(4): Math.min(Math.min(Math.min(a, b), c), d);

> Input

    `␊
      1 | Math.min(Math.min(Math.min(a, b), c), d);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Math.min(Math.min(Math.min(a, b), c), d);␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer a flat \`Math.min()\` call instead of nested calls.␊
    ␊
    Output:␊
      1 | Math.min(a, b, c, d);␊
    `

## invalid(5): Math.max(Math.max(a, b));

> Input

    `␊
      1 | Math.max(Math.max(a, b));␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Math.max(Math.max(a, b));␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^ Prefer a flat \`Math.max()\` call instead of nested calls.␊
    ␊
    Output:␊
      1 | Math.max(a, b);␊
    `

## invalid(6): Math.min(Math.min());

> Input

    `␊
      1 | Math.min(Math.min());␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Math.min(Math.min());␊
        | ^^^^^^^^^^^^^^^^^^^^ Prefer a flat \`Math.min()\` call instead of nested calls.␊
    ␊
    Output:␊
      1 | Math.min();␊
    `

## invalid(7): Math.max(Math.max(...values), fallback);

> Input

    `␊
      1 | Math.max(Math.max(...values), fallback);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | Math.max(Math.max(...values), fallback);␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer a flat \`Math.max()\` call instead of nested calls.␊
    ␊
    Output:␊
      1 | Math.max(...values, fallback);␊
    `

## invalid(8): const value = Math.max(foo, Math.max(bar, baz)).toString();

> Input

    `␊
      1 | const value = Math.max(foo, Math.max(bar, baz)).toString();␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const value = Math.max(foo, Math.max(bar, baz)).toString();␊
        |               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer a flat \`Math.max()\` call instead of nested calls.␊
    ␊
    Output:␊
      1 | const value = Math.max(foo, bar, baz).toString();␊
    `

## invalid(9): const value = Math.min((Math.min(a, b)), c);

> Input

    `␊
      1 | const value = Math.min((Math.min(a, b)), c);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const value = Math.min((Math.min(a, b)), c);␊
        |               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ Prefer a flat \`Math.min()\` call instead of nested calls.␊
    ␊
    Output:␊
      1 | const value = Math.min(a, b, c);␊
    `

## invalid(10): const value = Math.max( Math.max(a, b), c, );

> Input

    `␊
      1 | const value = Math.max(␊
      2 | 	Math.max(a, b),␊
      3 | 	c,␊
      4 | );␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const value = Math.max(␊
        |               ^^^^^^^^^␊
    > 2 | 	Math.max(a, b),␊
        | ^^^^^^^^^^^^^^^^␊
    > 3 | 	c,␊
        | ^^^␊
    > 4 | );␊
        | ^ Prefer a flat \`Math.max()\` call instead of nested calls.␊
    ␊
    Output:␊
      1 | const value = Math.max(a, b, c);␊
    `

## invalid(11): const value = Math.max( Math.max( a, b, ), c, );

> Input

    `␊
      1 | const value = Math.max(␊
      2 | 	Math.max(␊
      3 | 		a,␊
      4 | 		b,␊
      5 | 	),␊
      6 | 	c,␊
      7 | );␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const value = Math.max(␊
        |               ^^^^^^^^^␊
    > 2 | 	Math.max(␊
        | ^^^^^^^^^^␊
    > 3 | 		a,␊
        | ^^^^␊
    > 4 | 		b,␊
        | ^^^^␊
    > 5 | 	),␊
        | ^^^␊
    > 6 | 	c,␊
        | ^^^␊
    > 7 | );␊
        | ^ Prefer a flat \`Math.max()\` call instead of nested calls.␊
    ␊
    Output:␊
      1 | const value = Math.max(a, b, c);␊
    `

## invalid(12): const value = Math.max( Math.max(/* keep */ a, b), c, );

> Input

    `␊
      1 | const value = Math.max(␊
      2 | 	Math.max(/* keep */ a, b),␊
      3 | 	c,␊
      4 | );␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const value = Math.max(␊
        |               ^^^^^^^^^␊
    > 2 | 	Math.max(/* keep */ a, b),␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^␊
    > 3 | 	c,␊
        | ^^^␊
    > 4 | );␊
        | ^ Prefer a flat \`Math.max()\` call instead of nested calls.␊
    `
