# Snapshot report for `test/no-array-splice.js`

The actual snapshot is saved in `no-array-splice.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): let array = []; array.splice(index, deleteCount);

> Input

    `␊
      1 | let array = []; array.splice(index, deleteCount);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = []; array.splice(index, deleteCount);␊
        |                       ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = []; array = array.toSpliced(index, deleteCount);␊
    `

## invalid(2): var array = []; array.splice(index, deleteCount, item);

> Input

    `␊
      1 | var array = []; array.splice(index, deleteCount, item);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | var array = []; array.splice(index, deleteCount, item);␊
        |                       ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | var array = []; array = array.toSpliced(index, deleteCount, item);␊
    `

## invalid(3): let array = new Array(); array.splice(1, 1);

> Input

    `␊
      1 | let array = new Array(); array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = new Array(); array.splice(1, 1);␊
        |                                ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = new Array(); array = array.toSpliced(1, 1);␊
    `

## invalid(4): let array = new Array(5); array.splice(1, 1);

> Input

    `␊
      1 | let array = new Array(5); array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = new Array(5); array.splice(1, 1);␊
        |                                 ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = new Array(5); array = array.toSpliced(1, 1);␊
    `

## invalid(5): let array = Array.from(iterable); array.splice(1, 1);

> Input

    `␊
      1 | let array = Array.from(iterable); array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = Array.from(iterable); array.splice(1, 1);␊
        |                                         ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = Array.from(iterable); array = array.toSpliced(1, 1);␊
    `

## invalid(6): let array = Array.of(1, 2, 3); array.splice(1, 1);

> Input

    `␊
      1 | let array = Array.of(1, 2, 3); array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = Array.of(1, 2, 3); array.splice(1, 1);␊
        |                                      ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = Array.of(1, 2, 3); array = array.toSpliced(1, 1);␊
    `

## invalid(7): let array = items.filter(Boolean); array.splice(1, 1);

> Input

    `␊
      1 | let array = items.filter(Boolean); array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = items.filter(Boolean); array.splice(1, 1);␊
        |                                          ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = items.filter(Boolean); array = array.toSpliced(1, 1);␊
    `

## invalid(8): let array = items.map(String); array.splice(1, 1);

> Input

    `␊
      1 | let array = items.map(String); array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = items.map(String); array.splice(1, 1);␊
        |                                      ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = items.map(String); array = array.toSpliced(1, 1);␊
    `

## invalid(9): let array = items.flat(); array.splice(1, 1);

> Input

    `␊
      1 | let array = items.flat(); array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = items.flat(); array.splice(1, 1);␊
        |                                 ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = items.flat(); array = array.toSpliced(1, 1);␊
    `

## invalid(10): let array = items.toSorted(); array.splice(1, 1);

> Input

    `␊
      1 | let array = items.toSorted(); array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = items.toSorted(); array.splice(1, 1);␊
        |                                     ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = items.toSorted(); array = array.toSpliced(1, 1);␊
    `

## invalid(11): let array = items.with(0, item); array.splice(1, 1);

> Input

    `␊
      1 | let array = items.with(0, item); array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = items.with(0, item); array.splice(1, 1);␊
        |                                        ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = items.with(0, item); array = array.toSpliced(1, 1);␊
    `

## invalid(12): let array = original.toSpliced(0, 1); array.splice(1, 1);

> Input

    `␊
      1 | let array = original.toSpliced(0, 1); array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = original.toSpliced(0, 1); array.splice(1, 1);␊
        |                                             ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = original.toSpliced(0, 1); array = array.toSpliced(1, 1);␊
    `

## invalid(13): let array = object.items.filter(Boolean); array.splice(1, 1);

> Input

    `␊
      1 | let array = object.items.filter(Boolean); array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = object.items.filter(Boolean); array.splice(1, 1);␊
        |                                                 ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = object.items.filter(Boolean); array = array.toSpliced(1, 1);␊
    `

## invalid(14): let array = []; array.forEach(foo); array.splice(1, 1);

> Input

    `␊
      1 | let array = []; array.forEach(foo); array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = []; array.forEach(foo); array.splice(1, 1);␊
        |                                           ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = []; array.forEach(foo); array = array.toSpliced(1, 1);␊
    `

## invalid(15): let array = []; foo(...array); array.splice(1, 1);

> Input

    `␊
      1 | let array = []; foo(...array); array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = []; foo(...array); array.splice(1, 1);␊
        |                                      ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = []; foo(...array); array = array.toSpliced(1, 1);␊
    `

## invalid(16): let array = []; const first = array[0]; array.splice(1, 1);

> Input

    `␊
      1 | let array = []; const first = array[0]; array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = []; const first = array[0]; array.splice(1, 1);␊
        |                                               ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = []; const first = array[0]; array = array.toSpliced(1, 1);␊
    `

## invalid(17): let array = []; for (const element of array) {} array.splice(1, 1);

> Input

    `␊
      1 | let array = []; for (const element of array) {} array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = []; for (const element of array) {} array.splice(1, 1);␊
        |                                                       ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = []; for (const element of array) {} array = array.toSpliced(1, 1);␊
    `

## invalid(18): let array = []; array.splice(1, 1); array.splice(2, 1);

> Input

    `␊
      1 | let array = []; array.splice(1, 1); array.splice(2, 1);␊
    `

> Error 1/2

    `␊
    Message:␊
    > 1 | let array = []; array.splice(1, 1); array.splice(2, 1);␊
        |                       ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = []; array = array.toSpliced(1, 1); array.splice(2, 1);␊
    `

> Error 2/2

    `␊
    Message:␊
    > 1 | let array = []; array.splice(1, 1); array.splice(2, 1);␊
        |                                           ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = []; array.splice(1, 1); array = array.toSpliced(2, 1);␊
    `

## invalid(19): let array = []; array.splice(1, 1, /* comment */ item, ...items);

> Input

    `␊
      1 | let array = []; array.splice(1, 1, /* comment */ item, ...items);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = []; array.splice(1, 1, /* comment */ item, ...items);␊
        |                       ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = []; array = array.toSpliced(1, 1, /* comment */ item, ...items);␊
    `

## invalid(20): let array = []; array /* comment */ .splice(1, 1);

> Input

    `␊
      1 | let array = []; array /* comment */ .splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = []; array /* comment */ .splice(1, 1);␊
        |                                      ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = []; array = array /* comment */ .toSpliced(1, 1);␊
    `

## invalid(21): let \u0061rray = []; \u0061rray.splice(1, 1);

> Input

    `␊
      1 | let \\u0061rray = []; \\u0061rray.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let \\u0061rray = []; \\u0061rray.splice(1, 1);␊
        |                                 ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let \\u0061rray = []; \\u0061rray = \\u0061rray.toSpliced(1, 1);␊
    `

## invalid(22): let array = []; (array).splice(1, 1);

> Input

    `␊
      1 | let array = []; (array).splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = []; (array).splice(1, 1);␊
        |                         ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = []; array = (array).toSpliced(1, 1);␊
    `

## invalid(23): let array = []; array. /* comment */ splice(1, 1);

> Input

    `␊
      1 | let array = []; array. /* comment */ splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array = []; array. /* comment */ splice(1, 1);␊
        |                                      ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array = []; array = array. /* comment */ toSpliced(1, 1);␊
    `

## invalid(24): let array: string[] = []; array.splice(1, 1);

> Input

    `␊
      1 | let array: string[] = []; array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array: string[] = []; array.splice(1, 1);␊
        |                                 ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array: string[] = []; array = array.toSpliced(1, 1);␊
    `

## invalid(25): let array: Array<string> = []; array.splice(1, 1);

> Input

    `␊
      1 | let array: Array<string> = []; array.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array: Array<string> = []; array.splice(1, 1);␊
        |                                      ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array: Array<string> = []; array = array.toSpliced(1, 1);␊
    `

## invalid(26): let array: string[] = []; (array as string[]).splice(1, 1);

> Input

    `␊
      1 | let array: string[] = []; (array as string[]).splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array: string[] = []; (array as string[]).splice(1, 1);␊
        |                                               ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array: string[] = []; array = (array as string[]).toSpliced(1, 1);␊
    `

## invalid(27): let array: string[] = []; array!.splice(1, 1);

> Input

    `␊
      1 | let array: string[] = []; array!.splice(1, 1);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | let array: string[] = []; array!.splice(1, 1);␊
        |                                  ^^^^^^ Use \`Array#toSpliced()\` instead of \`Array#splice()\`.␊
    ␊
    Suggestion 1/1: Assign the \`.toSpliced()\` result back to the array.:␊
      1 | let array: string[] = []; array = array!.toSpliced(1, 1);␊
    `
