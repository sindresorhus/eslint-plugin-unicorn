# Snapshot report for `test/no-useless-set-construction.js`

The actual snapshot is saved in `no-useless-set-construction.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(other))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(other))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(other))␊
        |                                                                                                                          ^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(other)␊
    `

## invalid(2): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(other.keys()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(other.keys()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(other.keys()))␊
        |                                                                                                                          ^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(other)␊
    `

## invalid(3): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(other.values()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(other.values()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(other.values()))␊
        |                                                                                                                          ^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(other)␊
    `

## invalid(4): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(records.keys()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(records.keys()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(records.keys()))␊
        |                                                                                                                          ^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(records)␊
    `

## invalid(5): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).union(other)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).union(other)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).union(other)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(other)␊
    `

## invalid(6): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).union(other)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).union(other)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).union(other)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(other)␊
    `

## invalid(7): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).union(records)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).union(records)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).union(records)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(records)␊
    `

## invalid(1): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(new Set(other))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(new Set(other))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(new Set(other))␊
        |                                                                                                                                 ^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#intersection()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(other)␊
    `

## invalid(2): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(new Set(other.keys()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(new Set(other.keys()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(new Set(other.keys()))␊
        |                                                                                                                                 ^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#intersection()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(other)␊
    `

## invalid(3): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(new Set(other.values()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(new Set(other.values()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(new Set(other.values()))␊
        |                                                                                                                                 ^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#intersection()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(other)␊
    `

## invalid(4): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(new Set(records.keys()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(new Set(records.keys()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(new Set(records.keys()))␊
        |                                                                                                                                 ^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#intersection()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(records)␊
    `

## invalid(5): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).intersection(other)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).intersection(other)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).intersection(other)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#intersection()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(other)␊
    `

## invalid(6): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).intersection(other)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).intersection(other)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).intersection(other)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#intersection()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(other)␊
    `

## invalid(7): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).intersection(records)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).intersection(records)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).intersection(records)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#intersection()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(records)␊
    `

## invalid(1): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(new Set(other))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(new Set(other))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(new Set(other))␊
        |                                                                                                                               ^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#difference()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(other)␊
    `

## invalid(2): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(new Set(other.keys()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(new Set(other.keys()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(new Set(other.keys()))␊
        |                                                                                                                               ^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#difference()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(other)␊
    `

## invalid(3): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(new Set(other.values()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(new Set(other.values()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(new Set(other.values()))␊
        |                                                                                                                               ^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#difference()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(other)␊
    `

## invalid(4): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(new Set(records.keys()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(new Set(records.keys()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(new Set(records.keys()))␊
        |                                                                                                                               ^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#difference()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(records)␊
    `

## invalid(5): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).difference(other)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).difference(other)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).difference(other)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#difference()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(other)␊
    `

## invalid(6): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).difference(other)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).difference(other)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).difference(other)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#difference()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(other)␊
    `

## invalid(7): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).difference(records)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).difference(records)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).difference(records)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#difference()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(records)␊
    `

## invalid(1): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(new Set(other))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(new Set(other))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(new Set(other))␊
        |                                                                                                                                        ^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#symmetricDifference()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(other)␊
    `

## invalid(2): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(new Set(other.keys()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(new Set(other.keys()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(new Set(other.keys()))␊
        |                                                                                                                                        ^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#symmetricDifference()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(other)␊
    `

## invalid(3): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(new Set(other.values()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(new Set(other.values()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(new Set(other.values()))␊
        |                                                                                                                                        ^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#symmetricDifference()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(other)␊
    `

## invalid(4): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(new Set(records.keys()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(new Set(records.keys()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(new Set(records.keys()))␊
        |                                                                                                                                        ^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#symmetricDifference()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(records)␊
    `

## invalid(5): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).symmetricDifference(other)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).symmetricDifference(other)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).symmetricDifference(other)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#symmetricDifference()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(other)␊
    `

## invalid(6): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).symmetricDifference(other)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).symmetricDifference(other)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).symmetricDifference(other)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#symmetricDifference()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(other)␊
    `

## invalid(7): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).symmetricDifference(records)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).symmetricDifference(records)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).symmetricDifference(records)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#symmetricDifference()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(records)␊
    `

## invalid(1): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(new Set(other))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(new Set(other))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(new Set(other))␊
        |                                                                                                                               ^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isSubsetOf()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(other)␊
    `

## invalid(2): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(new Set(other.keys()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(new Set(other.keys()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(new Set(other.keys()))␊
        |                                                                                                                               ^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isSubsetOf()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(other)␊
    `

## invalid(3): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(new Set(other.values()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(new Set(other.values()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(new Set(other.values()))␊
        |                                                                                                                               ^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isSubsetOf()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(other)␊
    `

## invalid(4): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(new Set(records.keys()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(new Set(records.keys()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(new Set(records.keys()))␊
        |                                                                                                                               ^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isSubsetOf()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(records)␊
    `

## invalid(5): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).isSubsetOf(other)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).isSubsetOf(other)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).isSubsetOf(other)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isSubsetOf()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(other)␊
    `

## invalid(6): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).isSubsetOf(other)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).isSubsetOf(other)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).isSubsetOf(other)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isSubsetOf()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(other)␊
    `

## invalid(7): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).isSubsetOf(records)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).isSubsetOf(records)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).isSubsetOf(records)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isSubsetOf()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSubsetOf(records)␊
    `

## invalid(1): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(new Set(other))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(new Set(other))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(new Set(other))␊
        |                                                                                                                                 ^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isSupersetOf()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(other)␊
    `

## invalid(2): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(new Set(other.keys()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(new Set(other.keys()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(new Set(other.keys()))␊
        |                                                                                                                                 ^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isSupersetOf()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(other)␊
    `

## invalid(3): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(new Set(other.values()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(new Set(other.values()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(new Set(other.values()))␊
        |                                                                                                                                 ^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isSupersetOf()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(other)␊
    `

## invalid(4): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(new Set(records.keys()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(new Set(records.keys()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(new Set(records.keys()))␊
        |                                                                                                                                 ^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isSupersetOf()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(records)␊
    `

## invalid(5): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).isSupersetOf(other)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).isSupersetOf(other)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).isSupersetOf(other)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isSupersetOf()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(other)␊
    `

## invalid(6): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).isSupersetOf(other)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).isSupersetOf(other)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).isSupersetOf(other)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isSupersetOf()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(other)␊
    `

## invalid(7): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).isSupersetOf(records)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).isSupersetOf(records)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).isSupersetOf(records)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isSupersetOf()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isSupersetOf(records)␊
    `

## invalid(1): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(new Set(other))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(new Set(other))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(new Set(other))␊
        |                                                                                                                                   ^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isDisjointFrom()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(other)␊
    `

## invalid(2): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(new Set(other.keys()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(new Set(other.keys()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(new Set(other.keys()))␊
        |                                                                                                                                   ^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isDisjointFrom()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(other)␊
    `

## invalid(3): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(new Set(other.values()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(new Set(other.values()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(new Set(other.values()))␊
        |                                                                                                                                   ^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isDisjointFrom()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(other)␊
    `

## invalid(4): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(new Set(records.keys()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(new Set(records.keys()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(new Set(records.keys()))␊
        |                                                                                                                                   ^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isDisjointFrom()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(records)␊
    `

## invalid(5): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).isDisjointFrom(other)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).isDisjointFrom(other)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).isDisjointFrom(other)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isDisjointFrom()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(other)␊
    `

## invalid(6): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).isDisjointFrom(other)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).isDisjointFrom(other)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.keys()).isDisjointFrom(other)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isDisjointFrom()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(other)␊
    `

## invalid(7): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).isDisjointFrom(records)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).isDisjointFrom(records)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.values()).isDisjointFrom(records)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#isDisjointFrom()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.isDisjointFrom(records)␊
    `

## invalid(1): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.union(other))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.union(other))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.union(other))␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(other)␊
    `

## invalid(2): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.intersection(other))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.intersection(other))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.intersection(other))␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#intersection()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.intersection(other)␊
    `

## invalid(3): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.difference(other))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.difference(other))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.difference(other))␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#difference()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.difference(other)␊
    `

## invalid(4): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.symmetricDifference(other))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.symmetricDifference(other))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.symmetricDifference(other))␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#symmetricDifference()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.symmetricDifference(other)␊
    `

## invalid(5): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); const alias = other; selected.union(new Set(alias))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); const alias = other; selected.union(new Set(alias))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); const alias = other; selected.union(new Set(alias))␊
        |                                                                                                                                               ^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); const alias = other; selected.union(alias)␊
    `

## invalid(6): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set((condition ? other : selected)))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set((condition ? other : selected)))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set((condition ? other : selected)))␊
        |                                                                                                                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union((condition ? other : selected))␊
    `

## invalid(7): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set((condition ? other : selected)).union(records)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set((condition ? other : selected)).union(records)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set((condition ? other : selected)).union(records)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); (condition ? other : selected).union(records)␊
    `

## invalid(8): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(((records)).keys()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(((records)).keys()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(((records)).keys()))␊
        |                                                                                                                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(((records)))␊
    `

## invalid(9): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(other /* retained */))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(other /* retained */))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(other /* retained */))␊
        |                                                                                                                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    `

## invalid(10): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(/* wrapper comment */ other))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(/* wrapper comment */ other))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(/* wrapper comment */ other))␊
        |                                                                                                                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    `

## invalid(11): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(records./* method comment */keys()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(records./* method comment */keys()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(new Set(records./* method comment */keys()))␊
        |                                                                                                                          ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    `

## invalid(12): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(/* receiver comment */ selected).union(other)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(/* receiver comment */ selected).union(other)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(/* receiver comment */ selected).union(other)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    `

## invalid(13): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(/* result comment */ selected.union(other))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(/* result comment */ selected.union(other))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(/* result comment */ selected.union(other))␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    `

## invalid(14): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.union(/* retained */ other)).has(2)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.union(/* retained */ other)).has(2)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.union(/* retained */ other)).has(2)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(/* retained */ other).has(2)␊
    `

## invalid(15): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.union(other)).intersection(records)

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.union(other)).intersection(records)␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.union(other)).intersection(records)␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(other).intersection(records)␊
    `

## invalid(16): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new new Set(selected.union(other))()

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new new Set(selected.union(other))()␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new new Set(selected.union(other))()␊
        |                                                                                                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new (selected.union(other))()␊
    `

## invalid(17): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new new Set(selected.union(other)).constructor()

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new new Set(selected.union(other)).constructor()␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new new Set(selected.union(other)).constructor()␊
        |                                                                                                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new (selected.union(other)).constructor()␊
    `

## invalid(18): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new (new Set(selected.union(other))).constructor()

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new (new Set(selected.union(other))).constructor()␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new (new Set(selected.union(other))).constructor()␊
        |                                                                                                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new ((selected.union(other))).constructor()␊
    `

## invalid(19): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new new Set(selected.union(other)).constructor.prototype.constructor()

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new new Set(selected.union(other)).constructor.prototype.constructor()␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new new Set(selected.union(other)).constructor.prototype.constructor()␊
        |                                                                                                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new (selected.union(other)).constructor.prototype.constructor()␊
    `

## invalid(20): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(new Set(selected).union(new Set(other)))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(new Set(selected).union(new Set(other)))␊
    `

> Error 1/2

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(new Set(selected).union(new Set(other)))␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected).union(new Set(other))␊
    `

> Error 2/2

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(new Set(selected).union(new Set(other)))␊
        |                                                                                                                                           ^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(new Set(selected).union(other))␊
    `

## invalid(21): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.union(getOther()))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.union(getOther()))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.union(getOther()))␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union(getOther())␊
    `

## invalid(22): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.union((selected.clear(), other)))

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.union((selected.clear(), other)))␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); new Set(selected.union((selected.clear(), other)))␊
        |                                                                                                           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); selected.union((selected.clear(), other))␊
    `

## invalid(23): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); function result() { return new Set(selected.union(other)) }

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); function result() { return new Set(selected.union(other)) }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); function result() { return new Set(selected.union(other)) }␊
        |                                                                                                                                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); function result() { return selected.union(other) }␊
    `

## invalid(24): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); function result() { throw new Set(selected.union(other)) }

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); function result() { throw new Set(selected.union(other)) }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); function result() { throw new Set(selected.union(other)) }␊
        |                                                                                                                                     ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); function result() { throw selected.union(other) }␊
    `

## invalid(25): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); for (const item of new Set(selected.union(other))) {}

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); for (const item of new Set(selected.union(other))) {}␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); for (const item of new Set(selected.union(other))) {}␊
        |                                                                                                                              ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); for (const item of selected.union(other)) {}␊
    `

## invalid(26): const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); const view = <div>{selected.union(new Set(other))}</div>

> Input

    `␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); const view = <div>{selected.union(new Set(other))}</div>␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); const view = <div>{selected.union(new Set(other))}</div>␊
        |                                                                                                                                             ^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set([1, 2]); const other = new Set([2, 3]); const records = new Map([[2, "value"]]); const view = <div>{selected.union(other)}</div>␊
    `

## invalid(27): const selected = new Set(); const other = new Set(); previous() new Set(condition ? selected : other).union(other)

> Input

    `␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | previous()␊
      4 | new Set(condition ? selected : other).union(other)␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | previous()␊
    > 4 | new Set(condition ? selected : other).union(other)␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | previous()␊
      4 | ;(condition ? selected : other).union(other)␊
    `

## invalid(28): const selected = new Set(); const other = new Set(); previous() new Set((condition ? selected : other).union(other))

> Input

    `␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | previous()␊
      4 | new Set((condition ? selected : other).union(other))␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | previous()␊
    > 4 | new Set((condition ? selected : other).union(other))␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | previous()␊
      4 | ;(condition ? selected : other).union(other)␊
    `

## invalid(29): const selected = new Set(); const other = new Set(); const preceding = function() {} new Set(condition ? selected : other).union(other)

> Input

    `␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | const preceding = function() {}␊
      4 | new Set(condition ? selected : other).union(other)␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | const preceding = function() {}␊
    > 4 | new Set(condition ? selected : other).union(other)␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | const preceding = function() {}␊
      4 | ;(condition ? selected : other).union(other)␊
    `

## invalid(30): const selected = new Set(); const other = new Set(); const preceding = class {} new Set(condition ? selected : other).union(other)

> Input

    `␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | const preceding = class {}␊
      4 | new Set(condition ? selected : other).union(other)␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | const preceding = class {}␊
    > 4 | new Set(condition ? selected : other).union(other)␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | const preceding = class {}␊
      4 | ;(condition ? selected : other).union(other)␊
    `

## invalid(31): const selected = new Set(); const other = new Set(); const preceding = () => {} new Set(condition ? selected : other).union(other)

> Input

    `␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | const preceding = () => {}␊
      4 | new Set(condition ? selected : other).union(other)␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | const preceding = () => {}␊
    > 4 | new Set(condition ? selected : other).union(other)␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | const preceding = () => {}␊
      4 | (condition ? selected : other).union(other)␊
    `

## invalid(32): const selected = new Set(); const other = new Set(); function preceding() {} new Set(condition ? selected : other).union(other)

> Input

    `␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | function preceding() {}␊
      4 | new Set(condition ? selected : other).union(other)␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | function preceding() {}␊
    > 4 | new Set(condition ? selected : other).union(other)␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | function preceding() {}␊
      4 | (condition ? selected : other).union(other)␊
    `

## invalid(33): const selected = new Set(); const other = new Set(); class Preceding {} new Set(condition ? selected : other).union(other)

> Input

    `␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | class Preceding {}␊
      4 | new Set(condition ? selected : other).union(other)␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | class Preceding {}␊
    > 4 | new Set(condition ? selected : other).union(other)␊
        | ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | const selected = new Set();␊
      2 | const other = new Set();␊
      3 | class Preceding {}␊
      4 | (condition ? selected : other).union(other)␊
    `

## invalid(34): function check(selected: Set<number>, other: ReadonlySet<number>) { return selected.union(new Set(other)); }

> Input

    `␊
      1 | function check(selected: Set<number>, other: ReadonlySet<number>) { return selected.union(new Set(other)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function check(selected: Set<number>, other: ReadonlySet<number>) { return selected.union(new Set(other)); }␊
        |                                                                                           ^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | function check(selected: Set<number>, other: ReadonlySet<number>) { return selected.union(other); }␊
    `

## invalid(35): function check(selected: Set<number>, records: ReadonlyMap<number, string>) { return selected.union(new Set(records.keys())); }

> Input

    `␊
      1 | function check(selected: Set<number>, records: ReadonlyMap<number, string>) { return selected.union(new Set(records.keys())); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function check(selected: Set<number>, records: ReadonlyMap<number, string>) { return selected.union(new Set(records.keys())); }␊
        |                                                                                                     ^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | function check(selected: Set<number>, records: ReadonlyMap<number, string>) { return selected.union(records); }␊
    `

## invalid(36): function check(selected: ReadonlySet<number>, other: Set<number>) { return new Set(selected).union(other); }

> Input

    `␊
      1 | function check(selected: ReadonlySet<number>, other: Set<number>) { return new Set(selected).union(other); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function check(selected: ReadonlySet<number>, other: Set<number>) { return new Set(selected).union(other); }␊
        |                                                                            ^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | function check(selected: ReadonlySet<number>, other: Set<number>) { return selected.union(other); }␊
    `

## invalid(37): function check(selected: Set<number>, other: Set<number>) { return new Set(selected.union(other)); }

> Input

    `␊
      1 | function check(selected: Set<number>, other: Set<number>) { return new Set(selected.union(other)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function check(selected: Set<number>, other: Set<number>) { return new Set(selected.union(other)); }␊
        |                                                                    ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | function check(selected: Set<number>, other: Set<number>) { return selected.union(other); }␊
    `

## invalid(38): function check(selected: Set<number>, other: unknown) { return selected.union(new Set(other as Set<number>)); }

> Input

    `␊
      1 | function check(selected: Set<number>, other: unknown) { return selected.union(new Set(other as Set<number>)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function check(selected: Set<number>, other: unknown) { return selected.union(new Set(other as Set<number>)); }␊
        |                                                                               ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | function check(selected: Set<number>, other: unknown) { return selected.union((other as Set<number>)); }␊
    `

## invalid(39): function check(selected: Set<number>, other: unknown) { return new Set(other as Set<number>).union(selected); }

> Input

    `␊
      1 | function check(selected: Set<number>, other: unknown) { return new Set(other as Set<number>).union(selected); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function check(selected: Set<number>, other: unknown) { return new Set(other as Set<number>).union(selected); }␊
        |                                                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | function check(selected: Set<number>, other: unknown) { return (other as Set<number>).union(selected); }␊
    `

## invalid(40): function check(selected: Set<number>, other: unknown) { return new Set(<Set<number>>other).union(selected); }

> Input

    `␊
      1 | function check(selected: Set<number>, other: unknown) { return new Set(<Set<number>>other).union(selected); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function check(selected: Set<number>, other: unknown) { return new Set(<Set<number>>other).union(selected); }␊
        |                                                                ^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | function check(selected: Set<number>, other: unknown) { return (<Set<number>>other).union(selected); }␊
    `

## invalid(41): function check(selected: Set<number>, other: Set<number>) { return selected!.union(new Set(other)); }

> Input

    `␊
      1 | function check(selected: Set<number>, other: Set<number>) { return selected!.union(new Set(other)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function check(selected: Set<number>, other: Set<number>) { return selected!.union(new Set(other)); }␊
        |                                                                                    ^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | function check(selected: Set<number>, other: Set<number>) { return selected!.union(other); }␊
    `

## invalid(42): function check(selected: Set<number>, other: Set<number>) { return selected.union(new Set(other satisfies Set<number>)); }

> Input

    `␊
      1 | function check(selected: Set<number>, other: Set<number>) { return selected.union(new Set(other satisfies Set<number>)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function check(selected: Set<number>, other: Set<number>) { return selected.union(new Set(other satisfies Set<number>)); }␊
        |                                                                                   ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | function check(selected: Set<number>, other: Set<number>) { return selected.union((other satisfies Set<number>)); }␊
    `

## invalid(43): function check(selected: Set<number>, other: Set<number>) { return new new Set(selected.union(other))!(); }

> Input

    `␊
      1 | function check(selected: Set<number>, other: Set<number>) { return new new Set(selected.union(other))!(); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function check(selected: Set<number>, other: Set<number>) { return new new Set(selected.union(other))!(); }␊
        |                                                                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | function check(selected: Set<number>, other: Set<number>) { return new (selected.union(other))!(); }␊
    `

## invalid(44): function check(selected: Set<number>, other: Set<number>) { return new new Set(selected.union(other))!.constructor(); }

> Input

    `␊
      1 | function check(selected: Set<number>, other: Set<number>) { return new new Set(selected.union(other))!.constructor(); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function check(selected: Set<number>, other: Set<number>) { return new new Set(selected.union(other))!.constructor(); }␊
        |                                                                        ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | function check(selected: Set<number>, other: Set<number>) { return new (selected.union(other))!.constructor(); }␊
    `

## invalid(45): function check(selected: Set<number>, records: {value: Map<number, string>}) { return selected.union(new Set(records.value.keys())); }

> Input 'file.ts'

    `␊
      1 | function check(selected: Set<number>, records: {value: Map<number, string>}) { return selected.union(new Set(records.value.keys())); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function check(selected: Set<number>, records: {value: Map<number, string>}) { return selected.union(new Set(records.value.keys())); }␊
        |                                                                                                      ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | function check(selected: Set<number>, records: {value: Map<number, string>}) { return selected.union(records.value); }␊
    `

## invalid(46): function check(selected: {value: Set<number>}, other: Set<number>) { return new Set(selected.value.union(other)); }

> Input 'file.ts'

    `␊
      1 | function check(selected: {value: Set<number>}, other: Set<number>) { return new Set(selected.value.union(other)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function check(selected: {value: Set<number>}, other: Set<number>) { return new Set(selected.value.union(other)); }␊
        |                                                                             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | function check(selected: {value: Set<number>}, other: Set<number>) { return selected.value.union(other); }␊
    `

## invalid(47): function check(source: Set<Set<number>>, selected: Set<number>) { const [other] = source; return selected.union(new Set(other)); }

> Input 'file.ts'

    `␊
      1 | function check(source: Set<Set<number>>, selected: Set<number>) { const [other] = source; return selected.union(new Set(other)); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function check(source: Set<Set<number>>, selected: Set<number>) { const [other] = source; return selected.union(new Set(other)); }␊
        |                                                                                                                 ^^^^^^^^^^^^^^ This \`Set\` construction is unnecessary for \`Set#union()\`.␊
    ␊
    Output:␊
      1 | function check(source: Set<Set<number>>, selected: Set<number>) { const [other] = source; return selected.union(other); }␊
    `
