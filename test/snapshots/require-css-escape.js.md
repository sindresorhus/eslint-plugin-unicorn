# Snapshot report for `test/require-css-escape.js`

The actual snapshot is saved in `require-css-escape.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): document.querySelector(`[data-id="${id}"]`);

> Input

    `␊
      1 | document.querySelector(\`[data-id="${id}"]\`);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | document.querySelector(\`[data-id="${id}"]\`);␊
        |                                     ^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | document.querySelector(\`[data-id="${CSS.escape(id)}"]\`);␊
    `

## invalid(2): element.querySelectorAll(`a[href^="#${hash}"]`);

> Input

    `␊
      1 | element.querySelectorAll(\`a[href^="#${hash}"]\`);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | element.querySelectorAll(\`a[href^="#${hash}"]\`);␊
        |                                       ^^^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | element.querySelectorAll(\`a[href^="#${CSS.escape(hash)}"]\`);␊
    `

## invalid(3): element.matches(`[data-id="${id}"]`);

> Input

    `␊
      1 | element.matches(\`[data-id="${id}"]\`);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | element.matches(\`[data-id="${id}"]\`);␊
        |                              ^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | element.matches(\`[data-id="${CSS.escape(id)}"]\`);␊
    `

## invalid(4): element.closest(`[data-id="${id}"]`);

> Input

    `␊
      1 | element.closest(\`[data-id="${id}"]\`);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | element.closest(\`[data-id="${id}"]\`);␊
        |                              ^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | element.closest(\`[data-id="${CSS.escape(id)}"]\`);␊
    `

## invalid(5): document?.querySelector(`[data-id="${id}"]`);

> Input

    `␊
      1 | document?.querySelector(\`[data-id="${id}"]\`);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | document?.querySelector(\`[data-id="${id}"]\`);␊
        |                                      ^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | document?.querySelector(\`[data-id="${CSS.escape(id)}"]\`);␊
    `

## invalid(6): document.querySelector?.(`[data-id="${id}"]`);

> Input

    `␊
      1 | document.querySelector?.(\`[data-id="${id}"]\`);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | document.querySelector?.(\`[data-id="${id}"]\`);␊
        |                                       ^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | document.querySelector?.(\`[data-id="${CSS.escape(id)}"]\`);␊
    `

## invalid(7): document.querySelector(`[data-id="${id}"]`, root);

> Input

    `␊
      1 | document.querySelector(\`[data-id="${id}"]\`, root);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | document.querySelector(\`[data-id="${id}"]\`, root);␊
        |                                     ^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | document.querySelector(\`[data-id="${CSS.escape(id)}"]\`, root);␊
    `

## invalid(8): document.querySelector(`[data-id="${id}"][data-name="${name}"]`);

> Input

    `␊
      1 | document.querySelector(\`[data-id="${id}"][data-name="${name}"]\`);␊
    `

> Error 1/2

    `␊
    Message:␊
    > 1 | document.querySelector(\`[data-id="${id}"][data-name="${name}"]\`);␊
        |                                     ^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | document.querySelector(\`[data-id="${CSS.escape(id)}"][data-name="${name}"]\`);␊
    `

> Error 2/2

    `␊
    Message:␊
    > 1 | document.querySelector(\`[data-id="${id}"][data-name="${name}"]\`);␊
        |                                                        ^^^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | document.querySelector(\`[data-id="${id}"][data-name="${CSS.escape(name)}"]\`);␊
    `

## invalid(9): document.querySelector(`[data-id="${String(id)}"]`);

> Input

    `␊
      1 | document.querySelector(\`[data-id="${String(id)}"]\`);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | document.querySelector(\`[data-id="${String(id)}"]\`);␊
        |                                     ^^^^^^^^^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | document.querySelector(\`[data-id="${CSS.escape(String(id))}"]\`);␊
    `

## invalid(10): document.querySelector(`[data-id="${/* keep */ id}"]`);

> Input

    `␊
      1 | document.querySelector(\`[data-id="${/* keep */ id}"]\`);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | document.querySelector(\`[data-id="${/* keep */ id}"]\`);␊
        |                                                ^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | document.querySelector(\`[data-id="${/* keep */ CSS.escape(id)}"]\`);␊
    `

## invalid(11): document.querySelector(`[data-id="${id}"] .${className}`);

> Input

    `␊
      1 | document.querySelector(\`[data-id="${id}"] .${className}\`);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | document.querySelector(\`[data-id="${id}"] .${className}\`);␊
        |                                     ^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | document.querySelector(\`[data-id="${CSS.escape(id)}"] .${className}\`);␊
    `

## invalid(12): document.querySelector(`#${id}`);

> Input

    `␊
    Options:␊
    - checkAllSelectors: true␊
    ␊
    Code:␊
      1 | document.querySelector(\`#${id}\`);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | document.querySelector(\`#${id}\`);␊
        |                            ^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | document.querySelector(\`#${CSS.escape(id)}\`);␊
    `

## invalid(13): document.querySelectorAll(`.${className}`);

> Input

    `␊
    Options:␊
    - checkAllSelectors: true␊
    ␊
    Code:␊
      1 | document.querySelectorAll(\`.${className}\`);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | document.querySelectorAll(\`.${className}\`);␊
        |                               ^^^^^^^^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | document.querySelectorAll(\`.${CSS.escape(className)}\`);␊
    `

## invalid(14): document.querySelector(`#${id}[data-value="${value}"] .${className}`);

> Input

    `␊
    Options:␊
    - checkAllSelectors: true␊
    ␊
    Code:␊
      1 | document.querySelector(\`#${id}[data-value="${value}"] .${className}\`);␊
    `

> Error 1/3

    `␊
    Message:␊
    > 1 | document.querySelector(\`#${id}[data-value="${value}"] .${className}\`);␊
        |                            ^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | document.querySelector(\`#${CSS.escape(id)}[data-value="${value}"] .${className}\`);␊
    `

> Error 2/3

    `␊
    Message:␊
    > 1 | document.querySelector(\`#${id}[data-value="${value}"] .${className}\`);␊
        |                                              ^^^^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | document.querySelector(\`#${id}[data-value="${CSS.escape(value)}"] .${className}\`);␊
    `

> Error 3/3

    `␊
    Message:␊
    > 1 | document.querySelector(\`#${id}[data-value="${value}"] .${className}\`);␊
        |                                                          ^^^^^^^^^ Use \`CSS.escape()\` for interpolated values in CSS selectors.␊
    ␊
    Output:␊
      1 | document.querySelector(\`#${id}[data-value="${value}"] .${CSS.escape(className)}\`);␊
    `
