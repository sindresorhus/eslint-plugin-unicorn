# Snapshot report for `test/no-mismatched-map-key.js`

The actual snapshot is saved in `no-mismatched-map-key.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): function f(map: Map<string, number>) { if (map.has("a")) { return map.get("b"); } }

> Input

    `␊
      1 | function f(map: Map<string, number>) { if (map.has("a")) { return map.get("b"); } }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | function f(map: Map<string, number>) { if (map.has("a")) { return map.get("b"); } }␊
        |                                                                           ^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(2): map.has(key) ? map.get(anotherKey) : value;

> Input

    `␊
      1 | map.has(key) ? map.get(anotherKey) : value;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | map.has(key) ? map.get(anotherKey) : value;␊
        |                        ^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(3): map.has(key) ? map.set(anotherKey, value) : value;

> Input

    `␊
      1 | map.has(key) ? map.set(anotherKey, value) : value;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | map.has(key) ? map.set(anotherKey, value) : value;␊
        |                        ^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(4): if (map.has(key)) { const value = map.get(anotherKey); }

> Input

    `␊
      1 | if (map.has(key)) { const value = map.get(anotherKey); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | if (map.has(key)) { const value = map.get(anotherKey); }␊
        |                                           ^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(5): if (map.has(key)) { map.set(anotherKey, value); }

> Input

    `␊
      1 | if (map.has(key)) { map.set(anotherKey, value); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | if (map.has(key)) { map.set(anotherKey, value); }␊
        |                             ^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(6): if (!map.has(key)) { const value = map.get(anotherKey); }

> Input

    `␊
      1 | if (!map.has(key)) { const value = map.get(anotherKey); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | if (!map.has(key)) { const value = map.get(anotherKey); }␊
        |                                            ^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(7): if (!map.has(key)) { map.set(anotherKey, value); }

> Input

    `␊
      1 | if (!map.has(key)) { map.set(anotherKey, value); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | if (!map.has(key)) { map.set(anotherKey, value); }␊
        |                              ^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(8): map.has(key) ? value : map.get(anotherKey);

> Input

    `␊
      1 | map.has(key) ? value : map.get(anotherKey);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | map.has(key) ? value : map.get(anotherKey);␊
        |                                ^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(9): !map.has(key) ? value : map.get(anotherKey);

> Input

    `␊
      1 | !map.has(key) ? value : map.get(anotherKey);␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | !map.has(key) ? value : map.get(anotherKey);␊
        |                                 ^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(10): if (map.has(key)) {} else { map.get(anotherKey); }

> Input

    `␊
      1 | if (map.has(key)) {} else { map.get(anotherKey); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | if (map.has(key)) {} else { map.get(anotherKey); }␊
        |                                     ^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(11): if (!map.has(key)) {} else { map.get(anotherKey); }

> Input

    `␊
      1 | if (!map.has(key)) {} else { map.get(anotherKey); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | if (!map.has(key)) {} else { map.get(anotherKey); }␊
        |                                      ^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(12): if (map.has(key)) { for (const item of items) { map.get(anotherKey); } }

> Input

    `␊
      1 | if (map.has(key)) { for (const item of items) { map.get(anotherKey); } }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | if (map.has(key)) { for (const item of items) { map.get(anotherKey); } }␊
        |                                                         ^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(13): map.has(key) ? (map.has(key) ? map.get(anotherKey) : value) : value;

> Input

    `␊
      1 | map.has(key) ? (map.has(key) ? map.get(anotherKey) : value) : value;␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | map.has(key) ? (map.has(key) ? map.get(anotherKey) : value) : value;␊
        |                                        ^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(14): if (map.has(key)) { if (condition) { map.get(anotherKey); } }

> Input

    `␊
      1 | if (map.has(key)) { if (condition) { map.get(anotherKey); } }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | if (map.has(key)) { if (condition) { map.get(anotherKey); } }␊
        |                                              ^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(15): if (map.has(key)) { condition ? map.get(anotherKey) : value; }

> Input

    `␊
      1 | if (map.has(key)) { condition ? map.get(anotherKey) : value; }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | if (map.has(key)) { condition ? map.get(anotherKey) : value; }␊
        |                                         ^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(16): if (map.has(key)) { const key = anotherKey, value = map.get(key); }

> Input

    `␊
      1 | if (map.has(key)) { const key = anotherKey, value = map.get(key); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | if (map.has(key)) { const key = anotherKey, value = map.get(key); }␊
        |                                                             ^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(17): if (map.has(object.key)) { const object = other, value = map.get(object.key); }

> Input

    `␊
      1 | if (map.has(object.key)) { const object = other, value = map.get(object.key); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | if (map.has(object.key)) { const object = other, value = map.get(object.key); }␊
        |                                                                  ^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(18): if (map.has("key")) { map.get("anotherKey"); }

> Input

    `␊
      1 | if (map.has("key")) { map.get("anotherKey"); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | if (map.has("key")) { map.get("anotherKey"); }␊
        |                               ^^^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(19): if (map.has(1)) { map.get(2); }

> Input

    `␊
      1 | if (map.has(1)) { map.get(2); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | if (map.has(1)) { map.get(2); }␊
        |                           ^ Do not check a Map key before accessing a different key.␊
    `

## invalid(20): if (map.has(object.key)) { map.get(object.anotherKey); }

> Input

    `␊
      1 | if (map.has(object.key)) { map.get(object.anotherKey); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | if (map.has(object.key)) { map.get(object.anotherKey); }␊
        |                                    ^^^^^^^^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(21): if (this.map.has(this.key)) { this.map.get(this.anotherKey); }

> Input

    `␊
      1 | if (this.map.has(this.key)) { this.map.get(this.anotherKey); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | if (this.map.has(this.key)) { this.map.get(this.anotherKey); }␊
        |                                            ^^^^^^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `

## invalid(22): if (map.has(key as string)) { map.get(anotherKey as string); }

> Input

    `␊
      1 | if (map.has(key as string)) { map.get(anotherKey as string); }␊
    `

> Error 1/1

    `␊
    Message:␊
    > 1 | if (map.has(key as string)) { map.get(anotherKey as string); }␊
        |                                       ^^^^^^^^^^^^^^^^^^^^ Do not check a Map key before accessing a different key.␊
    `
