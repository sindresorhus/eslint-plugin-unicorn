# Snapshot report for `test/no-break-in-nested-loop.js`

The actual snapshot is saved in `no-break-in-nested-loop.js.snap`.

Generated by [AVA](https://avajs.dev).

## invalid(1): for (const item of items) { for (const child of item.children) { break; } }

> Input

    `␊
      1 | for (const item of items) {␊
      2 | 	for (const child of item.children) {␊
      3 | 		break;␊
      4 | 	}␊
      5 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | for (const item of items) {␊
      2 | 	for (const child of item.children) {␊
    > 3 | 		break;␊
        | 		^^^^^^ Move this nested loop or switch into a function instead of using \`break\` here.␊
      4 | 	}␊
      5 | }␊
    `

## invalid(2): for (const item of items) { while (item.children.pop()) { continue; } }

> Input

    `␊
      1 | for (const item of items) {␊
      2 | 	while (item.children.pop()) {␊
      3 | 		continue;␊
      4 | 	}␊
      5 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | for (const item of items) {␊
      2 | 	while (item.children.pop()) {␊
    > 3 | 		continue;␊
        | 		^^^^^^^^^ Move this nested loop or switch into a function instead of using \`continue\` here.␊
      4 | 	}␊
      5 | }␊
    `

## invalid(3): for (const item of items) { switch (item.type) { case 'child': break; } }

> Input

    `␊
      1 | for (const item of items) {␊
      2 | 	switch (item.type) {␊
      3 | 		case 'child':␊
      4 | 			break;␊
      5 | 	}␊
      6 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | for (const item of items) {␊
      2 | 	switch (item.type) {␊
      3 | 		case 'child':␊
    > 4 | 			break;␊
        | 			^^^^^^ Move this nested loop or switch into a function instead of using \`break\` here.␊
      5 | 	}␊
      6 | }␊
    `

## invalid(4): for (const item of items) { switch (item.type) { case 'child': continue; } }

> Input

    `␊
      1 | for (const item of items) {␊
      2 | 	switch (item.type) {␊
      3 | 		case 'child':␊
      4 | 			continue;␊
      5 | 	}␊
      6 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | for (const item of items) {␊
      2 | 	switch (item.type) {␊
      3 | 		case 'child':␊
    > 4 | 			continue;␊
        | 			^^^^^^^^^ Move this nested loop or switch into a function instead of using \`continue\` here.␊
      5 | 	}␊
      6 | }␊
    `

## invalid(5): for (let index = 0; index < items.length; index++) { for (const child of items[index].children) { break; } }

> Input

    `␊
      1 | for (let index = 0; index < items.length; index++) {␊
      2 | 	for (const child of items[index].children) {␊
      3 | 		break;␊
      4 | 	}␊
      5 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | for (let index = 0; index < items.length; index++) {␊
      2 | 	for (const child of items[index].children) {␊
    > 3 | 		break;␊
        | 		^^^^^^ Move this nested loop or switch into a function instead of using \`break\` here.␊
      4 | 	}␊
      5 | }␊
    `

## invalid(6): for (const key in items) { do { continue; } while (items[key].pending); }

> Input

    `␊
      1 | for (const key in items) {␊
      2 | 	do {␊
      3 | 		continue;␊
      4 | 	} while (items[key].pending);␊
      5 | }␊
    `

> Error 1/1

    `␊
    Message:␊
      1 | for (const key in items) {␊
      2 | 	do {␊
    > 3 | 		continue;␊
        | 		^^^^^^^^^ Move this nested loop or switch into a function instead of using \`continue\` here.␊
      4 | 	} while (items[key].pending);␊
      5 | }␊
    `
