# Snapshot report for `test/no-useless-length-check.mjs`

The actual snapshot is saved in `no-useless-length-check.mjs.snap`.

Generated by [AVA](https://avajs.dev).

## Invalid #1
      1 | array.length === 0 || array.every(Boolean)

> Output

    `␊
      1 | array.every(Boolean)␊
    `

> Error 1/1

    `␊
    > 1 | array.length === 0 || array.every(Boolean)␊
        |       ^^^^^^ \`Array#every()\` returns \`true\` on empty array, the non-empty check is not needed.␊
    `

## Invalid #2
      1 | array.length > 0 && array.some(Boolean)

> Output

    `␊
      1 | array.some(Boolean)␊
    `

> Error 1/1

    `␊
    > 1 | array.length > 0 && array.some(Boolean)␊
        |       ^^^^^^ \`Array#some()\` returns \`false\` on empty array, the empty check is not needed.␊
    `

## Invalid #3
      1 | array.length !== 0 && array.some(Boolean)

> Output

    `␊
      1 | array.some(Boolean)␊
    `

> Error 1/1

    `␊
    > 1 | array.length !== 0 && array.some(Boolean)␊
        |       ^^^^^^ \`Array#some()\` returns \`false\` on empty array, the empty check is not needed.␊
    `

## Invalid #4
      1 | ((
      2 | 	((
      3 | 		(( array )).length
      4 | 	)) === (( 0 ))
      5 | 	||
      6 | 	((
      7 | 		(( array )).every(Boolean)
      8 | 	))
      9 | ))

> Output

    `␊
      1 | ((␊
      2 | 	((␊
      3 | 		(( array )).every(Boolean)␊
      4 | 	))␊
      5 | ))␊
    `

> Error 1/1

    `␊
      1 | ((␊
      2 | 	((␊
    > 3 | 		(( array )).length␊
        | 		            ^^^^^^ \`Array#every()\` returns \`true\` on empty array, the non-empty check is not needed.␊
      4 | 	)) === (( 0 ))␊
      5 | 	||␊
      6 | 	((␊
      7 | 		(( array )).every(Boolean)␊
      8 | 	))␊
      9 | ))␊
    `

## Invalid #5
      1 | ((
      2 | 	((
      3 | 		(( array )).every(Boolean)
      4 | 	))
      5 | 	||
      6 | 	((
      7 | 		(( array )).length
      8 | 	)) === (( 0 ))
      9 | ))

> Output

    `␊
      1 | ((␊
      2 | 	((␊
      3 | 		(( array )).every(Boolean)␊
      4 | 	))␊
      5 | ))␊
    `

> Error 1/1

    `␊
      1 | ((␊
      2 | 	((␊
      3 | 		(( array )).every(Boolean)␊
      4 | 	))␊
      5 | 	||␊
      6 | 	((␊
    > 7 | 		(( array )).length␊
        | 		            ^^^^^^ \`Array#every()\` returns \`true\` on empty array, the non-empty check is not needed.␊
      8 | 	)) === (( 0 ))␊
      9 | ))␊
    `

## Invalid #6
      1 | if ((( array.length > 0 )) && array.some(Boolean));

> Output

    `␊
      1 | if (array.some(Boolean));␊
    `

> Error 1/1

    `␊
    > 1 | if ((( array.length > 0 )) && array.some(Boolean));␊
        |              ^^^^^^ \`Array#some()\` returns \`false\` on empty array, the empty check is not needed.␊
    `

## Invalid #7
      1 | if (
      2 | 	array.length !== 0 &&
      3 | 	array.some(Boolean) &&
      4 | 	foo
      5 | ) {
      6 | 	// ...
      7 | }

> Output

    `␊
      1 | if (␊
      2 | 	array.some(Boolean) &&␊
      3 | 	foo␊
      4 | ) {␊
      5 | 	// ...␊
      6 | }␊
    `

> Error 1/1

    `␊
      1 | if (␊
    > 2 | 	array.length !== 0 &&␊
        | 	      ^^^^^^ \`Array#some()\` returns \`false\` on empty array, the empty check is not needed.␊
      3 | 	array.some(Boolean) &&␊
      4 | 	foo␊
      5 | ) {␊
      6 | 	// ...␊
      7 | }␊
    `

## Invalid #8
      1 | (array.length === 0 || array.every(Boolean)) || foo

> Output

    `␊
      1 | (array.every(Boolean)) || foo␊
    `

> Error 1/1

    `␊
    > 1 | (array.length === 0 || array.every(Boolean)) || foo␊
        |        ^^^^^^ \`Array#every()\` returns \`true\` on empty array, the non-empty check is not needed.␊
    `

## Invalid #9
      1 | foo || (array.length === 0 || array.every(Boolean))

> Output

    `␊
      1 | foo || (array.every(Boolean))␊
    `

> Error 1/1

    `␊
    > 1 | foo || (array.length === 0 || array.every(Boolean))␊
        |               ^^^^^^ \`Array#every()\` returns \`true\` on empty array, the non-empty check is not needed.␊
    `

## Invalid #10
      1 | (array.length > 0 && array.some(Boolean)) && foo

> Output

    `␊
      1 | (array.some(Boolean)) && foo␊
    `

> Error 1/1

    `␊
    > 1 | (array.length > 0 && array.some(Boolean)) && foo␊
        |        ^^^^^^ \`Array#some()\` returns \`false\` on empty array, the empty check is not needed.␊
    `

## Invalid #11
      1 | foo && (array.length > 0 && array.some(Boolean))

> Output

    `␊
      1 | foo && (array.some(Boolean))␊
    `

> Error 1/1

    `␊
    > 1 | foo && (array.length > 0 && array.some(Boolean))␊
        |               ^^^^^^ \`Array#some()\` returns \`false\` on empty array, the empty check is not needed.␊
    `

## Invalid #12
      1 | array.every(Boolean) || array.length === 0

> Output

    `␊
      1 | array.every(Boolean)␊
    `

> Error 1/1

    `␊
    > 1 | array.every(Boolean) || array.length === 0␊
        |                               ^^^^^^ \`Array#every()\` returns \`true\` on empty array, the non-empty check is not needed.␊
    `

## Invalid #13
      1 | array.some(Boolean) && array.length !== 0

> Output

    `␊
      1 | array.some(Boolean)␊
    `

> Error 1/1

    `␊
    > 1 | array.some(Boolean) && array.length !== 0␊
        |                              ^^^^^^ \`Array#some()\` returns \`false\` on empty array, the empty check is not needed.␊
    `

## Invalid #14
      1 | array.some(Boolean) && array.length > 0

> Output

    `␊
      1 | array.some(Boolean)␊
    `

> Error 1/1

    `␊
    > 1 | array.some(Boolean) && array.length > 0␊
        |                              ^^^^^^ \`Array#some()\` returns \`false\` on empty array, the empty check is not needed.␊
    `

## Invalid #15
      1 | foo && array.length > 0 && array.some(Boolean)

> Output

    `␊
      1 | foo && array.some(Boolean)␊
    `

> Error 1/1

    `␊
    > 1 | foo && array.length > 0 && array.some(Boolean)␊
        |              ^^^^^^ \`Array#some()\` returns \`false\` on empty array, the empty check is not needed.␊
    `

## Invalid #16
      1 | foo || array.length === 0 || array.every(Boolean)

> Output

    `␊
      1 | foo || array.every(Boolean)␊
    `

> Error 1/1

    `␊
    > 1 | foo || array.length === 0 || array.every(Boolean)␊
        |              ^^^^^^ \`Array#every()\` returns \`true\` on empty array, the non-empty check is not needed.␊
    `

## Invalid #17
      1 | (foo || array.length === 0) || array.every(Boolean)

> Output

    `␊
      1 | (foo) || array.every(Boolean)␊
    `

> Error 1/1

    `␊
    > 1 | (foo || array.length === 0) || array.every(Boolean)␊
        |               ^^^^^^ \`Array#every()\` returns \`true\` on empty array, the non-empty check is not needed.␊
    `

## Invalid #18
      1 | array.length === 0 || (array.every(Boolean) || foo)

> Output

    `␊
      1 | (array.every(Boolean) || foo)␊
    `

> Error 1/1

    `␊
    > 1 | array.length === 0 || (array.every(Boolean) || foo)␊
        |       ^^^^^^ \`Array#every()\` returns \`true\` on empty array, the non-empty check is not needed.␊
    `

## Invalid #19
      1 | (foo && array.length > 0) && array.some(Boolean)

> Output

    `␊
      1 | (foo) && array.some(Boolean)␊
    `

> Error 1/1

    `␊
    > 1 | (foo && array.length > 0) && array.some(Boolean)␊
        |               ^^^^^^ \`Array#some()\` returns \`false\` on empty array, the empty check is not needed.␊
    `

## Invalid #20
      1 | array.length > 0 && (array.some(Boolean) && foo)

> Output

    `␊
      1 | (array.some(Boolean) && foo)␊
    `

> Error 1/1

    `␊
    > 1 | array.length > 0 && (array.some(Boolean) && foo)␊
        |       ^^^^^^ \`Array#some()\` returns \`false\` on empty array, the empty check is not needed.␊
    `

## Invalid #21
      1 | array.every(Boolean) || array.length === 0 || array.every(Boolean)

> Output

    `␊
      1 | array.every(Boolean) || array.every(Boolean)␊
    `

> Error 1/1

    `␊
    > 1 | array.every(Boolean) || array.length === 0 || array.every(Boolean)␊
        |                               ^^^^^^ \`Array#every()\` returns \`true\` on empty array, the non-empty check is not needed.␊
    `

## Invalid #22
      1 | array.length === 0 || array.every(Boolean) || array.length === 0 || array.every(Boolean)

> Output

    `␊
      1 | array.every(Boolean) || array.every(Boolean)␊
    `

> Error 1/2

    `␊
    > 1 | array.length === 0 || array.every(Boolean) || array.length === 0 || array.every(Boolean)␊
        |       ^^^^^^ \`Array#every()\` returns \`true\` on empty array, the non-empty check is not needed.␊
    `

> Error 2/2

    `␊
    > 1 | array.length === 0 || array.every(Boolean) || array.length === 0 || array.every(Boolean)␊
        |                                                     ^^^^^^ \`Array#every()\` returns \`true\` on empty array, the non-empty check is not needed.␊
    `

## Invalid #23
      1 | array1.every(Boolean)
      2 | || (array1.length === 0 || array2.length === 0) // Both useless
      3 | || array2.every(Boolean)

> Output

    `␊
      1 | array1.every(Boolean) // Both useless␊
      2 | || array2.every(Boolean)␊
    `

> Error 1/2

    `␊
      1 | array1.every(Boolean)␊
    > 2 | || (array1.length === 0 || array2.length === 0) // Both useless␊
        |            ^^^^^^ \`Array#every()\` returns \`true\` on empty array, the non-empty check is not needed.␊
      3 | || array2.every(Boolean)␊
    `

> Error 2/2

    `␊
      1 | array1.every(Boolean)␊
    > 2 | || (array1.length === 0 || array2.length === 0) // Both useless␊
        |                                   ^^^^^^ \`Array#every()\` returns \`true\` on empty array, the non-empty check is not needed.␊
      3 | || array2.every(Boolean)␊
    `
